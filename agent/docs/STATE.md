# ALR — Current State

**🏷️ Brand (2026-09-02): produk sekarang bernama "Titian Asa"**, tagline "Pasti Ada Jalannya!" — rebrand dari "TITIAN" polos, diminta user khusus buat landing page (`titian-web`) tapi diterapkan konsisten di semua surface user-facing: landing (`src/app/page.tsx`, `src/components/landing/landing-hero.tsx`), metadata+manifest (`src/app/layout.tsx`, `src/app/manifest.ts`), login (`src/app/login/page.tsx`, `src/components/auth/login-card.tsx`), top bar app-shell (`src/components/layout/top-bar.tsx`), default user-store placeholder. Nama repo/package (`titian-web`, `titian-backend-bun`) TIDAK diubah — itu identifier internal, bukan brand yang user lihat, tidak diminta juga. Icon/favicon (masih monogram "T") TIDAK diregenerasi — di luar scope, "T" tetap valid buat "Titian Asa". Kalau nulis copy/UI baru ke depan, pakai "Titian Asa" (bukan "TITIAN") dan tagline "Pasti Ada Jalannya!" kalau relevan.

Last updated: 2026-09-02 (**Phase 6 CLOSED**: all 5 tickets (P6-001 AIProvider audio capabilities, P6-002 speaking attempt + rubric evaluation, P6-003 AI Tutor prompt audio + correction, P6-004 FE recording UI, P6-005 test suite + exit checkpoint) done in `titian-backend-bun` + `titian-web` — all 5 Phase 6 exit checkpoint items checked, every one demonstrated live against the real OpenRouter provider (Whisper STT + Kokoro TTS + DeepSeek evaluation), not `FakeAIProvider` alone. 250/250 backend tests passing. **ADR-0010** records the provider decision, backed by a live smoke test that caught `openai/whisper-large-v3` silently truncating transcripts (a real provider defect) — `openai/whisper-1` is the default instead. **1 real checkpoint gap found and closed while finishing this phase**: checkpoint #3 needed a working "Try Again" that `SpeakingAttempt` (built in P6-004) never had — added as part of P6-005's closing work, verified live via 2 sequential real attempts through `Bun.WebView`. This is the SECOND time in a row a checkpoint-closing session found a real UI gap a build ticket left behind (Phase 4's P4-005 found the same pattern for mastery drill-down) — worth remembering as a recurring blind spot: a ticket's own AC being met doesn't guarantee the *combined* checkpoint is, so the closing ticket must actually re-verify each checkpoint item itself, not just check that each ticket's own tests pass. **Phase 6 jumped ahead of the already-written Phase 5** (`docs/tickets/phase-5.md`, Retrieval Variation + prerequisite integration into `/learning-queue`) after the user gave an explicit provider decision for Speaking/AI Tutor right after Phase 5 was scoped to defer it — Phase 5 is next, unchanged and still valid. **Phase 2/3/4 CLOSED** in earlier sessions. `titian-web` migrated from pnpm/Node to full Bun (package manager + runtime) in an earlier session. Rust → Bun backend migration + frontend cutover remain complete; Rust backend retirement is still the only open decision from that. See "Next action".)

## 🟢 Migrasi Backend: Rust → Bun+ElysiaJS (ADR-0009) — SELESAI, `titian-backend-bun` SEKARANG BACKEND AKTIF
**Baca ini dulu sebelum baca bagian lain di file ini.** User eksplisit minta backend diganti total dari Rust/Axum/sqlx ke Bun+ElysiaJS+Drizzle ORM, di repo baru `titian-backend-bun/`, lalu eksplisit minta cutover penuh ("pindah full ke bun"). Ini BUKAN fitur baru di atas Phase 1/2 — ini rewrite total infrastruktur, jadi status "Fase aktif" di bawah (Phase 1/2 Rust) tetap **akurat sebagai catatan apa yang sudah dibangun**, tapi tidak lagi menggambarkan backend yang aktif dipakai sekarang. Semua 7 fase build-order + cutover frontend sudah selesai (lihat "🟢 MIGRASI SELESAI"/"🟢 CUTOVER SELESAI" di bawah). `titian-backend` (Rust) masih ada di disk sebagai reference, belum di-retire.

- **Dokumentasi (`agent/`) sudah disesuaikan** (2026-08-30): ADR-0009 baru ditulis (keputusan lengkap: apa yang berubah/tidak berubah, kenapa Drizzle bukan Prisma, kenapa `titian-backend` tidak dihapus dulu). ADR-0004/0006 (contoh kode Rust) dan `domain-model.md` (referensi sqlx) diupdate ke padanan TypeScript/Drizzle — **isi keputusan ADR-nya sendiri tidak berubah**, cuma referensi bahasa/tooling. `ALR_Build_Roadmap.md`/`ALR_Detailed_Blueprint.md`/`ALR_Phase_Detail_Breakdown.md` dan `docs/tickets/phase-0/1/2.md` dapat catatan pointer ke ADR-0009 di bagian atas — isi historisnya **tidak ditulis ulang** (tetap catatan akurat implementasi Rust asli).
- **Kode (`titian-backend-bun/`) — progres akan diupdate di bagian ini tiap fase build-order selesai.** Urutan (dari plan yang disetujui user): (1) foundation (scaffold+Drizzle schema+migrations+health check), (2) identity & auth, (3) content/curriculum/questions/Drive (fase terbesar), (4) assessment & learning engine (mastery/FRSS), (5) AI gateway, (6) integrasi routes penuh + full test suite, (7) verifikasi browser asli (Playwright) sebelum dianggap setara.
  - **Fase 1 (foundation) SELESAI** (2026-08-30, commit `02a1700` di `titian-backend-bun`): skema Drizzle 36 tabel di `src/db/schema.ts` — ported kolom-per-kolom dari `domain-model.md`, diverifikasi lewat `psql \d` tabel-per-tabel (constraint/index/FK cocok persis). `drizzle-kit generate`+`migrate` jalan ke database `titian_bun` (container Postgres terpisah `titian-bun-postgres`, port 5433 — lihat catatan port di bawah). `config.ts`, `error.ts` (AppError + envelope sesuai `api-contract.md`), `middleware/auth-context.ts` (verifikasi JWT pakai `jose`), health service/repository/handler, CORS+request-id di `index.ts` (method allowlist GET/POST/PUT/DELETE sejak awal — pelajaran dari 2x bug CORS di Rust). 2 smoke test (`bun:test`, ported dari `tests/health_test.rs`) lulus ke Postgres asli; server juga dites langsung lewat `curl` ke `http://localhost:8081/health` (bukan cuma test hijau).
  - **Catatan lingkungan**: container `titian-postgres` (punya `titian-backend` Rust, port 5432) lagi stopped dan tidak bisa distart karena port 5432 sudah dipakai container `parelabs_db` (proyek lain, sedang jalan) — bukan disebabkan sesi ini, tidak disentuh. Untuk `titian-backend-bun`, dibuat container baru terpisah `titian-bun-postgres` di port 5433 (`docker run ... -p 5433:5432 postgres:16-alpine`), tidak mengganggu proyek lain maupun `titian-backend`. **Ini perlu diselesaikan sebelum build-order fase 7** (verifikasi browser asli — jalankan Rust dan Bun side-by-side): entah bebaskan port 5432 dari `parelabs_db`, atau jalankan `titian-backend` Rust juga di port lain untuk sesi verifikasi itu. Belum dibahas ke user.
  - **Fase 2 (identity & auth) SELESAI** (2026-08-30, commit `0400d67` di `titian-backend-bun`): `google_oauth.ts` (GoogleTokenVerifier pakai `jose` — `createRemoteJWKSet` di produksi, seeded-JWK di test biar gak pernah hit JWKS live Google), `auth_repository.ts`/`auth_service.ts`/`auth_handler.ts` (Google callback find-or-create user, refresh-token issue/verify/reject), `permissions.ts` (matrix RBAC ADR-0006 lengkap: `Resource`/`Action` union type + `isAllowed`/`requirePermission`/`requirePermissionInOrg`, termasuk bypass org-scoping buat `platform_admin`), `organization_repository.ts`/`organization_handler.ts` (`GET /organizations/{id}/members` keyset-paginated), `user_handler.ts` (`GET /users/me`). `app.ts` dapat `getAuthContext` lazy-derive (padanan Elysia buat extractor `AuthContext` Axum, sesuai catatan di ADR-0006). 20 test baru ported dari `tests/auth_test.rs` + `tests/user_organization_test.rs`, semua lulus ke `titian_bun` asli lewat helper `withTx` (transaction-rollback per test — padanan praktis `#[sqlx::test]`). Total 22/22 test lulus. Dites juga langsung lewat `curl` ke server asli — error envelope `/users/me`, `/auth/refresh`, `/auth/google/callback` cocok persis `api-contract.md`.
  - **Fase 3 (content/curriculum/questions/Drive) SELESAI** (2026-08-30, commit `f283e92` di `titian-backend-bun`, dipecah jadi 5 slice biar setiap potongan tetap dites+commit terpisah): (1) `concept_repository.ts`/`concept_service.ts` — hierarki konsep ADR-0007 (`WITH RECURSIVE`, cycle check di `setParent`); (2) modul pure-logic — `publish_flow.ts` (state machine draft→in_review→published), `block_schema.ts` (registry validator content_blocks.type), `alm_parser.ts` (parser ALM lengkap: directive block, heading, blockquote-as-example, image syntax), `paste_normalizer.ts` (HTML→ALM biar hasil AST identik dengan Markdown tulisan tangan); (3) `curriculum_constitution.ts` (Grammar Constitution 11-section, numeric-prefix matching) + `question_schema.ts` (registry mcq/fill_blank/matching); (4) `content_repository.ts`/`question_repository.ts`/`content_block_service.ts`/`content_qa_service.ts`/`content_service.ts`/`question_service.ts` + handler + ~20 route (curricula/levels/units/lessons/question-banks/questions, publish flow lengkap); (5) Drive — `storage.ts` (R2Storage asli via `@aws-sdk/client-s3`, `InMemoryStorage` buat test), `folder_repository.ts`/`resource_share_repository.ts`/`resource_activity_repository.ts`/`asset_repository.ts`, `drive_permissions.ts` (per-resource access control, beda dari role matrix `permissions.ts`), `drive_service.ts`/`asset_service.ts` + handler + ~30 route (`/assets*`, `/folders*`, `/drive*`).
    - **2 bug nyata ketemu & fixed selama porting** (bukan cuma typecheck — ketauan dari test yang di-port): (a) `content_handler.getLesson` awalnya return field camelCase (`rawSource`/`qaReport`) langsung, bukan snake_case sesuai `api-contract.md` (`raw_source`/`qa_report`) — ketauan dari test `content-authoring.test.ts`. (b) `sql\`... = any(\${array})\`` TIDAK bind sebagai Postgres array di driver `bun-sql` — jadi `any(($1, $2))`, syntax error — diganti pakai helper `inArray()` Drizzle di `folder_repository`/`asset_repository`; ketauan dari test cascade-delete di `drive.test.ts`.
    - 61 test baru ported dari `concept_hierarchy_test.rs` + inline test 4 modul pure-logic + `content_authoring_test.rs` + `content_qa_test.rs` + `drive_test.rs`. Total sekarang: **93/93 test lulus**. Server juga dites langsung lewat `curl`/browser-less HTTP: boot dengan kredensial R2 asli, semua route baru 401 tanpa token sesuai kontrak.
  - **Fase 4 (assessment & learning engine) SELESAI** (2026-08-30, commit `70da2a3` di `titian-backend-bun`): `mastery.ts`/`frss.ts`/`grading.ts` (pure math, no DB) — `mastery.compute` reproduce `mastery_score = 72` PERSIS (worked example ADR-0002), `frss.apply` reproduce tabel 5-siklus SM-2 PERSIS (ADR-0003), keduanya DoD ADR itu sendiri, dites di `mastery.test.ts`/`frss.test.ts`. `assessment_repository.ts`/`mastery_repository.ts`/`frss_repository.ts`/`learning_event_repository.ts` (upsert ON CONFLICT, review-queue join hormatin `min_gap_hours` via `masteries.last_reviewed_at`). `assessment_service.ts` (create-attempt tanpa bocorin `correct_answer`, submit-attempt: grading mcq/fill_blank, 1 learning_event per soal, recompute mastery tiap concept tersentuh, 1 update FRSS per concept pakai rata-rata correctness submit itu, snapshot soal ADR-0008). `mastery_service.ts`/`frss_service.ts` (`GET /mastery/{id}` + `GET /review-queue`). Handler + 5 route baru.
    - **Bug sistemik nyata ketemu & fixed** (bukan cuma typecheck — ketauan dari test yang di-port, `mastery_repository.findEventsForConcept` yang pakai raw SQL `payload->>'correct'`): kolom `jsonb()` bawaan drizzle-orm nge-`JSON.stringify` value SEBELUM dikirim ke driver, tapi `Bun.SQL` (driver native Bun) JUGA auto-serialize string parameter yang di-bind ke kolom jsonb — dua-duanya gabung jadi **double-encode SEMUA tulisan jsonb di seluruh schema**: Postgres nyimpen jsonb *scalar string* (teks JSON yang di-quote+escape) bukan object asli. `db.select()` biasa "kebetulan benar" karena `mapFromDriverValue` Drizzle nge-`JSON.parse` sekali di sisi baca (nutupin satu lapis double-encoding itu) — makanya lolos di semua test Fase 1-3 sebelumnya — tapi query SQL mentah yang pakai operator jsonb asli (`->>`, dll) kena corrupt-nya beneran, diam-diam return `null`. **Fix**: custom Drizzle column type di `src/db/columns.ts` yang pass-through murni (tidak stringify sama sekali) — `dataType()` tetap `"jsonb"` jadi TIDAK butuh migration baru (dikonfirmasi `drizzle-kit generate` → "No schema changes", plus verifikasi independen lewat `psql`/`jsonb_typeof`). Ini fix penting yang berlaku untuk SEMUA kolom jsonb di seluruh backend, bukan cuma yang di fase 4.
    - 27 test baru (12 pure-logic + port lengkap `assessment_test.rs`/`learning_test.rs` termasuk checkpoint wiring ADR-0002/ADR-0003 yang persis). Total sekarang: **127/127 test lulus**. Dites juga lewat `curl` — `/assessments/{id}` dan `/review-queue` 401 tanpa token.
  - **Fase 5 (AI gateway) SELESAI** (2026-08-30, commit `7071800` di `titian-backend-bun`): `ai_provider.ts` (interface `AIProvider`, `stripCodeFence`, `DeepSeekProvider` asli lewat OpenRouter via `fetch`, `FakeAIProvider` buat test). `ai_task_repository.ts`/`economy_repository.ts` (audit trail `ai_tasks`, `credits`/`transactions` ADR-0005). `ai_gateway_service.ts` (`POST /ai/evaluate` — cek kredit sebelum panggil provider, validasi output sesudahnya, tracking usage baik gagal/berhasil). `ai_content_service.ts` (`generateLesson`/`generateQuestions` — Blueprint→Generate→Validate→tulis lewat `content_service`/`question_service` yang sama dengan authoring manual→catat `ai_tasks`; 0 kredit dicharge karena cost platform bukan user per ADR-0005; validasi output AI SEBELUM nulis apa pun ke DB, generation gagal tidak pernah nyisain row setengah jadi; port juga prompt-engineering fix "contoh JSON terisi penuh, bukan placeholder abstrak" yang sudah kebukti kerja di sesi Rust). Handler + 3 route baru (`/ai/evaluate`, `/ai/generate-lesson`, `/ai/generate-questions`). 18 test baru ported dari `ai_gateway_test.rs` + `ai_content_test.rs`.
  - **Fase 6 (integrasi) — checkpoint end-to-end SELESAI** (2026-08-30, commit `0d16c14`): semua 52 path route di `titian-backend-bun/src/app.ts` dicek SATU-SATU cocok persis dengan `titian-backend/src/routes/mod.rs` (tidak ada yang kelewat). `tests/checkpoint.test.ts` — port `phase1_checkpoint_test.rs`, 1 test end-to-end yang nyambungin SEMUA fase dalam satu alur: login→lihat curriculum tree→submit assessment 3 soal MCQ→score otomatis→`learning_events`→`masteries`+`frss_schedule` (critical path)→`/ai/evaluate`→`ai_tasks`+`transactions`. **Lulus di percobaan pertama** — konfirmasi kuat semua fase benar-benar nyambung, bukan cuma lulus sendiri-sendiri. Total sekarang: **146/146 test lulus**.
  - **Fase 7 (verifikasi browser asli) SELESAI** (2026-08-31, commit `9bf13c5`+`786c937` di `titian-backend-bun`): dijalankan TANPA perlu jalanin `titian-backend` Rust bareng-bareng — verifikasinya adalah "backend Bun ini beneran jalan lewat browser asli, pakai frontend asli yang sama, flow yang sama yang udah kebukti kerja," bukan diff langsung sisi-ke-sisi vs Rust. `titian-backend-bun` dijalankan di port 8081, `titian-web` di port 3001 (port 3000 dipakai proyek lain), `NEXT_PUBLIC_API_URL`/`FRONTEND_ORIGIN` di-edit SEMENTARA (file gitignored, sudah dikembalikan setelah selesai). Auth di-bootstrap dengan cara yang sama seperti test backend (mint refresh token langsung ke DB, bukan OAuth Google asli — headless browser gak bisa login Google beneran), tapi lewat `Playwright` browser session (`localStorage`), bukan cuma HTTP client. Data seed dev asli (`titian-backend/seed.sql` — curriculum/lesson/question bank/assessment dengan UUID tetap yang di-hardcode di frontend) di-load ke `titian_bun` buat dapet halaman yang beneran ada isinya, bukan kosong.
    - **Verifikasi mencakup**: beranda (progress card asli), Content Studio (curricula list, curriculum detail dengan tree lengkap, lesson editor nampilin ALM content asli + badge Published + notice ADR-0008), Question Bank (list + detail 3 soal MCQ asli), Drive/Media (buat folder, upload file asli ke R2 — file beneran muncul di listing), 4 halaman learner shell (belajar/latihan/progres/profil), viewport mobile 390px (no horizontal overflow). Zero console error, zero page error di semua langkah setelah bug fix di bawah.
    - **1 bug produksi nyata ketemu & fixed** (baru ketauan lewat browser asli, BUKAN lewat 146 test yang udah lulus): bikin folder root-level di Drive UI return 500. Root cause: frontend (`driveApi.createFolder`/`moveFolder`/`moveAsset`) selalu kirim `parent_folder_id`/`folder_id` sebagai JSON `null` eksplisit buat "tidak ada parent"/"pindah ke root" (natural buat `Option<Uuid>` Rust via serde), tapi skema body `t.Optional(t.String())` Elysia CUMA nerima key yang di-omit, BUKAN `null` eksplisit — TypeBox nolak requestnya duluan sebelum sampe ke handler. **Fix**: bungkus field yang kena (`POST /folders`, `POST /folders/{id}/move`, `POST /assets/{id}/move`, `POST /assets/confirm`) dengan `t.Nullable(...)` juga di `app.ts`, lebarin tipe TS terkait di handler jadi `string | null`.
    - **Kenapa 146 test yang udah lulus gak ketauan bug ini**: helper test `createFolder` di `drive.test.ts` sendiri pakai `parent ?? undefined` — yang bikin `JSON.stringify` BUANG key-nya sama sekali, bukan kirim `null` beneran. Jadi test selama ini nge-tes bentuk wire yang BEDA dari yang beneran dikirim frontend. **Sudah diperbaiki**: helper sekarang kirim `parent` apa adanya, plus 1 test regresi baru eksplisit buat skenario null persis. **Dikonfirmasi manjur**: revert fix sementara → 10/10 test `drive.test.ts` gagal; restore fix → lulus lagi.
    - Total sekarang: **147/147 test lulus**, plus 1 bug produksi nyata ketemu+fixed+dikasih regression test yang gak akan ketauan tanpa verifikasi browser asli — bukti langsung kenapa langkah ini bukan cuma formalitas.
- **🟢 MIGRASI SELESAI (2026-08-31)**: semua 7 fase build-order (foundation, identity & auth, content/curriculum/questions/Drive, assessment & learning engine, AI gateway, integrasi, verifikasi browser asli) sudah selesai dan terverifikasi. `titian-backend-bun` sekarang setara fungsional dengan `titian-backend` (Rust) — 147 test lulus, semua 52 route match, 1 checkpoint end-to-end lulus, verifikasi browser asli lulus (termasuk nemu+fix 1 bug produksi nyata).
- **🟢 CUTOVER SELESAI (2026-08-31, user eksplisit minta "pindah full ke bun")**: `titian-web` sekarang default ke `titian-backend-bun` (`NEXT_PUBLIC_API_URL=http://localhost:8081`) — diubah di `.env.local` (dev lokal), `.env.example` (template buat dev lain), dan fallback default di `src/lib/api-client.ts`. `titian-backend` (Rust, port 8080) **TIDAK dihapus** — tetap hidup di disk sebagai reference implementation; keputusan retire/archive belum diambil eksplisit, ditunda sampai user secara spesifik minta hapus (bukan sekadar "pindah" — dua hal beda, "pindah" = cutover trafik, "hapus" = one-way door terpisah). `titian-backend-bun` sekarang backend aktif satu-satunya buat pengembangan sehari-hari.
- **`titian-backend` (Rust) TIDAK dihapus** — tetap jalan sebagai reference implementation sampai portingan Bun terverifikasi setara lewat browser asli, bukan cuma `bun test` hijau. Keputusan retire Rust backend menyusul terpisah setelah itu.
- **Stack baru**: Bun + ElysiaJS + **Drizzle ORM** (bukan Prisma — user ditanya eksplisit, pilih Drizzle karena gaya query-nya dekat SQL mentah, cocok buat porting kode sqlx yang sekarang, dan pasangan standar Bun+Elysia) via driver `drizzle-orm/bun-sql`. Auth pakai `jose` (ganti `jsonwebtoken` + hand-rolled JWKS fetch di `google_oauth.rs`). R2 pakai `@aws-sdk/client-s3` (JS SDK, sama keluarga dengan `aws-sdk-s3` Rust). Test pakai `bun:test`, isolasi per-test lewat transaction rollback (padanan praktis `#[sqlx::test]`'s ephemeral DB).

## Fase aktif
**Phase 1 — Backend Core resmi tertutup** (semua 13 ticket P1-001 s/d P1-013 done, semua 5 checkpoint keluar Phase 1 dicentang — lihat `docs/tickets/phase-1.md`, diverifikasi ulang 2026-08-23: 79 test lulus, working tree bersih di `titian-backend`/`titian-web`/`alr`). Phase 0 belum tertutup formal (P0-010 CI dan mobile scaffold masih terbuka) — ini disengaja atas instruksi eksplisit user, lihat "Deviasi".

**🟢 Phase 2 — Content Engine & Curriculum Pipeline: SELESAI, semua 17 ticket done, semua 5 checkpoint keluar Phase 2 dicentang** (`docs/tickets/phase-2.md`). Konsep hierarchy, versioning, block SDK, question registry, publish flow, ALM parser, paste normalizer, lesson authoring API, curriculum authoring API, presigned R2 upload, Grammar Constitution 11-section gate, 3 block type Indonesian learner support, **AI Content Generation Pipeline (lesson+question), Content QA Agent, OCR-to-Question Pipeline, dan validasi 1 modul nyata penuh ("Pre-Basic — Alphabet")**. Buktinya sudah hidup: admin bisa bikin curriculum→level→unit→lesson via ALM lewat API dan langsung muncul di `GET /curricula/{id}/tree`/`GET /lessons/{id}`; grammar lesson lengkap 11-section lolos submit-review, yang kurang 1 section ditolak jelas menyebut bagian yang hilang; **`POST /ai/generate-lesson`/`POST /ai/generate-questions` sudah dites ke DeepSeek/OpenRouter asli — bisa generate 1 lesson grammar lengkap 11-section dan 3 soal mcq valid dalam sekali panggil**, QA Agent otomatis attach laporan (pass/fail) di `submit-review` tanpa pernah memblokir transisi; **`POST /ai/ocr-to-question`** (P2-015) mengubah asset gambar hasil scan jadi draft `questions` lewat AI Gateway yang sama, tidak pernah auto-publish; **modul "Pre-Basic — Alphabet" (P2-016) benar-benar live** di `titian_bun` — 6 lesson (`learn`/`practice`/`speaking`/`writing`/`review`/`assessment`), 1 assessment nyata yang bisa dikerjakan end-to-end (attempt→submit→**score 100**), dibangun lewat HTTP asli ke server yang jalan sungguhan, bukan seed data. P2-017 menutup 20+ gap coverage nyata yang keluput dari migrasi ADR-0009 (lihat "Ticket selesai").

**🟢 Phase 3 — Exercise Engine (core): SELESAI, semua 5 ticket done, semua 4 checkpoint keluar Phase 3 dicentang** (`docs/tickets/phase-3.md`). Scope-nya sengaja dipersempit dari roadmap-Fase-4 penuh ("Exercise Engine & 4 Skills") lewat riset + keputusan eksplisit user (lihat ticket file-nya bagian "Keputusan scope") — speaking/AI Tutor, Micro Learning, Kids Mode ditunda ke ticket-Phase-4. Yang dibangun: `POST /questions/{id}/check` (jawab soal inline, langsung ke learning engine yang sama dengan attempt formal — **1 bug nyata ditemukan+diperbaiki**, `grading.ts` ternyata tidak pernah dukung tipe soal `matching`); `QuestionRenderer` registry + **halaman lesson viewer sisi learner pertama yang pernah ada** (`LessonRow` di tree Belajar dulu `<div>` statis); assessment-taking UI (mem-wire `POST /assessments/{id}/attempts`/`submit` yang sudah ada sejak Phase 1 tapi belum pernah dipanggil FE manapun, plus block type baru `assessment_embed` menutup gap struktural yang P2-016 sendiri tinggalkan); writing submission + rubric-based AI evaluation (**kode pertama yang menyentuh `evaluations`/`feedback`/`rubrics`** sejak dibuat Phase 0). Semua diverifikasi lewat Playwright ke server asli — P3-004 bahkan ke provider AI **asli** (bukan `FakeAIProvider`), submit essay sengaja salah tense dapat balik koreksi grammar yang semuanya benar. 201/201 test lulus. Detail lengkap tiap ticket di "Ticket selesai".

**🟢 Phase 4 — Core Learning Loop (roadmap-Fase-3 remainder): SELESAI, semua 5 ticket done, semua 5 checkpoint keluar tercentang.** User pilih "Lengkapi Core Learning Loop" ketika ditanya prioritas berikutnya setelah Phase 3 ditutup. `docs/tickets/phase-4.md` — 5 ticket (P4-001 weakness detection drill-down §3.1, P4-002 Personal Learning Queue §3.6, P4-003 prerequisite graph §3.2, P4-004 Lesson Packet §3.5, P4-005 test suite+checkpoint), scope-nya sengaja dipersempit ke 4 item MVP-first pertama roadmap-Fase-3 (§3.3/3.4/3.7 butuh data historis yang belum ada, §3.8/3.9 terhubung fase lain + §3.9 butuh ADR baru) — lihat ticket file-nya bagian "Keputusan scope" untuk detail lengkap termasuk penjelasan penomoran roadmap-vs-ticket yang membingungkan (sekarang 3 thread berbeda). **P4-001** (`GET /concepts/{id}/mastery-breakdown`, `titian-backend-bun`, 2026-08-31): drill-down 2 level lewat `concept_repository.findDescendantsUpTo` (varian depth-bounded dari `findDescendants`) + `mastery_service.getMasteryBreakdown` yang reuse aturan `insufficient_data` dari `getMastery` yang sudah ada — node tanpa data/confidence rendah tidak pernah di-flag `weak`. Threshold baru `weaknessScoreThreshold` (config, default 60.0) ikut pola `masteryConfidenceThreshold`. **P4-002** (`GET /learning-queue`, `titian-backend-bun` + `titian-web`, 2026-08-31): merge `frss_repository.findDue` (due) dengan `mastery_repository.findWeak` (baru) jadi 3 bucket prioritas (`critical`/`due`/`weak`) di service baru `learning_queue_service.ts`. FE: tombol "Review" di Latihan — **sejak awal proyek tidak pernah punya `onClick`** — akhirnya benar-benar buka `ReviewSessionDialog` baru yang render soal lewat `QuestionCheck` (P3-002) yang sudah ada, tidak ada grading/attempt machinery baru. Diverifikasi end-to-end lewat Playwright ke server asli (seed 2 concept demo, klik Review, jawab benar, dapat "Benar!", data demo dihapus setelah). **P4-003** (`POST`/`GET`/`DELETE /concepts/{id}/prerequisites`, `titian-backend-bun`, 2026-09-02): **HTTP surface pertama untuk `concepts` di seluruh sejarah proyek ini** — `setParent`/`findAncestors` (P2-001) juga belum pernah punya route sebelum ini. Cek siklus di `concept_service.addPrerequisite` persis pola `setParent` (self-loop, lalu transitive-walk lewat `concept_repository.findTransitivePrerequisiteIds`, padanan `findAncestors` tapi jalan di tabel `concept_prerequisites`, dipakai HANYA untuk cek siklus, tidak diekspos ke route). Mengisi `concept_prerequisites` persis sesuai desain ADR-0007 (graph terpisah dari `parent_concept_id`) — **tidak butuh ADR baru**, guard `docs/STATE.md` soal itu tetap utuh. 11 test baru. Total sekarang **223/223 test lulus**. **P4-004** (Personal Review / Lesson Packet, `titian-web`, 2026-09-02): halaman baru `/latihan/personal-review`, flatten top-3 `GET /learning-queue` item jadi 1 urutan step linear, dijawab lewat `QuestionCheck` (P3-002) yang sudah ada — tidak ada endpoint backend baru, tidak ada grading machinery baru. `QuestionCheck` dapat 1 prop opsional baru `onResult` (backward-compatible) supaya halaman ini bisa lacak benar/salah per step. **1 bug nyata ditemukan+diperbaiki**: 2 tombol baru pakai pola `render={<Link/>}` yang sudah ada di 4 tempat lain proyek ini, tapi kelewatan pasang `nativeButton={false}` yang seharusnya menyertainya — base-ui logging warning soal semantik native button, ketauan lewat log dev server saat verifikasi, bukan cuma "kelihatan jalan". Diverifikasi end-to-end lewat `Bun.WebView` (lihat catatan tooling di bawah) — sempat "gagal" di run kedua karena `/questions/{id}/check` run pertama beneran nge-trigger `masteryService.recomputeForConcept` (confidence jadi terlalu rendah buat lolos gate `findWeak` lagi) — bukan bug, bukti efek samping asli jalan; reseed lalu ulang → lulus lagi. **Tooling (permintaan eksplisit user, di luar ticket manapun)**: mulai sesi ini verifikasi browser FE pakai `Bun.WebView` (bawaan Bun 1.4 — `navigate`/`click`/`evaluate`/`screenshot`/`cdp`, real Chrome via CDP di mesin ini), bukan lagi `npm install playwright` di scratchpad — auth-token injection ke localStorage sekarang lewat CDP `Page.addScriptToEvaluateOnNewDocument` (butuh 1x `navigate("about:blank")` pemanasan sesi CDP dulu). Tidak ada dependency baru ditambahkan ke `package.json` manapun — murni perubahan skrip verifikasi scratchpad. **P4-005** (integration test suite + exit checkpoint, `titian-backend-bun` + `titian-web`, 2026-09-02): route-coverage audit (skrip Python, pola P2-017/P3-005) atas semua 71 route — **0 gap**, tiap route Phase 4 memang sudah dites langsung sewaktu ticket-nya sendiri dikerjakan. Checkpoint end-to-end baru (`tests/phase4-checkpoint.test.ts`) — skenario persis dari roadmap: 10 soal lintas 3 concept (0/4, 1/3, 3/3 benar) lewat `POST /questions/{id}/check` asli, `GET /learning-queue` dicek balikin urutan concept lemah dulu, concept kuat tidak muncul sama sekali. Total sekarang **224/224 test lulus**. **1 gap checkpoint nyata ditemukan+ditutup saat menutup fase** (bukan diasumsikan beres): checkpoint #1 ("drill-down mastery lewat UI") ternyata belum bisa didemo — P4-001 cuma pernah dibangun backend+test, nol FE consumer. Ditutup sekarang: `MasteryBreakdownDialog` baru di halaman Progres — tiap baris `MasteryBars` bisa diklik, buka dialog render tree drill-down 2 level dengan flag lemah (ikon+warna merah), reuse `GET /concepts/{id}/mastery-breakdown` yang sudah ada, tidak ada endpoint backend baru. Semua 5 poin checkpoint diverifikasi ulang lewat `Bun.WebView`/test asli, bukan diasumsikan dari ticket sebelumnya — detail lengkap di `docs/tickets/phase-4.md`'s "Catatan implementasi" P4-005 dan bagian checkpoint-nya sendiri.

**Phase 5 — sisa terakhir roadmap-Fase-3: ticket breakdown ditulis (2026-09-02), belum ada kode.** User pilih scope paling kecil/aman dari 3 opsi (riset + `AskUserQuestion`) setelah Phase 4 ditutup — 2 opsi lain (Speaking+AI Tutor roadmap-Fase-4, Assessment/Exam Engine roadmap-Fase-5) sengaja ditunda karena butuh keputusan arsitektur besar/ADR baru. `docs/tickets/phase-5.md` — 3 ticket (P5-001 Retrieval Variation §3.4, P5-002 sambungkan `concept_prerequisites` P4-003 yang belum pernah dipakai ke `/learning-queue`, P5-003 test suite+checkpoint). Riset ulang mengonfirmasi §3.3/§3.7 masih belum layak dibangun (database dev cuma 15 baris `learning_events`, 0 pasang user-concept tembus `masteryNMin=5`) — lihat ticket file-nya bagian "Keputusan scope" untuk detail lengkap. Lihat "Next action".

**`titian-web` — 5 layar learner disambungkan ke API asli (2026-08-24, atas permintaan eksplisit user, lihat "Deviasi").** Beranda/Belajar/Latihan/Progres sekarang pakai `GET /curricula/{id}/tree`, `GET /review-queue`, `GET /mastery/{concept_id}` (bukan `lib/mock-data.ts` lagi, sudah dihapus); Profil sinkron identity+role asli dari `GET /users/me`. Diverifikasi lewat sesi browser asli (Playwright, bukan mock fetch) — lihat detail di "Ticket selesai".

**Content Studio v1 selesai (2026-08-24)** — `/studio` di `titian-web`: bikin curriculum, tambah level/unit/lesson, tulis lesson manual (ALM) atau generate via AI asli, lihat QA report, submit-review/publish/reject, semua role-gated (curriculum_developer+ author, reviewer+ publish). 2 endpoint backend kecil ditambah (`GET /curricula`, `raw_source` di `GET /lessons/{id}`) + **1 bug CORS nyata ditemukan & diperbaiki** (`PUT` belum ada di allowlist backend — struktural tidak mungkin ketahuan dari Rust integration test, cuma browser asli yang bisa nemuin). Diverifikasi end-to-end lewat sesi browser asli penuh (bukan cuma smoke test dangkal) — lihat detail di "Ticket selesai". Question Bank UI (soal, generate AI soal) **belum dikerjakan**, itu sesi FE berikutnya kalau user minta lanjut ke situ.

**ALM editor/viewer diganti jadi full WYSIWYG (2026-08-24, atas instruksi eksplisit user)** — textarea polos v1 diganti editor TipTap penuh; admin tidak lagi pernah melihat ALM mentah (persis arahan `ALR_Phase_Detail_Breakdown.md` 2.0). Editor yang sama dipakai buat authoring DAN Viewer read-only (`editable` prop, node view sama). **2 bug data-loss nyata ditemukan lewat Playwright step-by-step, diperbaiki, diverifikasi ulang** — lihat detail di "Ticket selesai".

**Model AI default/text di-swap ke DeepSeek V4 Flash, model vision disiapkan buat OCR (2026-08-24, instruksi eksplisit user)** — lihat detail di "Deviasi". Sempat memicu 1 bug nyata di `POST /ai/generate-questions` (model baru nesting `correct_answer` di dalam `data`), ditemukan lewat smoke test asli, sudah diperbaiki (`ai_content_service.rs::question_type_shape_hint`).

**Content Studio v2 — Question Bank UI selesai (2026-08-24)** — `/studio/question-banks` di `titian-web`: bikin bank, tulis soal manual (mcq/fill_blank/matching, sesuai skema `question_schema.rs` persis), generate soal via AI asli, lihat detail soal penuh (jawaban benar di-highlight), submit-review/publish/reject — pola sama seperti lesson (`service/publish_flow.rs`). 3 endpoint backend baru ditambah (`GET`/`POST /question-banks`, `GET /questions/{id}`) + 1 fix kecil (`data` ditambah ke `GET /question-banks/{id}/questions`'s list summary, buat preview baris). Diverifikasi end-to-end lewat sesi browser asli penuh — lihat detail di "Ticket selesai". Ini menutup scope Question Bank UI yang sengaja ditunda dari Content Studio v1.

**Nav "Content Studio" ditambah ke shell learner (2026-08-28, ditemukan user lewat screenshot)** — akun admin yang login di app learner (`/beranda` dst) sebelumnya tidak punya jalan sama sekali ke `/studio` kecuali ketik URL manual. Ditambah 1 item role-gated (`canAccessStudio`) di dropdown akun `top-bar.tsx`. **1 bug nyata ditemukan & diperbaiki saat verifikasi**: pola `DropdownMenuItem render={<Link/>}` yang dipakai butuh children ada LANGSUNG di `<Link>` (bukan di `DropdownMenuItem`) — pola yang salah bikin klik tidak nge-navigate sama sekali (dropdown cuma nutup, tetap di halaman yang sama), ketahuan lewat Playwright yang assert URL akhir, bukan cuma screenshot visual.

**ALM editor UX di-redesign — full slash-command + toolbar rombak (2026-08-28, instruksi eksplisit user lewat screenshot dark-mode)** — user tunjuk screenshot nyata: editor gelap tidak kebaca (`bg-background` lebih gelap dari dialog pembungkusnya), toolbar cuma baris 9 ikon tanpa label, tidak ada slash command sama sekali padahal diminta "ux nya mirip notion juga". Ditanya dulu (AskUserQuestion) apakah slash command mau termasuk — user pilih ya. Dibangun: `commands.ts` (satu sumber kebenaran daftar insert, dipakai toolbar DAN slash command sekaligus, tidak bisa drift), `slash-command.tsx` (extension `@tiptap/suggestion`, ketik "/" munculin menu grouped+filterable+keyboard-nav), toolbar diganti jadi H1/H2/H3/¶ + 1 menu "+ Blok" berlabel (bukan baris ikon), surface editor pindah ke `bg-card`+`shadow-sm` (kebaca sebagai halaman di dialog MAUPUN full-page route), input di dalam card block jadi "ghost sampai difokus" (kebaca sebagai teks, bukan form). **Serialisasi ALM 100% tidak berubah** — diverifikasi lewat `content_blocks.raw_source` asli setelah authoring pass penuh (slash command + toolbar menu), hasilnya identik persis format sebelumnya. **1 bug nyata ditemukan lewat screenshot verifikasi** (bukan cuma "kelihatannya jalan"): slash-menu di-mount ke `document.body` di luar portal Dialog, dan `@tiptap/suggestion`'s `mount()` API baru (versi 3.30.5, ternyata sudah pakai Floating UI internal, bukan lagi pola tippy.js klasik dari tutorial lama — `tippy.js` sempat ditambah lalu dicopot lagi begitu ini ketahuan) ninggalin wrapper-nya di `z-index:auto`, jadi ke-tutup diam-diam oleh Dialog yang `isolate z-50` — menu-nya BENERAN ada di DOM dan berfungsi (keyboard nav jalan), cuma tidak kelihatan. Ketahuan lewat screenshot literal yang nunjukin "/head" diketik tapi menu-nya kosong, bukan dari behavior yang salah. Fix: set `z-index` langsung di elemen wrapper yang di-mount, bukan di komponen React sendiri (yang percuma karena bukan itu elemen yang positioned).

**Drive-style file/media management selesai (2026-08-29)** — user minta "sekalian file/media management system", ditanya scope-nya (AskUserQuestion), user pilih **penuh**: folder, sharing per-user/role (Viewer/Editor), ownership+activity trail — bukan cuma tombol upload. `/studio/media` di `titian-web` + endpoint baru di `titian-backend` (folders, resource_shares, resource_activity — 3 migrasi baru, 9 test integrasi baru, 183 test total). Menutup gap desain nyata yang ditemukan pas baca kode: `block_schema.rs` sudah lama mewajibkan `data.asset` berupa `asset://<id>` (bukan URL mentah) tapi belum ada apa pun yang benar-benar meresolusi `asset://<id>` jadi URL yang bisa diputar — sekarang ada (`GET /assets/{id}`, re-sign fresh tiap baca). ALM editor sekarang punya media picker asli (bukan textarea nunggu `asset://` diketik manual) — **ini permintaan awal user, sekarang benar-benar bisa dites end-to-end**. Detail lengkap, termasuk 3 bug nyata yang ditemukan (CORS DELETE, `request()` 204 handling, konteks-menu "Hapus" yang ambigu di trash), ada di "Ticket selesai".

## PENTING — baca sebelum mulai kerja
1. **Ada proyek lain di `/home/john/Dev/sanja-workspace/lms/`** (sejajar dengan `alr/`, di luar folder ini) berisi `alr-backend`/`alr-web`/`alr-mobile` versi lain yang jauh lebih maju (Phase 0-2 sudah banyak selesai di sana), dengan dokumentasi sendiri (`ALR_TECHNICAL_BLUEPRINT.md`, bukan `agent/docs/`). **User sudah eksplisit konfirmasi (2026-08-23): `lms/` tidak dipakai lagi, abaikan.** Proyek yang aktif adalah `alr/` ini. Jangan tanya ulang soal ini kecuali user membawanya lagi.
2. **Struktur bukan monorepo** — lihat bagian di bawah.

## Struktur repo (PENTING — beda dari `ALR_Build_Roadmap.md`)
`ALR_Build_Roadmap.md` menyebut monorepo sebagai bagian stack "terkunci". Itu sudah **tidak berlaku** —
atas instruksi eksplisit user (2026-08-23), proyek pindah ke **satu repo git independen per aplikasi**
supaya gampang dikerjakan tim terpisah:
- `alr/titian-web/` — Next.js 16, repo git sendiri. **Sudah di-scaffold, sudah di-commit.**
- `alr/titian-backend/` — Rust/Axum/sqlx, repo git sendiri. **Backend ASLI/lama** — 34 tabel, ~30 endpoint, 183 test, semua fase sampai P2-014 + Drive feature. **Sedang di-migrasi ke `titian-backend-bun` (ADR-0009, 2026-08-30) — dibiarkan utuh di disk, TIDAK dihapus**, jadi reference implementation sampai portingan Bun terverifikasi setara. Lihat "Migrasi Backend: Rust → Bun+ElysiaJS" di bawah untuk progres.
- `alr/titian-backend-bun/` — Bun+ElysiaJS+Drizzle ORM, repo git sendiri. **Backend BARU, sedang dibangun** — pengganti `titian-backend` per keputusan eksplisit user. Lihat ADR-0009 dan bagian progres di bawah.
- `alr/titian-mobile/` — Expo/React Native, repo git sendiri. **Belum dibuat — ditunda atas instruksi user ("mobile nanti aja").**
- `alr/agent/` — dokumentasi bersama (roadmap, ADR, ticket, file ini) — satu-satunya isi repo `alr/` itu sendiri.

Konsekuensi: tidak ada lagi `packages/shared` package. Kontrak tipe (zod schema dll) di-duplikasi
manual per-repo saat dibutuhkan — lihat `titian-web/src/lib/schemas.ts`. Setiap repo build & deploy
independen; yang menyatukan mereka cuma kontrak yang didokumentasikan di `docs/api-contract.md` dan
`docs/domain-model.md`, bukan tooling. Detail lengkap di catatan P0-009 (`docs/tickets/phase-0.md`).

## Ticket sedang dikerjakan
Tidak ada yang in-progress. **Phase 2 selesai penuh** (P2-001 s/d P2-017, 17/17 ticket). **Phase 3 selesai penuh** (P3-001 s/d P3-005, 5/5 ticket, semua 4 checkpoint tercentang). **Phase 4 selesai penuh** (P4-001 s/d P4-005, 5/5 ticket, semua 5 checkpoint tercentang). **Phase 6 selesai penuh** (P6-001 s/d P6-005, 5/5 ticket, semua 5 checkpoint tercentang — lihat `docs/tickets/phase-6.md`). Content Studio (FE) — curriculum+lesson (v1) DAN Question Bank UI (v2) — **keduanya sudah selesai**. **Phase 5 ticket breakdown sudah ditulis** (`docs/tickets/phase-5.md`, 3 ticket) dan **belum ada satupun yang dikerjakan** — P5-001 berikutnya begitu user bilang lanjut. Lihat "Next action".

## Ticket selesai
- **P3-005: Integration test suite + exit checkpoint (2026-08-31, `titian-backend-bun`) — Phase 3 CLOSED, semua 5 ticket done.** Cross-check `grep`-based (skrip Python, sama pola P2-017) atas semua 66 route di `app.ts` vs `tests/*.test.ts` — **0 gap ditemukan** kali ini (beda dari P2-017 yang nemu 20+ gap nyata sisa migrasi ADR-0009) — masuk akal karena tiap route baru P3-001 s/d P3-004 memang sudah dites langsung sewaktu ticket-nya sendiri dikerjakan, bukan diporting belakangan. Ketiga skenario test end-to-end yang diminta ticket **sudah punya coverage nyata dari ticket sebelumnya, tidak perlu test baru** (dicek dulu, bukan diasumsikan, sesuai catatan ticket-nya sendiri): (a) `tests/question-check.test.ts` — query `masteries` langsung setelah `/check`. (b) `tests/assessment.test.ts` (sejak Phase 1) — assersi skor presisi, bukan cuma status. (c) `tests/ai-writing-evaluation.test.ts` — query `evaluations`+`feedback` langsung, `rubric_id` dicocokkan ke `WRITING_RUBRIC_ID`. Total **201/201 test lulus**. **Semua 4 poin checkpoint keluar Phase 3 dicentang** — tiap satu sudah didemo nyata (bukan asumsi) lewat sesi Playwright P3-002/003/004 masing-masing, ditautkan langsung sebagai bukti di `docs/tickets/phase-3.md`. Detail lengkap di ticket file itu (P3-005's "Catatan implementasi" + bagian checkpoint).
- **P3-004: Writing submission + rubric-based AI evaluation (2026-08-31, `titian-web` + `titian-backend-bun`)** — ticket terakhir Phase 3 yang bikin fitur (P3-005 tinggal test suite + checkpoint). Kode PERTAMA yang menyentuh `evaluations`/`feedback`/`rubrics` sejak dibuat Phase 0. `POST /lessons/{id}/attempts` (attempt berbasis lesson, mirror pola assessment) + `POST /attempts/{id}/submit` di-extend bercabang berdasar `lesson_id`/`assessment_id` si attempt (bukan endpoint submit terpisah — percabangannya di **handler**, bukan service, supaya tidak circular import antara `assessment_service.ts` dan `ai_writing_evaluation_service.ts` baru). Rubric: 4 kriteria gaya IELTS (`task_achievement`/`coherence_cohesion`/`lexical_resource`/`grammar_accuracy`, 0-100), 1 row di-seed idempotent lewat kode (`rubric_repository.ensure`, id tetap). **Ditemukan**: `attempts.status` sudah lama punya nilai ke-3 `evaluated` di CHECK constraint yang tidak pernah dipakai kode manapun — persis pas buat model "submitted (teks masuk) → evaluated (sudah dinilai)" yang dibutuhkan di sini. Kalau AI evaluation gagal (provider error/output tidak valid), attempt tetap `submitted` (bukan error ke caller) — teks learner tetap aman, `evaluator_type: "human"` tetap opsi valid di skema (gerbang manusia diterapkan ke *timing*, bukan cuma otoritas). **1 keputusan desain prompt**: AI diminta ngutip *exact quote* dari essay, bukan hitung character offset sendiri (LLM lemah di aritmatika offset) — service cari posisinya sendiri lewat `indexOf` setelahnya. **Diverifikasi lewat provider AI asli** (bukan `FakeAIProvider`) via Playwright: submit essay yang sengaja salah tense ke lesson nyata "Writing Uppercase and Lowercase" (P2-016) — dapat 4 skor kriteria yang masuk akal (Grammar Accuracy 50, paling rendah, sesuai banyaknya error tense) + 5 koreksi grammar yang **semuanya benar** (go→went, buy→bought, eat→ate, teach→taught, vegetable→vegetables), dikonfirmasi cocok persis dengan row `attempts`/`evaluations`/`feedback` di database. Nol console error. 10 test baru (`tests/ai-writing-evaluation.test.ts`) — total sekarang **201/201 test lulus**. Detail lengkap di `docs/tickets/phase-3.md` (P3-004's "Catatan implementasi") dan `agent/docs/api-contract.md`.
- **`titian-web`: migrasi pnpm/Node → full Bun (2026-08-31)** — permintaan eksplisit user, di luar ticket-Phase-3, menutup follow-up yang sudah diantisipasi ADR-0009 sendiri ("Next.js masih menyasar Node/Bun sebagai runtime web framework secara terpisah"). `pnpm-lock.yaml`/`pnpm-workspace.yaml` dihapus, `node_modules` (struktur symlink `.pnpm` yang tidak bisa dipakai ulang installer Bun) di-wipe, `bun install` dari nol → `bun.lock`. `package.json`'s `scripts` di-prefix `bun --bun` (`dev`/`build`/`start`) supaya binary `next` (shebang Node) benar-benar dieksekusi runtime Bun, bukan cuma Bun dipakai buat install lalu diam-diam jatuh balik ke Node — `lint` (eslint) tidak perlu diubah, sudah jalan native di Bun. **1 bug nyata ketemu+diperbaiki**: `@tiptap/core` sebelumnya di-pin exact `"3.30.2"` sementara `@tiptap/starter-kit` dkk semua pakai range `^3.30.2` — di bawah pnpm ini entah bagaimana ke-dedupe/tersembunyi, tapi di bawah Bun menghasilkan **2 copy `@tiptap/core` yang beda** (satu di top-level, satu ternested di `starter-kit`), bikin `tsc --noEmit` gagal dengan error struktural yang panjang (`Node<any,any>` dari 2 lokasi modul dianggap 2 tipe tidak kompatibel). Diperbaiki dengan melebarkan pin itu ke `^3.30.2` (menyamakan gaya sibling-nya) — Bun lalu menyatukan semua paket `@tiptap/*` ke 1 versi (`3.30.5`), bukan workaround, ini benar-benar bug versioning yang sudah ada tapi kebetulan tidak pernah kepicu di bawah pnpm. `pnpm-workspace.yaml`'s `allowBuilds: {sharp: false, unrs-resolver: false}` (blokir postinstall native 2 paket itu) **tidak butuh port manual** — Bun sudah menolak lifecycle script paket manapun secara default kecuali didaftarkan di `trustedDependencies`; dikonfirmasi lewat `bun pm untrusted` yang cuma nunjukkan `unrs-resolver` diblokir (persis maksud config pnpm lama), `sharp` bahkan tidak muncul sebagai dependency asli. **Diverifikasi penuh, bukan cuma "harusnya jalan"**: `bunx tsc --noEmit` bersih, `bun run lint` bersih, `bun run dev` (server asli, beberapa route di-curl, 200 semua), **`bun run build`** (langkah paling berisiko — apakah `@ducanh2912/next-pwa`'s webpack plugin generate service worker dengan benar waktu build-nya sendiri dijalankan runtime Bun bukan Node) **sukses penuh**, `public/sw.js`+`workbox-*.js` ke-generate benar, `bun run start` (production server) jalan dan `/sw.js` benar-benar bisa diakses (200), lalu 1 pass Playwright browser asli (3 halaman, nol console error selain isu Google OAuth origin whitelist yang sudah lama tercatat, tidak terkait migrasi ini). Tidak ada CI/Docker/deploy config di manapun di repo `alr` yang perlu diikutkan — migrasi ini murni lingkup dev-tooling lokal.
- **P3-003: Assessment-taking UI (2026-08-31, `titian-web` + `titian-backend-bun`)** — mem-wire `POST /assessments/{id}/attempts`/`POST /attempts/{id}/submit` (dibangun Phase 1, **belum pernah dipanggil dari kode FE mana pun**) jadi layar nyata: mulai attempt → jawab tiap soal lewat `QuestionRenderer` (P3-002) → submit → skor. Block type baru `assessment_embed` (backend, `block_schema.ts` — pola identik `question_embed`, termasuk cek referensial `assessment_id` beneran ada lewat `assessment_repository.findExistingIds` baru) supaya lesson bisa link terstruktur ke `assessments` row-nya — menutup gap yang persis ditemukan P2-016 (lesson "Alphabet Quiz" dulu cuma *menyebut* assessment-nya di teks prosa). **Kendala nyata**: lesson itu statusnya `published`, dan ADR-0008 sengaja tidak punya jalur unpublish/edit-langsung — diatasi dengan 1x `UPDATE lessons SET status='draft'` langsung lewat `psql` (konten demo P2-016 milik sesi ini sendiri, bukan konten produksi), lalu edit+submit-review+publish semuanya lewat API asli seperti biasa. **Diverifikasi end-to-end lewat Playwright ke server asli**: buka "Alphabet Quiz", "Mulai kuis", jawab 3 soal (sengaja semua salah untuk membuktikan grading beneran jalan, bukan hardcoded), submit, skor **0** muncul — dikonfirmasi cocok persis dengan row `attempts` baru di database. Nol console error. Detail lengkap di `docs/tickets/phase-3.md` (P3-003's "Catatan implementasi").
- **P3-002: QuestionRenderer registry + learner-facing lesson viewer (2026-08-31, `titian-web` + `titian-backend-bun`)** — sebelum ini, sisi learner **tidak punya jalan sama sekali** untuk membaca isi 1 lesson (`LessonRow` di tree Belajar `<div>` statis, tidak bisa diklik) dan `question_embed` di mana pun (termasuk Content Studio) cuma render chip UUID inert. Sekarang: registry `QuestionRenderer` (`src/components/exercise/question-renderers.tsx`, pola sama `DIRECTIVE_CONFIGS` ALM editor) dengan input interaktif buat `mcq`/`fill_blank`/`matching`; halaman baru `/belajar/[lessonId]` (display-only, bukan editor TipTap) yang render `content_blocks` asli; `LessonRow` sekarang `<Link>` ke halaman itu untuk lesson published. **1 endpoint backend tambahan ditemukan dibutuhkan saat implementasi**: `GET /questions/{id}/stem` — `QuestionRenderer` butuh `type`+`data` buat merender, tapi `GET /questions/{id}` eksplisit surface authoring (selalu include `correct_answer`), tidak boleh dipakai learner-facing. Ditambah endpoint learner-safe baru (pola sama `AttemptQuestion` P1-005), sekalian refactor 3 fungsi baca `question_service.ts` ke 1 helper `findViewableQuestion` bersama. **Diverifikasi end-to-end lewat Playwright ke server asli** (reuse modul "Alphabet" P2-016): buka lesson "Alphabet Recap" (punya `question_embed` asli) lewat URL langsung DAN lewat klik `LessonRow` di tree (dikonfirmasi via DOM query — semua 6 href lesson ada persis), jawab soal MCQ dengan benar, dapat feedback "Benar!" — nol console error. Detail lengkap + 1 catatan UX (matching di-render sebagai dropdown per item, bukan drag-and-drop) di `docs/tickets/phase-3.md` (P3-002's "Catatan implementasi").
- **P3-001: Exercise Engine core — inline question check + grading (2026-08-31, `titian-backend-bun`)** — `POST /questions/{id}/check`, ticket pertama Phase 3. Menjawab 1 soal inline (buat `question_embed` di dalam lesson) tanpa row `attempts` formal, tapi efek sampingnya persis sama dengan attempt formal: `question_service.checkAnswer` menulis 1 `learning_event` lalu memanggil `masteryService.recomputeForConcept`+`frssService.recordReview` untuk tiap concept yang ditaut — bukan cuma catat event lalu berhenti (checkpoint keluar Phase 3 poin 4 eksplisit minta `masteries` ikut ter-update, jadi scope ini dimasukkan ke P3-001 langsung, bukan ditunda). **1 bug nyata ditemukan+diperbaiki**: `grading.ts` (`isAutoGradable`/`isCorrect`) ternyata tidak pernah mendukung tipe soal `matching` walau `question_schema.ts` sudah mendaftarkannya sejak P2-004 — soal matching manapun yang lewat `submitAttempt` formal akan selalu ke-grade salah, tidak pernah ketahuan karena tidak ada test/data yang pernah mengetesnya lewat alur attempt. Ketauan karena `/check` (dipakai `QuestionRenderer` P3-002 nanti) eksplisit butuh ketiga tipe soal terdaftar jalan benar. Fix-nya (perbandingan pasangan set, order-independent) otomatis juga membetulkan `submitAttempt` yang sudah ada, bukan cuma endpoint baru. 9 test baru (`tests/question-check.test.ts` + 2 kasus baru di `tests/grading.test.ts`) — total sekarang **186/186 test lulus**. Dites juga lewat `curl` server asli: `POST /questions/{id}/check` tanpa token → 401. Detail lengkap di `docs/tickets/phase-3.md` (P3-001's "Catatan implementasi") dan `agent/docs/api-contract.md`.
- **P2-016 + P2-017: Alphabet module validation + Phase 2 exit checkpoint (2026-08-31, `titian-backend-bun`) — Phase 2 CLOSED, semua 17 ticket done.** User bilang "start" setelah disodori rekomendasi lanjut ke P2-016/017 (satu-satunya sisa Phase 2). Dikerjakan langsung lewat HTTP asli ke server yang jalan sungguhan (bukan cuma test) — pola verifikasi yang sama dipakai sepanjang migrasi ADR-0009.
  - **P2-016**: curriculum "Pre-Basic English" (subject baru `english-p2016`) → level "Pre-Basic" → unit "Alphabet" berisi 6 lesson penuh: `learn` ditulis manual, **`practice` digenerate lewat `POST /ai/generate-lesson` ke DeepSeek/OpenRouter asli** (hasilnya bagus — 25+ block ALM: heading/text/example/common_trap/indonesian_learner_alert/think_in_english/flashcard, semua tentang "spelling nama pakai alphabet Inggris"), `speaking`/`writing`/`assessment` (intro) ditulis manual, `review` menyematkan `question_embed` ke salah satu soal (membuktikan validasi referensial P2-003 jalan). 1 question bank baru, 3 soal mcq **digenerate lewat `POST /ai/generate-questions` asli**. Semua 6 lesson + 3 soal lewat `submit-review`→`publish` asli (dev sebagai author, reviewer terpisah sebagai publisher — role terpisah bukan asumsi). **1 gap infrastruktur nyata ditemukan**: tidak ada authoring API untuk `assessments` sama sekali di seluruh sejarah proyek ini (Phase 1 cuma pernah mengonsumsi row hasil seed) — ditambah `POST /assessments` minimal (repo/service/handler/route baru, reuse permission tier `question_bank:create`, 3 test baru) supaya assessment bisa dibuat lewat API asli. Attempt asli (siswa) → submit → **score 100**, learning_events tercatat. **Concept hierarchy (P2-001) dipakai nyata**: 1 concept `vocabulary` "The Alphabet" ditaut ke 1 soal tambahan lewat `concept_ids` (field yang sudah ada di `POST /question-banks/{id}/questions`, cuma belum pernah dipakai nyata sebelum ini) — attempt siswa menghasilkan `masteries` row asli (`confidence: 0.2`, `insufficient_data` karena baru 1 attempt dari `N_min=5` — jujur, bukan dipalsukan biar "keliatan lulus").
  - **1 percobaan panggilan OCR asli ke vision model** (bukan `FakeAIProvider`): upload gambar asli ke R2, `POST /ai/ocr-to-question` ke `deepseek/deepseek-v4-flash-vision-exp` lewat OpenRouter — provider menolak `HTTP 404 "No endpoints available matching your guardrail restrictions and data policy"` (setting privasi akun OpenRouter, bukan bug kode). **Jalur gagalnya sendiri terverifikasi benar**: `422 ai_output_validation_failed`, 1 row `ai_tasks` status `failed` tercatat, nol `questions` nyasar — persis kontraknya. Jalur sukses lewat provider vision asli masih belum diverifikasi live (model lain dibutuhkan) — dicatat sebagai item terbuka di `docs/tickets/phase-2.md`, bukan diklaim selesai; jalur sukses tetap terverifikasi penuh lewat `ai-ocr.test.ts` (`FakeAIProvider`).
  - **P2-017**: cross-check `grep`-based (pola P1-013) nyata dijalankan atas semua route `src/app.ts` vs `tests/*.test.ts` — menemukan **20+ route nyata tanpa test HTTP-level sama sekali**, sisa keluput migrasi ADR-0009: seluruh slice Question Bank UI (`GET`/`POST /question-banks`, `GET /questions/{id}`), `POST /questions/{id}/publish`+`reject`, `POST /lessons/{id}/publish`+`reject` (state-machine murninya sudah dites di `publish-flow.test.ts`, tapi rute HTTP-nya sendiri belum pernah dipanggil test manapun — DAN ternyata jadi test end-to-end AC#2 ticket ini), serta rute Drive sisi asset yang cuma pernah punya test sisi folder (`rename`/`shares`/`activity`/trash-delete/permanent-delete) plus `/assets/confirm`, `/assets/presigned-upload`, `/folders/{id}/restore`, `/folders/{id}/permanent`, `/drive/shared-with-me`, `/drive/share-candidates`. Semua ditutup: 1 file test baru (`tests/question-bank.test.ts`, 7 test) + penambahan ke `tests/drive.test.ts` (10 test baru) + `tests/content-authoring.test.ts` (3 test baru).
  - **Efek samping nyata dari P2-016 ditemukan+diperbaiki lewat proses P2-017**: 4 test lama asumsi tabel kosong (`count()` tanpa filter, `.toBe(0)`) jadi gagal begitu `titian_bun` (database dev bersama, BUKAN diisolasi per-test seperti `withTx`-nya sendiri — `withTx` cuma rollback perubahan di dalam 1 test, bukan data yang sudah committed sebelumnya) permanen berisi konten P2-016. Diperbaiki jadi pola before/after delta, bukan angka absolut — kelas bug yang sama persis dengan catatan "test-database pollution" di bagian migrasi Fase 7 di atas. **Konten P2-016 sengaja TIDAK dihapus setelah verifikasi** (beda dari data seed Fase 7 yang memang throwaway) — modul "Alphabet" ini adalah deliverable nyata ticket ini, dimaksud tetap ada di `titian_bun`.
  - Total test: **177/177 lulus** (dari 154 sebelum sesi ini). Semua 5 poin checkpoint keluar Phase 2 tercentang di `docs/tickets/phase-2.md`.
- **P2-015: OCR-to-Question Pipeline v1 (2026-08-31, `titian-backend-bun`)** — `POST /ai/ocr-to-question`, dibangun langsung di Bun (tidak pernah dibangun sisi Rust, statusnya `todo` waktu migrasi selesai duluan). `ai_provider.ts`'s `GenerationRequest` dapat field opsional baru `imageUrl` (bukan method baru — additive, tiap call site lama yang tidak set field ini tidak berubah), `DeepSeekProvider` bangun multimodal `content` array (OpenAI-compatible `image_url`) kalau field itu di-set. `ai_ocr_service.ts` (modul terpisah dari `ai_content_service.ts`, sesuai catatan ticket "paling kompleks, sengaja ditaruh sendirian"): resolve `asset_id` lewat `drivePermissions.resolveAccess` asli (bukan bypass) → re-sign URL lewat `storage.signedUrl` → prompt vision pakai pola "contoh JSON terisi penuh" yang sudah kebukti kerja di P2-013 → tiap item hasil ekstraksi (yakin diklasifikasi atau tidak) ditulis langsung lewat `question_repository.insert` (bypass gate throw-on-invalid `questionService.createQuestion`, karena tipe tak-dikenal itu hasil yang DIHARAPKAN buat ditangkap di sini, bukan error caller) — **tidak pernah** ada yang di-skip atau dipaksa masuk kategori. Scope v1 **dibatasi ke gambar saja** (PDF ditunda, butuh page-rendering terpisah) dan **setiap** question hasil OCR — bukan cuma yang tak yakin — dapat entri `ocr_verification` di `qa_report.issues` (plus `ocr_uncertain_type` buat yang gagal validasi/tak dikenal, dan `ocr_raw_text` buat pembanding manual) — dua keputusan scope ini dikonfirmasi eksplisit ke user lewat `AskUserQuestion` sebelum implementasi, keduanya pilih opsi yang direkomendasikan. 7 test baru (`tests/ai-ocr.test.ts`) — total sekarang **154/154 test lulus**. Dites juga lewat `curl` server asli: `POST /ai/ocr-to-question` tanpa token → 401, sesuai kontrak semua route lain. Detail di `agent/docs/api-contract.md` dan `docs/tickets/phase-2.md`.
- P0-001 s/d P0-006: ADR-0001 s/d ADR-0006 [done]
- P0-007: Migration SQL — 10 grup migration (`titian-backend/migrations/`, grup ke-10 ditambah di sesi P1-001), jalan + revert teruji, 34 tabel terverifikasi [done]
- P0-008: API contract cross-check — tidak ada mismatch field antara `api-contract.md` dan schema [done]
- P0-009: `titian-web` + `titian-backend` selesai di-scaffold, jalan, di-commit. `titian-mobile` masih belum (ditunda) — lihat status P0-009 di `docs/tickets/phase-0.md` untuk detail, ticket ini tetap tercatat in-progress di sana.
- P0-011: file protokol & STATE.md [done]
- **P1-001: Auth Google OAuth + JWT** — `POST /auth/google/callback` dan `POST /auth/refresh` jalan penuh di `titian-backend`, 11 integration test lulus. Detail lengkap di `docs/tickets/phase-1.md`.
- **`titian-web` disambungkan ke auth asli** — tombol login mock diganti Google Identity Services beneran (`src/components/auth/google-sign-in-button.tsx`), session di-persist via `auth-store.ts` (cuma refresh token yang disimpan, access token di-re-derive tiap boot lewat `/auth/refresh` — lihat `session-bootstrap.tsx`), route `(app)/*` di-gate lewat `require-auth.tsx` (redirect ke `/login` kalau belum login). CORS ditambah di backend (`FRONTEND_ORIGIN`) supaya browser boleh manggil API cross-origin.
- **`titian-web`: 5 layar learner disambungkan ke API Phase 1/2 asli (2026-08-24)** — `lib/mock-data.ts` dihapus total. `lib/api-client.ts` dapat `authedRequest` (attach `Authorization: Bearer`, satu kali refresh-and-retry kalau 401 — token akses cuma 15 menit, tanpa ini halaman yang dibuka lama bakal 401 diam-diam) + `contentApi`/`learningApi`/`userApi`. **2 keterbatasan backend nyata dihadapi, diselesaikan dengan desain jujur bukan fake data** (detail lengkap di komentar kode `lib/curriculum-view.ts` dan `app/(app)/progres/page.tsx`):
  - Tidak ada progress tracking per-learner (locked/available/completed) atau persentase progress per level — itu baru ada di Phase 3. Belajar sekarang cuma menampilkan status publish konten (bukan pura-pura ada progress), level "progress bar" di-relabel jujur jadi "% materi tersedia" (content coverage, bukan completion).
  - Tidak ada endpoint agregat mastery-per-skill — Progres di-desain ulang jadi mastery per-concept dari `review-queue` (bukti data yang sungguhan ada), bukan breakdown 6-skill yang dulu difake.
  - `GET /curricula` (list) **belum ada saat sesi ini ditulis — sekarang sudah ada**, ditambah di sesi Content Studio (lihat bullet di bawah). `lib/curriculum.ts`'s `ACTIVE_CURRICULUM_ID` tetap dipertahankan untuk sisi learner (belum ada konsep "kurikulum yang diikuti user", jadi picker asli belum relevan di situ) — Content Studio sendiri sekarang pakai `GET /curricula` sungguhan buat listing.
  - Endpoint yang di-gate role (`/review-queue`, `/mastery/{id}`) 403 kalau akun tidak punya `user_organization_roles` sama sekali (tidak ada alur self-serve join org) — di-deteksi eksplisit (`lib/api-client.ts::isForbidden`) dan ditampilkan sebagai empty-state jujur ("belum terdaftar sebagai pelajar"), bukan generic error. **Ditemukan bug nyata saat verifikasi**: React Query default retry 403 (percuma, tidak akan pernah sukses) bikin empty-state jujur ini telat muncul beberapa detik — diperbaiki di `query-provider.tsx` (jangan retry error 4xx).
  - `components/auth/account-sync.tsx` (dulu nama file/fungsi `role-sync`, di-rename jadi lebih akurat) sinkron identity+role dari `GET /users/me` di **setiap load**, bukan cuma sekali pas Google login — profil sekarang selalu akurat walau sesi dipulihkan murni dari refresh token tersimpan.
  - **Diverifikasi end-to-end lewat sesi browser asli** (Playwright headless, bukan mock fetch): backend asli dijalankan, refresh token asli di-mint langsung ke DB (bypass popup Google OAuth yang tidak bisa diotomasi headless — lihat "Blocker aktif"), 5 layar di-screenshot di viewport mobile+desktop, `console --errors` bersih di semua halaman, plus jalur 403-no-role diverifikasi terpisah (role dicabut sementara, restore setelah).
- **`titian-web`: Content Studio v1 — curriculum/lesson authoring (2026-08-24)** — `/studio` (role-gated: curriculum_developer/reviewer/academic_director/org_owner/platform_admin, gate di FE cuma UX, backend tetap yang enforce beneran). 3 layar: `/studio` (list+create curriculum), `/studio/curricula/{id}` (tree penuh + tambah level/unit/lesson), `/studio/lessons/{id}` (editor ALM + save/submit-review/publish/reject + panel QA report, dari P2-014). "+ Lesson" punya 2 tab: tulis manual atau generate lewat AI asli (`POST /ai/generate-lesson`, P2-013). 2 primitive `ui/` baru (`textarea.tsx`, `select.tsx`, pola wrapper Base UI yang sama seperti `dialog.tsx`).
  - **2 backend addition kecil** (bukan ticket P2-XXX baru, murni kebutuhan FE): `GET /curricula` (list — sebelumnya cuma bisa create+get-by-id/tree) dan `raw_source` per block di `GET /lessons/{id}` (perlu buat editor bisa nge-seed textarea dari konten yang sudah ada — tanpa ini tidak ada cara rekonstruksi ALM asli dari block JSON hasil parse). Lihat commit `titian-backend` sesi ini untuk detail + test.
  - **1 bug CORS nyata ditemukan lewat verifikasi browser asli, bukan lewat test suite**: `PUT /lessons/{id}` (dipakai tombol Simpan editor) gagal CORS preflight karena `Method::PUT` tidak ada di `main.rs`'s `CorsLayer::allow_methods` — cuma `GET`/`POST` yang pernah dipakai FE sebelum ini. **Struktural tidak mungkin ketahuan dari Rust integration test manapun**: test suite manggil `routes::build(state).oneshot(...)` langsung, `CorsLayer` cuma dipasang di `main.rs`'s router produksi, jadi tidak pernah lewat lapisan itu di test. Diperbaiki (`Method::PUT` ditambah ke allowlist), diverifikasi lagi lewat browser asli sampai jalan.
  - **Diverifikasi end-to-end penuh lewat sesi browser asli**: sebagai curriculum_developer — bikin curriculum → tambah level → tambah unit → tulis lesson manual → edit+simpan → submit-review → generate lesson kedua lewat AI asli (DeepSeek/OpenRouter, bukan mock) → submit itu juga; lalu role di-swap ke reviewer — publish lesson pertama (jadi read-only, textarea disabled, badge "Published"), tolak lesson kedua (balik ke draft). `console --errors` bersih di semua fase. Role akun test dikembalikan ke `student` setelah selesai, semua data test dihapus.
- **`titian-web`: ALM editor diganti full WYSIWYG, textarea polos dibuang (2026-08-24)** — user eksplisit menilai textarea v1 "sangant kurang" dan minta editor+viewer "full feature", mengarahkan baca ulang `ALR_Phase_Detail_Breakdown.md` 2.0 yang eksplisit bilang authoring pipeline itu "Google Docs-like Editor (WYSIWYG) → ALM → Parser" dan "Admin sendiri tidak perlu melihat markdown mentahnya". Dibangun `src/components/studio/alm-editor/` (TipTap v3, `@tiptap/react`+`pm`+`starter-kit`+`extension-placeholder`, pakai **pnpm** bukan npm — lihat "Deviasi"): 1 tabel registry (`config.ts::DIRECTIVE_CONFIGS`) drive node extension + node view + toolbar button + serializer buat tiap tipe block ALM (example/audio/video/image/flashcard/indonesian_learner_alert/common_trap/think_in_english/question_embed) — nambah tipe block baru = 1 baris tabel, bukan file baru, pola sama kayak `BlockTypeRegistry` di backend. Tidak butuh parser ALM sisi client — `GET /lessons/{id}` sudah balikin block terstruktur, jadi arah load cuma `blocks → TipTap doc` (structural mapping), cuma arah `doc → ALM text` (`doc.ts::docToAlm`) yang butuh serializer, buat endpoint `PUT`/`POST` yang minta ALM text penuh. Editor yang sama dipakai authoring (`editable=true`) dan Viewer read-only (`editable=false`) — node view sama, jadi apa yang dilihat reviewer persis apa yang ditulis author, bukan render path terpisah yang bisa drift.
  - **2 bug nyata ditemukan lewat verifikasi Playwright screenshot-per-langkah (bukan cuma checkpoint tinggi), diperbaiki, diverifikasi ulang sampai bersih**: (1) tombol toolbar insert block pakai selection saat ini (`insertContent`), yang collapse jadi `NodeSelection` sehabis isi field block sebelumnya (`updateAttributes`) — insert berikutnya **mengganti** node yang lagi terpilih, bukan nambah setelahnya, jadi cuma block terakhir yang ke-insert yang selamat lewat 1 save (diverifikasi lewat DB: 4 dari 7 block hilang). Fix: selalu insert di posisi akhir dokumen (`insertContentAt(doc.content.size, ...)`), bukan bergantung selection manapun. (2) tombol toolbar icon-only tidak punya accessible name (Tooltip doang, bukan `aria-label`) — genuine a11y gap, bukan cuma masalah test tooling, ditemukan pas nulis Playwright locator. Ketiga: heading text sempat tidak ke-style (`prose` class dipakai tapi `@tailwindcss/typography` plugin tidak pernah ter-install) — diganti heading rule eksplisit scoped ke `.ProseMirror` di `alm-editor.tsx`.
  - **Diverifikasi ulang penuh setelah fix**: bikin lesson 7 block type lewat toolbar (heading, paragraph, example, flashcard, indonesian_learner_alert, common_trap, think_in_english) → reload editor (konfirmasi round-trip lewat backend parser) → publish → cek Viewer read-only. Ketujuh block selamat penuh, query DB `content_blocks` konfirmasi `raw_source` benar per block. Viewer read-only dicek langsung lewat DOM (bukan cuma visual): 0 `<input>` di dalam `.ProseMirror`, `contenteditable="false"` — field beneran non-interaktif, bukan cuma disembunyikan visual.
  - **1 kemungkinan artefak Playwright (bukan bug produk) dicatat**: run pertama yang ketik teks tanpa delay (`page.keyboard.type` default) menghasilkan heading "Present Perfect" hilang total dan tergabung ke paragraph berikutnya jadi 1 block heading aneh — run ulang dengan delay ketik manusiawi (`{ delay: 30 }` + `waitForTimeout` antar aksi) menghasilkan hasil benar sempurna. Disimpulkan Playwright mengirim keystroke lebih cepat dari yang React/ProseMirror bisa proses untuk contenteditable, bukan defect nyata — tapi kalau muncul lagi di sesi verifikasi berikutnya, jangan langsung diasumsikan artefak, cek ulang.
- **User: `ndsanja@gmail.com` (Sanja) di-set jadi `platform_admin` di org "Smoke Org" (2026-08-24)** — permintaan eksplisit user ("jadikan ndsanja@gmail.com super admin biar bisa mengakses semua fitur"). `platform_admin` di `permissions.rs` bypass org-scoping sepenuhnya dan masuk di setiap baris matrix permission — akses penuh persis yang diminta. Perubahan data (`user_organization_roles`), bukan kode — role `student` lama di org itu dihapus, diganti 1 baris `platform_admin`.
  - **Diulang di `titian_bun` (2026-08-31)** — permintaan sama persis user, kali ini di database backend aktif (data user tidak ikut pindah waktu migrasi ADR-0009, `titian_bun` adalah database terpisah dari `titian-postgres` milik Rust). Akun `ndsanja@gmail.com` sudah ada (login Google asli sebelumnya) tapi belum punya `user_organization_roles` row sama sekali. Org baru dibuat khusus untuk ini — **"ALR Platform"** (`type: platform`, satu-satunya org bertipe itu, beda dari org-org `school`/`tutor_org` yang lain) — lalu 1 baris `platform_admin` ditambahkan. Perubahan data langsung lewat `psql`, bukan kode (tidak ada authoring API untuk organizations/user_organization_roles).
- **`titian-web`: Content Studio v2 — Question Bank UI (2026-08-24)** — `/studio/question-banks`, scope yang sengaja ditunda dari Content Studio v1. User diberi pilihan lanjut P2-015 (OCR) atau ini, pilih ini. 3 endpoint backend baru ditambah, mirror persis pola `GET /curricula`/`GET /lessons/{id}` dari sesi Content Studio v1: `GET`/`POST /question-banks` (list+create bank, `Resource::QuestionBank, Action::Create` yang sudah ada dipakai ulang, bukan permission baru) dan `GET /questions/{id}` (detail penuh termasuk `correct_answer` — reuse `question_repository::find_by_id` yang sudah ada tapi belum pernah punya route; `ViewUnpublished` ditambah ke matrix untuk `QuestionBank`, role list sama seperti `Lesson`'s). 5 integration test baru (total 174).
  - **1 gap desain ditemukan & ditutup saat implementasi FE, bukan di planning**: list row butuh preview (prompt soal / jumlah pasangan), tapi `QuestionSummary` (`GET /question-banks/{id}/questions`) cuma punya id/type/difficulty/status/qa_report — tidak ada `data`. Ditambah `data` ke summary itu (`correct_answer` sengaja tidak, `GET /questions/{id}` yang jadi review surface).
  - FE: 3 layar (`question-banks/page.tsx` list+create, `question-banks/[id]/page.tsx` detail+list soal, dialog detail+aksi status) + `add-question-dialog.tsx` (2 tab: tulis manual per-tipe pakai local state buat field dinamis (opsi mcq, pasangan matching) — bukan `useFieldArray`, sama gaya kayak `ManualLessonForm`'s `content` state — atau generate lewat AI asli). Status action (submit-review/publish/reject) ditaruh di dalam dialog detail, bukan halaman terpisah, karena tidak ada alur edit-setelah-create yang butuh route sendiri.
  - **1 bug nyata ditemukan pas nulis nav**: `pathname.startsWith('/studio/')` bikin "Curricula" ikut nyala terang pas lagi di `/studio/question-banks` (karena `/studio` prefix dari semua route Studio lain) — diperbaiki, "Curricula" cuma nyala di `/studio` persis atau `/studio/curricula/*`.
  - **Diverifikasi end-to-end lewat sesi browser asli penuh**: bikin bank → bikin 1 soal tiap tipe (mcq/fill_blank/matching) manual → buka "Lihat" (jawaban benar ter-highlight benar untuk ketiga tipe, dikonfirmasi juga lewat DOM: 0 input dst tidak relevan di sini karena bukan editor) → submit-review → publish → generate 2 soal lagi lewat AI asli (nemuin & memperbaiki bug nesting `correct_answer`, lihat "Deviasi") → konfirmasi keduanya muncul status draft. `console --errors` bersih. Semua data test dihapus.
- **Drive-style file/media management (2026-08-29)** — `/studio/media`. User minta "sekalian file management, biar bisa dites embed media di ALM Studio" lalu, ditanya scope (AskUserQuestion), eksplisit minta **penuh**: "hampir mirip Google Drive... upload, sharing, folder, collaboration". 2 pertanyaan lanjutan dijawab: sharing = per-user/role ACL penuh (bukan cuma role-based sederhana), collaboration = ownership+activity trail (bukan komentar/version history — keduanya eksplisit ditolak).
  - **Data model baru** (additive, 3 migrasi 0016-0018): `assets.filename`/`folder_id`/`updated_at`/`deleted_at`; tabel `folders` (self-referencing `parent_folder_id`, `WITH RECURSIVE` ancestor/descendant walk sama pola seperti `concept_repository` ADR-0007); `resource_shares` + `resource_activity` — **satu pasang tabel polimorfik** (`resource_type` 'asset'|'folder') dipakai untuk KEDUANYA, bukan 4 tabel nyaris-duplikat.
  - **`service/drive_permissions.rs`** — potongan yang genuinely baru: access control per-resource (beda dari `permissions.rs`'s matrix role statis). `platform_admin`/owner → Editor; kalau tidak, cari grant tertinggi di `resource_shares` yang cocok resource-nya SENDIRI atau folder leluhurnya (jadi share 1 folder otomatis nurun ke semua isi di dalamnya) DAN (`principal_type=user` cocok `ctx.user_id`) ATAU (`principal_type=role` cocok `ctx.role`). Sharing/restore-dari-trash/permanent-delete cuma boleh owner/platform_admin — grant Editor tidak dapat hak share-ulang (2 layer auth, bukan 3).
  - **Kenapa ini urgent, bukan cuma "tombol upload"**: `block_schema.rs` sudah lama mewajibkan `data.asset` berupa `asset://<id>` (nolak URL mentah — "see ADR-0001/2.1"), tapi **tidak ada apa pun yang pernah meresolusi `asset://<id>` jadi URL yang bisa diputar** — `assets.url` cuma signed URL sekali-tulis yang expire (1 jam private/7 hari public). `GET /assets`/`GET /assets/{id}`/`GET /drive*` sekarang re-sign `url` fresh tiap baca (`storage.signed_url`, HMAC lokal, tidak ada network call) — tanpa ini, `asset://` reference sama sekali tidak bisa dipakai.
  - **`GET /drive/share-candidates` baru, bukan reuse `GET /organizations/{id}/members`**: endpoint itu digerbang org_owner/academic_director (ADR-0006) — persis akun curriculum_developer/reviewer yang justru butuh sharing sehari-hari bakal 403. Endpoint baru scoped ke org caller sendiri, tanpa gerbang role.
  - **9 integration test baru** (`tests/drive_test.rs`, total 183): folder CRUD, cascade soft-delete+trash+restore, sharing grant/revoke (403→200→403), folder-share nurun ke asset di dalamnya, role-based share, owner-only share management.
  - **FE**: `driveApi` di `api-client.ts` (termasuk `requestForm`/`authedFormRequest` baru buat body `FormData` — `authedRequest` selalu set `Content-Type: application/json` yang bakal ngerusak multipart boundary). `/studio/media`: navigasi folder+breadcrumb, upload (tombol + drag-drop), rename/pindah/bagikan/hapus lewat context-menu per-item, trash+restore, "Dibagikan dengan saya". `MediaPickerDialog` (reuse `DriveGrid` mode picker) dipasang ke field `asset` di `alm-editor/directive-node-view.tsx` (`DirectiveField` dapat `kind: "asset"` baru) — **ini permintaan awal user**: field audio/video/image sekarang preview media asli + tombol "Pilih media", bukan textarea nunggu `asset://` diketik manual.
  - **3 bug nyata ditemukan lewat Playwright, bukan cuma "kelihatan jalan"**: (1) CORS `main.rs` belum mengizinkan method `DELETE` (persis pola bug `PUT` yang sama sebelumnya — `CorsLayer` cuma di `main.rs`, test Rust manapun bypass ini lewat `routes::build(state).oneshot(...)` langsung, cuma browser asli yang nemuin) — trash/unshare/permanent-delete gagal total di request pertama sampai diperbaiki. (2) `request()` di `api-client.ts` selalu panggil `.json()` termasuk buat response `204 No Content` (semua endpoint DELETE Drive) — `.json()` di body kosong throw error, ditambah short-circuit `if (status === 204) return undefined`. (3) Context-menu di trash view pakai label "Hapus" yang sebenarnya berarti "pulihkan" (dipakai ulang dari mode normal) — dipisah jadi mode trash eksplisit ("Pulihkan"/"Hapus permanen") biar tidak ambigu dengan makna hapus-beneran.
  - **1 temuan yang TERNYATA bukan bug** (dicek dulu sebelum diklaim): edit-control (tombol "Ganti media"/X) masih kelihatan di lesson yang BARU SAJA di-publish tanpa reload halaman. Dicek lewat navigasi fresh (`page.goto` langsung, bukan transisi status live di tab yang sama) — hasilnya 0 input, 0 button, `contenteditable=false`, genuinely benar. Ini pola staleness TipTap node-view yang sudah ada sebelumnya (`setEditable` tanpa transaksi dokumen tidak otomatis re-render `ReactNodeViewRenderer` node view manapun) — berlaku sama untuk SEMUA directive type, bukan spesifik ke field asset yang baru, jadi tidak diperbaiki di sesi ini (di luar scope, butuh investigasi arsitektural tersendiri kalau memang jadi masalah nyata di alur reviewer sungguhan).
  - **Diverifikasi end-to-end lewat sesi browser asli penuh**: bikin folder → upload gambar (drag-drop asli, PNG kecil di-generate manual) → ganti nama → hapus (trash) → pulihkan → bagikan (dialog kebuka, share-candidates kepanggil) → bikin lesson baru → slash-command "/gambar" → "Pilih media" → browse folder → pilih gambar → preview inline muncul di editor → simpan → reload → submit-review → publish → **gambar tetap tampil di Viewer read-only** dengan `asset://<uuid>` yang sama (dikonfirmasi lewat `content_blocks.raw_source` langsung). `console --errors` bersih semua langkah. Semua data test dihapus.
- **P1-002: User & Profile + middleware AuthContext** — `GET /users/me` dan `GET /organizations/{id}/members` jalan penuh, 7 integration test lulus (total 18 test di `titian-backend`). `AuthContext` (`middleware/auth_context.rs`) sekarang jadi extractor axum yang dipakai handler manapun yang butuh auth — pola ini yang bakal dipakai P1-003 dst. Detail lengkap di `docs/tickets/phase-1.md`.
- **P1-003+P1-004: Content API + Question Bank API** — `GET /curricula/{id}/tree`, `GET /lessons/{id}`, `POST`/`GET /question-banks/{id}/questions` jalan penuh, 11 integration test lulus (total 29 test) + smoke test manual end-to-end lewat curl pakai data seed asli (`titian-backend/seed.sql`, aman dijalankan ulang). `service/permissions.rs` dipecah jadi `require_permission` (role-only, buat resource platform-wide kayak `curricula`/`question_banks` yang di `domain-model.md` memang tidak punya kolom `organization_id`) vs `require_permission_in_org` (buat resource yang scoped ke 1 org kayak org members). Detail lengkap di `docs/tickets/phase-1.md`.
- **P1-005+P1-006: Assessment API + Attempt Submit/Scoring** — `GET /assessments/{id}`, `POST /assessments/{id}/attempts` (tidak pernah bocorkan `correct_answer`, tolak attempt in-progress duplikat), `POST /attempts/{id}/submit` (auto-grade mcq/fill_blank via `service/grading.rs`, tolak submit ganda/kurang jawaban) jalan penuh, 10 integration test + 4 unit test grading lulus (total 43 test) + smoke test manual end-to-end lewat curl (skor 100 buat semua benar, 422 `missing` yang isinya benar, 409 submit ganda — semua cocok kontrak). Detail lengkap di `docs/tickets/phase-1.md`.
- **P1-007: Learning Event Writer** — setiap `POST /attempts/{id}/submit` sekarang menulis 1 `learning_event` (`event_type=question_answered`) per soal dijawab, payload `{correct: bool|null, difficulty, question_id, concept_ids}` (`repository/learning_event_repository.rs`, `question_repository::find_concept_ids_for_questions`). `learning_events_created` di response sekarang count asli (bukan hardcode `0` lagi). 2 integration test baru + smoke test manual end-to-end lewat curl. Detail lengkap di `docs/tickets/phase-1.md`.
- **P1-008+P1-009: Mastery Calculator v1 + FRSS Scheduler v1** — `GET /mastery/{concept_id}` dan `GET /review-queue?limit=` jalan penuh. Formula ADR-0002 (`service/mastery.rs::compute`) dan ADR-0003 (`service/frss.rs::apply`) diimplementasi sebagai pure function, masing-masing dengan unit test yang mereproduksi angka contoh manual di ADR **persis** (`mastery_score = 72`, tabel 5 siklus SM-2). `assessment_service::submit_attempt` sekarang memicu recompute mastery + update FRSS untuk tiap concept yang tersentuh submit — **checkpoint keluar Phase 1 poin 4 (submit → learning_events → masteries → frss_schedule) sekarang jalan end-to-end**, diverifikasi manual lewat curl (termasuk retroactive concept-linking dan review-queue gap/due filtering). **Ada 1 deviasi ADR yang sengaja & di-flag** — lihat "Deviasi" di bawah soal trigger FRSS. Detail lengkap (termasuk kenapa `min_gap_hours` tidak butuh kolom baru) di `docs/tickets/phase-1.md`.
- **P1-010: Asset Upload (Cloudflare R2)** — `POST /assets/upload` jalan penuh lewat trait `service/storage.rs::AssetStorage` (real `R2Storage` di prod, `InMemoryStorage` di test — pola sama seperti `GoogleTokenVerifier`). Smoke test manual **ke R2 asli** (bukan cuma test lokal): upload lewat curl → signed URL asli → di-fetch lagi → 200 dengan isi file yang benar. Bucket `lms` (punya bersama proyek `lms/` lama) di-namespace pakai prefix `titian/assets/`. Detail di `docs/tickets/phase-1.md`.
- **P1-011: AI Gateway v1 (grammar_evaluation)** — `POST /ai/evaluate` jalan penuh sesuai alur ADR-0004 (cost estimate → cek saldo → provider → validasi output → `ai_tasks` → charge credit ADR-0005), lewat trait `service/ai_provider.rs::AIProvider`. **Provider asli = OpenRouter (bukan DeepSeek API langsung)** — satu-satunya kredensial AI yang ada — routing ke model `deepseek/deepseek-chat`, `ai_tasks.provider` tetap tercatat `"deepseek"`. Smoke test manual **ke OpenRouter/DeepSeek asli** berhasil penuh (skor grammar check benar, credit ke-charge, semua row DB benar). **2 keputusan penting di-flag** — lihat "Deviasi" di bawah (substitusi provider, dan markdown-fence stripping karena model tidak selalu patuh instruksi "no fences"). Detail di `docs/tickets/phase-1.md`.
- **P1-012: Health Check & Logging Standar** — `x-request-id` middleware terpasang di `main.rs` (`tower_http::request_id`, auto-generate + propagate + masuk ke setiap `tracing` span), jadi semua log 1 request kebawa id yang sama otomatis. Error format diaudit manual — semua kode error di semua handler cocok kontrak. **Redis sengaja tidak diimplementasikan** (tetap jujur lapor `"not_configured"`) — tidak ada satupun fitur Phase 1 yang butuh Redis, nambah infra itu buat lolos 1 baris health check adalah scope creep. **1 temuan besar di-flag** — lihat "Deviasi" di bawah soal `api-contract.md`'s "Base URL: /api/v1" yang tidak pernah diimplementasikan. Detail di `docs/tickets/phase-1.md`.
- **P1-013: Integration Test Suite Penuh** — cross-check konfirmasi semua 15 route punya ≥1 integration test; **1 gap nyata ditemukan & ditutup**: `POST /auth/google/callback` belum pernah dites lewat HTTP route asli (cuma fungsi level-bawah) — ditambah `GoogleTokenVerifier::with_seeded_jwk` (`service/google_oauth.rs`) supaya jalur `verify()` produksi asli bisa dites tanpa network call ke JWKS Google. `tests/phase1_checkpoint_test.rs::phase1_exit_checkpoint_end_to_end` — 1 fungsi test, 1 user, 1 router, mengalir lurus lewat kelima checkpoint keluar Phase 1 tanpa reset state di antaranya. **Kelima checkbox "Checkpoint keluar Phase 1" sekarang dicentang** (sebelumnya sengaja kosong sampai ada bukti otomatis, bukan cuma verifikasi manual). Detail di `docs/tickets/phase-1.md`.

**Phase 1 — Backend Core resmi tertutup** (P1-001 s/d P1-013 semua done).

- **P2-001: ADR-0007 Concept Hierarchy** — `concepts.parent_concept_id` (migrasi `0011`), `concept_repository::find_ancestors`/`find_descendants` (`WITH RECURSIVE`), `concept_service::set_parent` (cycle prevention di application layer — Postgres tidak bisa enforce ini di self-referencing FK). 6 test di `tests/concept_hierarchy_test.rs`. Detail di `docs/tickets/phase-2.md`.
- **P2-002: ADR-0008 Content Versioning & Attempt Snapshot** — `attempts.question_snapshot` (migrasi `0012`, captured di submit time, terverifikasi tidak berubah walau `questions` diedit belakangan), `questions.superseded_by`/`lessons.superseded_by` (schema siap, belum ada consumer — itu scope tiket berikutnya). 1 test baru di `tests/assessment_test.rs`. Detail di `docs/tickets/phase-2.md`.
- **P2-003: Content Block SDK v1** — `service/block_schema.rs` (registry 8 tipe block: text/heading/example/audio/video/image/flashcard/question_embed), `content_block_service::validate_and_replace_blocks` (transaksional, cek referensi `question_embed` batched). 4 integration test + unit test per tipe. Detail di `docs/tickets/phase-2.md`.
- **P2-004: Question Type Registry v1** — `service/question_schema.rs` di-refactor dari `match` jadi trait `QuestionTypeValidator` + registry (pola sama `AssetStorage`/`AIProvider`), `matching` ditambah sebagai bukti extensibility. Tidak ada regression di test P1-004. Detail di `docs/tickets/phase-2.md`.
- **P2-005: Content & Question Publish Flow** — `service/publish_flow.rs` (1 state machine dipakai `questions` DAN `lessons`), 6 endpoint baru (`/questions|lessons/{id}/submit-review|publish|reject`), permission baru di ADR-0006 pattern (`curriculum_developer` submit-review, **bukan** publish — cuma reviewer+). Menutup "P1-004b" yang dulu ditunda. 9 integration test + 3 unit test + **smoke test manual end-to-end ke server asli** (submit→publish→publish-ulang-ditolak). Detail di `docs/tickets/phase-2.md`.
- **P2-006: ALM Parser v1** — `service/alm_parser.rs`, pure function, `#`/`>`/`:::type:::` directive → typed blocks. **Temuan penting**: contoh ALM di dokumen sumber sendiri kontradiktif soal `question_embed` (contoh kode isi soal inline, aturan tertulis bilang by-reference only) — parser mengikuti aturan tertulis, didokumentasikan eksplisit di kode + `docs/tickets/phase-2.md`. 11 unit test termasuk 1 yang menjalankan contoh lengkap dari `ALR_Phase_Detail_Breakdown.md` 2.1 persis. Detail di `docs/tickets/phase-2.md`.
- **P2-007: Paste Normalizer v1** — `service/paste_normalizer.rs`, HTML→ALM (tag scanner sederhana, bukan parser HTML5 penuh — cukup untuk paste dari Word/Google Docs/website sesuai scope). Golden test: `<h2>X</h2>` dan `## X` menghasilkan AST identik (dibandingkan `Value`, bukan cuma "mirip"). 5 unit test. Detail di `docs/tickets/phase-2.md`.
- **P2-008: Lesson Authoring API** — `POST /lessons`, `PUT /lessons/{id}` (body: `content`+`format`, server jalankan normalizer→parser→validate→write dalam 1 request). ADR-0008 dienforce nyata: edit lesson `published` ditolak (`cannot_edit_published_content`). 6 integration test + **smoke test manual end-to-end ke server asli**: bikin lesson via ALM multi-baris persis contoh "Present Simple" dari dokumen sumber, hasil block-nya cocok persis (heading→text→example→example). Detail di `docs/tickets/phase-2.md`.
- **P2-009: Curriculum/Level/Unit Authoring API** — `POST /curricula`, `POST /curricula/{id}/levels`, `POST /levels/{id}/units`. **1 test rantai penuh** (`full_authoring_chain_shows_up_in_curriculum_tree`): curriculum→level→unit→lesson dibuat murni lewat API P2-008/P2-009, muncul persis di `GET /curricula/{id}/tree` — endpoint Phase 1 yang tidak diubah sama sekali, bukti Phase 1+2 satu sistem. Detail di `docs/tickets/phase-2.md`.
- **P2-010: Direct-to-R2 Presigned Upload** — `POST /assets/presigned-upload` + `POST /assets/confirm` jalan penuh lewat 2 method baru di trait `AssetStorage` (`presigned_put_url`, `exists`). Desain stateless (tidak ada row "pending" — key UUID server-generated cuma bisa ditulis lewat presigned URL yang sah, `confirm` verifikasi `exists()`/HEAD sebelum menulis row `assets`). `assets.visibility` (`public`/`private`, migrasi `0013`) menentukan TTL signed GET URL yang dikembalikan. 2 test baru + **smoke test manual ke R2 asli penuh**: presigned PUT → upload file beneran → confirm-sebelum-upload ditolak `422` → confirm sukses → GET signed URL balikin isi file yang benar. Detail di `docs/tickets/phase-2.md`.
- **P2-011: Curriculum Constitution (Grammar 11-Section Pattern)** — `agent/docs/curriculum-constitution.md` (direview, isi pola 11-bagian persis dari `ALR_Phase_Detail_Breakdown.md` 2.9) + `service/curriculum_constitution.rs::validate_grammar_lesson`, dicek di `POST /lessons/{id}/submit-review` untuk lesson yang tertaut ke concept `type='grammar'` (lewat `lesson_concepts`, bukan `lessons.type` yang enum-nya beda). Matching numeric-prefix ("05 — ..."), bukan keyword, sengaja hindari false positive. 4 unit test + 3 integration test + **smoke test manual end-to-end ke server asli**: lesson ALM asli (pakai block Indonesian learner P2-012) kurang 1 section ditolak `422 grammar_constitution_incomplete` menyebut persis section yang hilang, dilengkapi lewat `PUT /lessons/{id}`, submit-review sukses. Detail di `docs/tickets/phase-2.md`.
- **P2-012: Indonesian Learner Support Blocks** — 3 block type baru di registry P2-003 (`indonesian_learner_alert`, `common_trap`, `think_in_english`), tidak butuh perubahan `alm_parser.rs` sama sekali (directive generik `:::type\nkey: value\n:::` otomatis mendukung tipe baru begitu terdaftar). `concepts.indonesian_difficulty_tag` (migrasi `0014`, nullable, dipilih di `concepts` bukan `questions` karena weakness detection Phase 3 beroperasi di level concept). 6 unit test baru + **dibuktikan lewat smoke test manual P2-011** (ketiga block type ditulis via ALM asli, ter-parse+tervalidasi lewat `POST /lessons`). Detail di `docs/tickets/phase-2.md`.

- **P2-013: AI Content Generation Pipeline v1 (Lesson + Question)** — `POST /ai/generate-lesson`, `POST /ai/generate-questions` jalan penuh lewat AI Gateway (P1-011). Validasi output AI (parse ALM/block schema/Grammar Constitution untuk lesson; JSON array+P2-004 schema untuk question) selalu terjadi **sebelum** ada row `lessons`/`questions` ditulis — generation gagal tidak pernah menyisakan row setengah jadi (dibuktikan test DB-level dan smoke test manual). 0 credit charge (ADR-0005, cost platform), `ai_tasks` tetap tercatat baik sukses maupun gagal. 10 integration test (`tests/ai_content_test.rs`) + **smoke test manual ke DeepSeek/OpenRouter asli**: 2 percobaan pertama gagal karena model salah syntax directive ALM (`ai_tasks.status=failed` tercatat benar, **tidak ada orphan lesson row**, diverifikasi query DB langsung) — diperbaiki dengan system prompt yang kasih contoh syntax persis; setelah itu lesson non-grammar, lesson grammar (11-section lengkap), dan 3 soal mcq semua sukses generate dengan konten yang masuk akal. Detail lengkap (termasuk kenapa gagal awalnya bukan bug) di `docs/tickets/phase-2.md`.
- **P2-014: Content QA Agent v1** — QA otomatis jalan di `POST /lessons|questions/{id}/submit-review` (P2-005), hasil `{passed, issues[]}` disimpan (`qa_report` JSONB, migration 0015) dan dikembalikan di response + `GET /lessons/{id}`/`GET /question-banks/{id}/questions` — **QA fail tidak pernah memblokir transisi ke `in_review`** (2 test eksplisit assert 200 + `passed:false` sekaligus, prinsip "gerbang manusia"). 3 kategori diimplementasi (`grammar_constitution`, `question_schema`, `cefr_mismatch` — kolom baru `questions.cefr_tag`); kategori ke-4 ("konsistensi terminologi grammar") **sengaja tidak dibangun**, tidak ada spec konkret di dokumen sumber, di-flag eksplisit bukan didiamkan. 7 integration test (`tests/content_qa_test.rs`) + 2 unit test. Detail di `docs/tickets/phase-2.md`.

**Total test `titian-backend` di akhir Phase 2 (P2-001 s/d P2-014): 169** (`cargo test`, butuh `DATABASE_URL` ke Postgres yang bisa CREATEDB). Sesi-sesi FE berikutnya nambah lebih banyak lagi (lihat masing-masing entri di atas) — **total sekarang: 183**.

## Blocker aktif
**Google Sign-In belum bisa dites end-to-end sampai origin di-whitelist.** Waktu dicoba di `http://localhost:3000`, Google nolak dengan `[GSI_LOGGER]: The given origin is not allowed for the given client ID` (403). Ini bukan bug kode — client ID di `Credential.md`/`google-auth.md` belum punya `http://localhost:3000` (dan domain produksi nanti) di daftar "Authorized JavaScript origins"-nya. **Cuma user yang bisa fix ini** (butuh akses Google Cloud Console project punya client ID `1029049482781-...`), bukan sesuatu yang bisa diperbaiki dari kode. Langkah: Google Cloud Console → APIs & Services → Credentials → pilih OAuth client itu → tambahkan `http://localhost:3000` ke "Authorized JavaScript origins" → save (biasanya langsung aktif, kadang perlu beberapa menit). Sisa alur (redirect kalau belum login, error handling, sesi persist, dsb) sudah diverifikasi jalan lewat Playwright — cuma langkah terakhir (klik tombol Google beneran) yang belum bisa dites dari sini.

## Lingkungan dev lokal (state mesin ini, bukan "keputusan" — dicatat biar next action tidak install ulang)
- Rust toolchain (rustup, stable) ter-install di `~/.cargo` — **tidak ada di PATH shell non-interaktif**, selalu `source "$HOME/.cargo/env"` dulu sebelum `cargo`/`sqlx` di sesi baru.
- `sqlx-cli` ter-install (`cargo install sqlx-cli --no-default-features --features rustls,postgres`).
- Postgres lokal jalan via Docker: `docker run -d --name titian-postgres -e POSTGRES_USER=titian -e POSTGRES_PASSWORD=titian_dev_password -e POSTGRES_DB=titian -p 5432:5432 postgres:16-alpine`. Kalau container ini mati/hilang, `titian-backend/.env`/`.env.example` sudah berisi `DATABASE_URL` yang cocok — tinggal jalankan ulang container dengan command yang sama.
- Git identity di-set **lokal per-repo saja** (bukan global) di `titian-web` dan `titian-backend`: `Sanja <ndsanja@gmail.com>`. `alr/` (root docs repo) belum di-set — set kalau mau commit di situ lagi dan belum ke-inherit.
- Dev server `titian-web` (`next dev --webpack`, port 3000) dan `titian-backend` (`cargo run`/binary langsung, port 8080) sempat dijalankan manual di background lintas sesi (termasuk sesi P1-001 ini) untuk verifikasi — kemungkinan sudah tidak jalan lagi begitu sesi berakhir (bukan proses yang di-daemonize, bukan systemd/supervisor).
- `titian-backend` sekarang struktur `lib.rs` + `main.rs` (dipecah di sesi P1-001 supaya `tests/*.rs` bisa import modul internal & build router asli). Kalau nambah module baru, daftarkan di `lib.rs`, bukan `main.rs`.
- **(Stale, lihat entri 2026-08-31 di bawah) `titian-web` dulu dikelola `pnpm`** — sudah dimigrasi penuh ke Bun (package manager + runtime), `pnpm-lock.yaml`/`pnpm-workspace.yaml` sudah dihapus. Catatan ini ditinggal sebagai jejak kenapa keputusan itu diambil, bukan instruksi aktif lagi.

## Deviasi dari rencana
- `domain-model.md` (SQL migration siap pakai) dan `api-contract.md` ditulis lebih awal dari urutan ticket asli (harusnya P0-007/P0-008) karena isinya derivatif langsung dari ADR-0001 — bukan keputusan baru, jadi tidak perlu ADR terpisah.
- P0-009 dikerjakan sebelum P0-007/P0-008 pada sesi sebelumnya (bukan pelanggaran urutan — P0-009 "Depends on: -", user eksplisit minta mulai dari UI). P0-007/P0-008 lalu dikerjakan di sesi ini, setelah backend discaffold — urutan akhirnya: P0-009 (web) → P0-009 (backend scaffold kosong) → P0-007 → P0-008, bukan urutan linear dokumen asli, tapi semua acceptance criteria tetap terpenuhi.
- **Monorepo → repo terpisah per app** (lihat bagian "Struktur repo" di atas). Ini membatalkan satu poin yang disebut "locked" di `ALR_Build_Roadmap.md`. Keputusan datang langsung dari user.
- Next.js 16 default ke Turbopack, tapi PWA plugin (`@ducanh2912/next-pwa`) hanya support webpack → `titian-web` dev/build script dipaksa `--webpack`.
- shadcn/ui di `titian-web` di-init dengan library **Base UI** (bukan Radix, preset "Nova"/Lucide+Geist) — beda API polymorphism (`render` prop, bukan `asChild`).
- `titian-backend` pakai `docker run` manual untuk Postgres lokal, bukan `docker-compose` — tidak ada docker-compose.yml di repo ini. Kalau P0-010 (CI) atau kerja tim butuh compose file, itu belum dibuat.
- Belum ada ADR baru untuk keputusan stack/struktur repo di atas — dianggap keputusan tooling/proses, bukan keputusan yang mengunci data model, AI gateway, atau permission matrix (cakupan ADR 0001–0006).
- **Tabel `refresh_tokens` ditambahkan** (migrasi ke-10, `titian-backend/migrations/0010_auth_sessions.*`) untuk P1-001 — tidak ada di dump ERD asli ADR-0001. Ini additive (tabel baru, bukan ubah tabel lama), jadi menurut aturan "JANGAN lakukan ini tanpa ADR baru" di bawah tidak melanggar (yang dilarang cuma ubah `learning_events`/`masteries`/`frss_schedule`, dan mengunci struktur tanpa ADR — nambah tabel baru untuk kebutuhan ticket yang sudah disetujui itu normal). Sudah disinkronkan ke `docs/domain-model.md`.
- Google id_token diverifikasi dengan fetch JWKS asli Google (`https://www.googleapis.com/oauth2/v3/certs`) di runtime, di-cache in-memory (`Arc<RwLock<Vec<Jwk>>>`, refresh sekali kalau `kid` tidak ketemu). Belum ada TTL/refresh berkala terjadwal — kalau Google rotate key lebih sering dari yang diasumsikan ini masih aman (refresh-on-miss), tapi kalau butuh proactive refresh itu future work, bukan blocker sekarang.
- **Gotcha zustand-persist (kejadian di sesi ini, catat biar tidak keulang):** jangan panggil `useXStore.setState(...)` di dalam `onRehydrateStorage` milik store yang sama — itu bisa jalan **sinkron selama `create()` masih berjalan**, sebelum `const useXStore = ...` selesai di-assign, jadi referensinya belum ada (gagal diam-diam, hydration flag jadi macet, komponen yang nunggu hydration nge-hang selamanya tanpa error). Pola yang benar: taruh registrasi listener-nya **setelah** `const useXStore = create(...)` selesai, pakai `useXStore.persist.onFinishHydration(fn)` + cek `useXStore.persist.hasHydrated()` buat kasus udah kejadian duluan. Lihat `titian-web/src/store/auth-store.ts` buat contoh polanya.
- **Trigger FRSS: ADR-0003 vs checkpoint Phase 1 (flagged, P1-009 session) — resolusi dipilih, dicatat di sini biar next session tidak kaget:** ADR-0003 bilang update FRSS "dipicu dari learning_event hasil review, bukan attempt biasa" (implikasinya butuh alur "review" terpisah dari submit assessment biasa). Tapi checkpoint keluar Phase 1 poin 4 eksplisit minta submit attempt memicu update `masteries` **dan** `frss_schedule` sebagai 1 jalur kritis, dan Phase 1 belum punya ticket/endpoint apapun untuk alur "review" terpisah. Resolusi: untuk MVP, `submit_attempt` biasa **juga** memicu FRSS update (dianggap "review" sampai ada alur terpisah). Kalau nanti alur practice vs review perlu dipisah beneran (kemungkinan pas recommendation engine dibangun), itu butuh ADR baru yang men-supersede bagian trigger di ADR-0003 — jangan diam-diam diubah lagi tanpa dokumentasi. Detail lengkap di catatan implementasi P1-009 (`docs/tickets/phase-1.md`).
- **AI provider asli untuk P1-011: OpenRouter, bukan DeepSeek API langsung (flagged, P1-011 session):** ADR-0004's routing table nyebut "deepseek" sebagai provider default, tapi kredensial AI yang tersedia di `Credential.md` cuma OpenRouter API key, bukan DeepSeek API key langsung. OpenRouter itu router OpenAI-compatible yang bisa target model DeepSeek lewat model id (`deepseek/deepseek-chat`) — jadi `service/ai_provider.rs::DeepSeekProvider` secara transport HTTP manggil OpenRouter, tapi kolom `ai_tasks.provider` tetap `"deepseek"` sesuai routing table. Kalau nanti dapat DeepSeek API key langsung, tinggal ganti base URL di `DeepSeekProvider`, tidak perlu ubah struktur data/kontrak. Kalau butuh provider lain (OpenAI dst) yang beda API key, itu perlu keputusan baru soal kredensial, bukan sekadar kode.
- **Model AI default/text diganti ke DeepSeek V4 Flash, model vision disiapkan buat OCR (2026-08-24, instruksi eksplisit user, sebelum mulai P2-015):** `config.rs`'s 3 model config (`ai_grammar_evaluation_model`/`ai_lesson_generation_model`/`ai_question_generation_model`, dipakai lewat `AIProvider` trait yang sama, cuma ganti default env var) pindah dari `deepseek/deepseek-chat` ke `~deepseek/deepseek-v4-flash-latest` (alias OpenRouter yang auto-redirect ke snapshot DeepSeek V4 Flash terbaru — dikonfirmasi lewat `GET https://openrouter.ai/api/v1/models`, bukan ditebak). 1 field config baru `ai_ocr_model` (default `deepseek/deepseek-v4-flash-vision-exp`, model vision-capable, di-override lewat `AI_OCR_MODEL`) ditambah **mendahului P2-015** — belum ada consumer kode (OCR pipeline belum dibangun), murni disiapkan biar keputusan model tidak perlu diulang pas sesi P2-015 mulai. **Diverifikasi lewat smoke test asli ke OpenRouter DAN lewat server `titian-backend` yang jalan sungguhan** (bukan cuma compile check): `POST /ai/evaluate` (grammar_evaluation) dan `POST /ai/generate-lesson` keduanya sukses penuh dengan model baru — hasil grammar check benar, lesson ALM 11-section-ready ter-generate dengan konten masuk akal, `ai_tasks`/`credits`/`transactions` semua tercatat benar. Data test (curriculum/lesson/ai_tasks/transaction/refresh_token) sudah dihapus, `credits.balance` akun test dikembalikan ke 0.
  - **Follow-up (sesi Question Bank UI, hari yang sama):** `POST /ai/generate-questions` sempat gagal `422` beneran waktu dites lewat Question Bank UI asli — modelnya nesting `correct_answer` di dalam `data` (`{"data": {"prompt": ..., "correct_answer": {...}}}`) padahal shape di system prompt sudah eksplisit bilang itu field top-level, sibling dari `data`. Sama kategori masalah "model tidak selalu patuh instruksi abstrak" seperti markdown-fence P1-011 dan ALM-syntax P2-013 — fix yang sama: `question_type_shape_hint` diganti dari placeholder abstrak jadi contoh JSON terisi penuh + 1 baris peringatan eksplisit "jangan di-nest". Diverifikasi ulang sampai `generate-questions` sukses penuh ke provider asli (2 soal mcq draft valid ter-generate).
  - **Temuan penting yang belum di-follow-up, catat biar tidak kaget nanti:** `~deepseek/deepseek-v4-flash-latest` punya **reasoning mode enabled by default** (`default_effort: "high"` di metadata OpenRouter) — beda dari `deepseek/deepseek-chat` lama yang tidak reasoning. Smoke test `grammar_evaluation` (max_tokens 512) tetap sukses (kadang 0 reasoning token dipakai tergantung kompleksitas prompt), tapi smoke test `generate-lesson` (max_tokens 2048) jelas lebih lambat dari yang dirasakan sesi-sesi sebelumnya (>60 detik, sampai kena timeout default shell dan harus ditunggu di background). Belum ada perubahan kode buat matiin reasoning (`"reasoning": {"enabled": false}` didukung `supported_parameters` model ini kalau nanti mau dicoba) — kalau P2-015 (OCR, butuh respons cepat buat UX upload-scan) atau volume AI generation naik dan latensi ini jadi masalah nyata, itu next action yang belum dikerjakan, bukan sesuatu yang sudah dianggap selesai.
- **Model AI kadang bungkus JSON dalam markdown fence walau diminta jangan (P1-011 session):** smoke test manual pertama gagal (`ai_output_validation_failed`) padahal isinya JSON yang benar & lengkap — modelnya bungkus dengan ` ```json ... ``` ` walau system prompt eksplisit bilang "no markdown fences". Fix: `service/ai_gateway_service.rs::strip_code_fence` buang fence sebelum parse, JSON di dalamnya tetap divalidasi penuh sesudahnya (bukan bypass validasi ADR-0004, cuma toleran ke noise formatting yang umum dari LLM). Kalau nanti ada task AI lain yang butuh output non-JSON, jangan asal reuse fungsi ini tanpa mikir ulang.
- **`api-contract.md` bilang "Base URL: /api/v1" tapi tidak pernah diimplementasikan (ditemukan & di-flag saat audit P1-012, BELUM diperbaiki — butuh keputusan user):** semua route dari P1-001 s/d P1-011 nempel langsung di root (`/health`, `/auth/google/callback`, dst — lihat `titian-backend/src/routes/mod.rs`), bukan di bawah prefix `/api/v1` seperti yang tertulis di baris pertama `api-contract.md`. `titian-web` sudah manggil endpoint-endpoint ini tanpa prefix (jadi FE dan BE konsisten satu sama lain, cuma dokumennya yang menyimpang). **Belum diperbaiki** karena mengubah salah satu sisi (nambah prefix di router ATAU hapus baris itu dari dokumen) adalah keputusan yang lebih baik dikonfirmasi user dulu — kalau nanti mau nambah reverse proxy/gateway di depan production yang menyuntikkan prefix `/api/v1`, kode saat ini sudah kompatibel (tidak perlu ubah apa-apa); kalau tidak, opsi termudah cuma hapus baris "Base URL: /api/v1" dari `api-contract.md` biar dokumen cocok realita.
- **Sesi 1 dan sesi 2 Phase 2 (5 ticket, 2 ADR) dikerjakan sekaligus dalam 1 sesi, bukan berhenti di antaranya untuk approval user seperti rencana awal di `docs/tickets/phase-2.md`:** rencana strategi eksekusi semula eksplisit bilang "sesi 1 wajib berhenti dan menunggu user review/approve ADR-0007/0008 sebelum sesi 2 dimulai". User secara eksplisit meminta ("buat sekalian yang terbaik arsitektur dan lainnya yang bisa menopang fase 2 berikutnya, karena fase 2 ini memang fundamental") untuk langsung membangun arsitektur fondasi sekaligus — ini dianggap otorisasi eksplisit yang menggantikan gerbang "berhenti-dan-tanya" default, bukan pelanggaran diam-diam terhadap rencana sendiri. ADR-0007/0008 tetap ditulis lengkap dengan Context/Decision/Alternatives/Consequences (bisa direview user kapan saja, status `Accepted` bisa diturunkan lagi kalau user tidak setuju) — cuma tidak ada jeda menunggu konfirmasi eksplisit sebelum lanjut ke implementasi.
- **P1-004b (publish flow, dulu ditunda di Phase 1) resmi ditutup lewat P2-005** — bukan ticket terpisah "P1-004b" seperti nama sementara sebelumnya, digabung 1 ticket generik untuk `questions` DAN `lessons` (state machine identik). Lihat `docs/tickets/phase-1.md` P1-004 dan `docs/tickets/phase-2.md` P2-005 untuk detail lengkap.
- **Contoh ALM di `ALR_Phase_Detail_Breakdown.md` 2.1 kontradiktif dengan aturannya sendiri (ditemukan sesi P2-006):** contoh kode `:::question id="q_123" ...:::` menulis isi soal penuh inline, padahal 2 paragraf di bawahnya dokumen yang sama eksplisit melarang ini ("question tidak ditulis penuh di dalam ALM"). `alm_parser.rs` mengikuti **aturan tertulis** (by-reference only lewat `:::question_embed\nquestion_id: <uuid>\n:::`), bukan contoh kode yang salah — flagged di komentar kode dan `docs/tickets/phase-2.md`, bukan diam-diam dipilih salah satu tanpa catatan.
- **User eksplisit minta FE (titian-web) ditunda dulu (2026-08-23), lalu eksplisit minta dilanjutkan (2026-08-24):** ditanya apakah mau mulai kerja FE bareng BE, user awalnya jawab "kalau memang belum saatnya, tunda dulu sampai saat pembangunan dan integrasi ke FE" — jadi beberapa sesi backend Phase 2 dikerjakan dulu (sesi 1-5, P2-001 s/d P2-014). Sehari kemudian user eksplisit minta lanjut FE ("kerjakan untuk fe nya juga") di tengah sesi P2-015 (OCR pipeline) — P2-015 di-pause (statusnya tetap `todo`, belum ada kode ditulis untuk itu), kerja pindah ke FE. Ditanya lagi scope-nya spesifik (5 layar learner existing vs Content Studio admin UI baru vs keduanya) karena beda besar — user pilih **"keduanya, learner dulu baru Content Studio"**. Sesi FE pertama (2026-08-24) menyelesaikan bagian learner — lihat "Ticket selesai". Content Studio adalah kandidat sesi FE berikutnya, **bukan** P2-015 — konfirmasi ke user sebelum pilih lanjut P2-015 (backend) vs Content Studio (FE) di sesi berikutnya, jangan asumsi sendiri.
- **P2-014's "CEFR level tag konsisten dengan level curriculum-nya" butuh kolom baru yang tidak disebut eksplisit di ticket manapun (sesi P2-014):** tidak ada field CEFR tag di `questions`/`lessons` sebelumnya — cuma `levels.code` (level curriculum, bukan tag per-konten). Diputuskan: tambah `questions.cefr_tag` (nullable, migration 0015; sengaja tidak ditambah ke `lessons` — level lesson sudah implisit dari `unit_id → level_id`, tidak ada redundansi untuk di-mismatch-kan) dan `cefr_mismatch` check membandingkan tag ini terhadap `levels.code` lesson yang meng-`question_embed` question tsb. Belum ada endpoint authoring buat isi field ini (diset langsung lewat SQL untuk sekarang) — kalau butuh UI/API buat set `cefr_tag` saat create/edit question, itu ticket terpisah.
- **P2-014's kategori "grammar_constitution" jadi redundan-tapi-sengaja terhadap hard gate P2-011 (sesi P2-014):** P2-011 sudah menolak (`422`) submit-review untuk grammar lesson yang kurang section, sebelum QA Agent sempat jalan — jadi lewat endpoint asli, kategori ini di `qa_report` akan selalu "pass" untuk lesson yang berhasil sampai QA. Tetap diimplementasi (dites lewat `content_qa_service::run_lesson_qa` langsung, bukan endpoint) karena ticket P2-014 eksplisit menyebutnya sebagai salah satu dari 4 kategori QA — laporan QA yang lengkap lebih penting daripada menghindari 1 baris kode "redundan". Kategori ke-4 di 2.14 ("konsistensi terminologi grammar") **tidak diimplementasi sama sekali** — tidak ada spec/vocabulary konkret di dokumen sumber manapun, di-flag eksplisit di `content_qa_service.rs` dan `docs/tickets/phase-2.md`, bukan didiamkan atau dipaksakan jadi heuristik palsu.

## JANGAN lakukan ini tanpa ADR baru
- Jangan ubah struktur tabel di `domain-model.md` (khususnya `learning_events`, `masteries`, `frss_schedule` — dipakai lintas banyak fase) tanpa ADR baru yang menyatakan supersedes.
- Jangan panggil provider AI (DeepSeek/dst) langsung dari business logic — selalu lewat AI Gateway abstraction (ADR-0004).
- Jangan tambah role baru di luar daftar ADR-0006 tanpa update permission matrix.
- Jangan buat allowance subscription sebagai kolom terpisah dari `credits.balance` — semua saldo lewat agregasi `transactions` (ADR-0005).
- Jangan ubah semantik `concepts.parent_concept_id` (containment hierarchy) supaya jadi multi-parent/DAG, atau campur artinya dengan `concept_prerequisites` (yang tetap prerequisite graph, beda konsep) — tanpa ADR baru yang men-supersede ADR-0007.
- Jangan buat endpoint yang mengizinkan edit langsung ke row `questions`/`lessons` berstatus `published` (mutate in-place) — ADR-0008 mengunci "row published tidak pernah dimutasi, edit = row baru + `superseded_by`". Kalau butuh alur edit-setelah-publish yang lebih mulus dari yang ada sekarang (archive manual + draft baru), itu perlu direncanakan sesuai ADR-0008, bukan jalan pintas di kode.
- **Jangan campur metrik Gamification (XP/streak/achievement/league, Phase 8) ke formula `mastery.compute` (ADR-0002) atau `frss.apply` (ADR-0003)** — §6.1's roadmap sendiri mengunci 3 metrik terpisah (Learning Credits / XP / Mastery), XP tinggi TIDAK boleh jadi proxy kemampuan. `xp_service`/`streak_service`/`achievement_service` read-only terhadap `learning_events`/`masteries`, tidak pernah menulis ke tabel itu — tanpa ADR baru yang men-supersede ADR-0002/0003 secara eksplisit.

## Next action
**Migrasi backend Rust → Bun+ElysiaJS SELESAI + cutover SELESAI + retire SELESAI** (lihat "🟢 Migrasi Backend" + "🟢 CUTOVER SELESAI" di atas). `titian-backend-bun` sekarang backend aktif satu-satunya (`titian-web` default ke port 8081). **`titian-backend` (Rust) dihapus permanen 2026-09-03** atas instruksi eksplisit user ("full bun") — repo git-nya sendiri tidak punya remote, jadi diarsipkan dulu (`tar.gz` source+`.git` penuh, minus `target/` build cache 14G yang disposable) ke `~/backups/titian-backend-rust-archive-20260903.tar.gz` sebelum `rm -rf`, bukan langsung dibuang.

**🟢 Phase 2 SELESAI PENUH** (17/17 ticket, 5/5 checkpoint — lihat "Ticket selesai"'s P2-016+P2-017 entry), **🟢 Phase 3 SELESAI PENUH** (5/5 ticket, 4/4 checkpoint — lihat "Ticket selesai"'s P3-005 entry dan `docs/tickets/phase-3.md`), **🟢 Phase 4 SELESAI PENUH** (5/5 ticket, 5/5 checkpoint — lihat `docs/tickets/phase-4.md`, 224/224 test lulus), **🟢 Phase 5 SELESAI PENUH** (3/3 ticket, 4/4 checkpoint — lihat `docs/tickets/phase-5.md`, 260/260 test lulus), **🟢 Phase 6 SELESAI PENUH** (5/5 ticket, 5/5 checkpoint — lihat `docs/tickets/phase-6.md`, 250/250 test lulus), **🟢 Phase 7 SELESAI PENUH** (4/4 ticket, 4/4 checkpoint — lihat `docs/tickets/phase-7.md`, 278/278 test lulus), **dan 🟢 Phase 8 SELESAI PENUH** (6/6 ticket, 5/5 checkpoint — lihat `docs/tickets/phase-8.md`, 323/323 test lulus). Phase 3 scope-nya sengaja dipersempit ke "Exercise Engine core" (bukan roadmap-Fase-4 penuh); Phase 4 scope-nya sengaja dipersempit ke roadmap-Fase-3 §3.1/3.6/3.2/3.5 (bukan semua §3.1-3.9); Phase 6 murni Speaking/AI Tutor (ADR-0010); Phase 7 murni roadmap-Fase-5 §5.1+§5.2 (ADR-0011, bukan §5.3/Proctoring); Phase 8 murni roadmap-Fase-6 §6.1-6.8 (bukan §6.9-16 model bisnis) — detail alasan masing-masing ada di ticket file-nya sendiri bagian "Keputusan scope", tidak diulang di sini. **Dengan Phase 5 selesai, seluruh roadmap-Fase-3 (§3.1-§3.9) resmi ditutup** — kecuali §3.3/§3.7 (butuh data historis yang belum terkumpul) dan §3.8/§3.9 (butuh ADR baru + fase marketplace/tutor lain yang belum dibangun), yang di-defer eksplisit sejak `phase-5.md` ditulis, bukan didiamkan.

**P5-001** (Retrieval Variation §3.4, `titian-backend-bun`, 2026-09-02): `frss_repository.findSuggestedQuestionIds` sekarang menghindari saran tipe soal yang sama dengan review terakhir user untuk concept itu (kalau concept punya >1 tipe soal terdaftar) — `findLastAnsweredQuestionType` baru, mirror join pattern `mastery_repository.findEventsForConcept`. Concept single-type atau belum ada riwayat: behavior lama (`ORDER BY id`) tetap identik, nol regresi. **1 keterbatasan test-harness nyata ditemukan**: Postgres's `now()` dibekukan di awal transaksi, bukan per-statement — 2 panggilan `/check` berurutan di dalam 1 `withTx` dapat `created_at` identik (bukan masalah di produksi, di sana 2 request nyata = 2 transaksi terpisah) — diperbaiki dengan insert `learning_events` langsung + `createdAt` eksplisit buat test ordering itu. 5 test baru. **P5-002** (`titian-backend-bun`, 2026-09-02, menutup gap P4-003): `learning_queue_service.getLearningQueue` sekarang mengecek `concept_repository.findPrerequisites` (1 level) untuk tiap item `weak`/`critical` — prerequisite yang mastery-nya di bawah `weaknessScoreThreshold` (atau belum ada data sama sekali, dicek lewat `masteryRepository.find` bukan `findWeak` karena butuh bedakan "belum ada data" dari "confidence rendah") dinaikkan/ditambahkan sebagai `critical`, item asalnya dapat field baru `blocked_by_concept_id`. Snapshot kandidat diambil sebelum pass ini memodifikasi map, jadi prerequisite yang baru ditambahkan tidak pernah ikut di-walk sendiri — menegakkan batas 1-level tanpa guard eksplisit. Concept tanpa prerequisite edge: behavior identik P4-002, dikonfirmasi lewat `learning-queue.test.ts` yang tidak diubah sama sekali tetap lulus. 4 test baru. **P5-003 done** (`titian-backend-bun`, 2026-09-02): route-coverage audit 72 route — 0 gap (Phase 5 tidak menambah endpoint baru). Checkpoint end-to-end baru (`tests/phase5-checkpoint.test.ts`) menyatukan P5-001+P5-002 dalam 1 skenario nyata lewat request flow asli (bukan panggilan repository langsung): "Verb To Be" (prerequisite) + "Present Simple" (dependent) — Present Simple dijawab salah semua tanpa pernah menyentuh Verb To Be → `/learning-queue` menunjukkan Verb To Be `critical` + Present Simple dapat `blocked_by_concept_id`; lalu Verb To Be dijawab (mcq) → saran soal berikutnya untuk Verb To Be jadi fill_blank, bukan mcq lagi. Semua 4 poin checkpoint keluar Phase 5 tercentang. Total sekarang **260/260 test lulus**.

**Catatan tooling baru (2026-09-02, permintaan eksplisit user "maksimalkan fitur Bun"):** verifikasi browser FE sekarang pakai `Bun.WebView` (Bun 1.4 native, real Chrome via CDP) bukan lagi paket `playwright` eksternal di scratchpad — lihat detail di `docs/tickets/phase-4.md`'s P4-004/P4-005 "Catatan implementasi". Pola ini yang dipakai untuk verifikasi FE ke depannya.

**🟢 Phase 6 — Speaking / AI Tutor: SELESAI, semua 5 ticket done, semua 5 checkpoint keluar tercentang.** Phase 5 ditulis (`docs/tickets/phase-5.md`, 3 ticket) tapi langsung disalip Phase 6 sebelum sempat dikerjakan. User membalas dengan keputusan provider eksplisit untuk Speaking/AI Tutor (opsi yang barusan ditunda di riset Phase 5) — "stt pakai whisper, tts pakai kokoro, semua pakai openrouter" — plus instruksi "kerjakan". **`docs/tickets/phase-6.md` ditulis (5 ticket: P6-001 AIProvider audio capabilities, P6-002 speaking attempt+evaluasi, P6-003 AI Tutor conversation, P6-004 FE recording UI, P6-005 test suite+checkpoint), ADR-0010 ditulis** (keputusan provider, dites live: `whisper-large-v3` cacat—transkrip terpotong diam-diam di clip nyata—dipakai `whisper-1` sebagai default; `hexgrad/kokoro-82m` dikonfirmasi jalan). **P6-001 done** (`titian-backend-bun`, 2026-09-02): `AIProvider` interface dapat `transcribe(audio, mimeType, model)`/`synthesizeSpeech(text, voice, model)`, `DeepSeekProvider` implementasi asli lewat OpenRouter (`FormData`/`Blob` native Bun buat multipart STT), `FakeAIProvider` dapat `withTranscription`/`withTranscriptionFailure` (independen dari `generate()`'s hasil). Diverifikasi lewat `DeepSeekProvider`-nya sendiri ke provider asli (bukan cuma curl): TTS→STT round-trip lengkap, transkrip cocok. **P6-002 done** (`titian-backend-bun`, 2026-09-02): `POST /attempts/{id}/submit` sekarang bercabang ke speaking (`ai_speaking_evaluation_service.ts` baru, mirror `ai_writing_evaluation_service.ts`) berdasar `lesson.type` asli (bukan asumsi "semua lesson attempt = writing" seperti sebelumnya). **1 gap arsitektur nyata ditemukan**: `AssetStorage` cuma punya `signedUrl()` (URL yang di-fetch provider eksternal, pola OCR) — STT butuh bytes ASLI server-side, jadi ditambah `AssetStorage.get(key)` baru di `R2Storage`+`InMemoryStorage`. Diverifikasi penuh lewat provider asli (Kokoro→upload→lesson speaking asli→submit lewat server asli): 5 skor terpisah masuk akal (grammar 95/vocabulary 80/fluency 90/naturalness 85/pronunciation 85). **Temuan menarik dicatat, bukan bug**: Whisper otomatis mengoreksi grammar kalimat uji yang sengaja salah sebelum evaluasi teks sempat melihatnya — keterbatasan pengujian pakai audio TTS yang terlalu jernih, bukan kegagalan evaluasi. **P6-003 done** (`titian-backend-bun`, 2026-09-02): block type baru `speaking_prompt` (`data: {text}`, pola sama `text`/`example`) — ALM parser tidak perlu diubah sama sekali (sudah generic terhadap directive type baru). `GET /lessons/{id}/speaking-prompt-audio` balikin audio bytes mentah langsung (bukan JSON/upload-asset-dulu). "Correction": `feedback[0]` di-spotlight jadi field baru kalau grammar/naturalness di bawah threshold (konstanta lokal, bukan config — presentasi, bukan algoritma skor), reuse feedback yang sudah ada, tidak ada AI call ke-2. Diverifikasi lewat provider asli 2x skenario: grammar sedang (70/75, di atas threshold) → `correction: null` meski feedback tetap ada (kalibrasi masuk akal, bukan bug); grammar rusak (15/15) → `correction` terisi kalimat relevan. **P6-004 done** (`titian-web`, 2026-09-02): `SpeakingRecorder` baru (browser `MediaRecorder`/`getUserMedia`, state machine idle→requesting→recording→recorded), `SpeakingAttempt` (mirror `WritingAttempt`, upload lewat `driveApi.uploadAsset` yang sudah ada→create attempt→submit), `AiTutorPrompt` (tombol "Dengarkan", TTS hanya dipanggil saat diklik, 404 lesson tanpa mode AI Tutor diam-diam sembunyikan tombolnya). Halaman lesson viewer diperluas render keduanya untuk `type==='speaking'`. Client baru `authedBlobRequest` (endpoint pertama yang balikin bytes mentah bukan JSON). **Diverifikasi penuh lewat `Bun.WebView` end-to-end**: `getUserMedia`/`MediaRecorder` di-mock lewat CDP (headless tidak bisa akses mic asli) TAPI diberi makan audio Kokoro ASLI lewat kontrak `MediaRecorder` yang sama persis — kode komponen sendiri jalan 100% asli. Alur penuh sukses: dengarkan prompt asli → rekam → kirim → upload+attempt+submit+evaluasi AI asli → 5 skor + transkrip + callout KOREKSI beneran muncul dengan saran relevan. 12+7 test baru dari P6-002/003, total sekarang **246/246 test lulus** (P6-004 murni FE, tidak nambah test backend). **P6-005 done** (`titian-backend-bun` + `titian-web`, 2026-09-02): route-coverage audit 72 route — 0 gap. Checkpoint test baru (`tests/phase6-checkpoint.test.ts`, 4 test). **1 gap checkpoint nyata ditemukan+ditutup saat menutup fase** (pola sama P4-005): checkpoint #3 minta "Try Again" tapi `SpeakingAttempt` (P6-004) belum punya jalan balik ke recorder setelah hasil muncul — ditambah tombol "Coba lagi" yang reset `result`/`submitMutation`, backend sudah mendukung tanpa perubahan (dedup cuma menolak attempt yang masih `in_progress`). Diverifikasi lewat `Bun.WebView` 2 attempt berurutan lewat provider asli (skor 84 kedua kalinya, attempt row baru dikonfirmasi, latensi nyata ~14 detik dan ~31 detik — variabilitas normal provider AI, bukan bug). Total sekarang **250/250 test lulus**. Semua 5 poin checkpoint diverifikasi ulang (bukan diasumsikan dari ticket sebelumnya) — detail lengkap di `docs/tickets/phase-6.md`'s "Catatan implementasi" P6-005. Phase 5 **tetap valid**, dikerjakan sekarang.

**🟢 Phase 7 — Assessment / Exam Engine (roadmap-Fase-5, §5.1+§5.2): SELESAI, semua 4 ticket done, semua 4 checkpoint keluar tercentang.** Setelah Phase 1-6 SELESAI PENUH, roadmap artifact TITIAN diperbarui dan user diberi 4 opsi eksplisit lewat `AskUserQuestion` — **user pilih Assessment/Exam Engine**. Riset ulang sumber (`ALR_Phase_Detail_Breakdown.md` §2.11) menemukan 1 keputusan arsitektur yang wajib diputuskan eksplisit dulu (bobot skor Level Assessment) — ditanya langsung, **user pilih Knowledge 40% / Communication 60%** (opsi rekomendasi dokumen sumber). **ADR-0011 ditulis** (`agent/docs/adr/0011-level-assessment-scoring-weights.md`) mencatat keputusan itu + 2 temuan riset nyata: (1) `exam_sessions`/`proctoring_*` sudah ada di schema sejak ADR-0001 tapi nol kode pernah menyentuhnya (pola sama `concept_prerequisites` sebelum P4-003); (2) `assessment_service.submitAttempt` (jalur assessment) hari ini diam-diam mengabaikan soal writing/speaking sepenuhnya dari skor — gap nyata yang bikin Communication-weighted scoring butuh evaluasi AI per-soal (bukan per-attempt seperti jalur lesson P3-004/P6-002), pola baru. `docs/tickets/phase-7.md` ditulis (4 ticket: P7-001 Exam Session Runtime §5.1, P7-002 question skill-category tagging, P7-003 composite scoring §5.2/ADR-0011, P7-004 test suite+checkpoint) — scope sengaja dibatasi §5.1+§5.2 saja, §5.3 (IELTS/TOEFL/PTE) dan Proctoring (roadmap-Fase-9) eksplisit di luar scope (lihat ticket file-nya bagian "Keputusan scope"). **P7-001 done** (`titian-backend-bun`, 2026-09-02): mengaktifkan `exam_sessions` (dead sejak ADR-0001) — `exam_repository.ts`/`exam_service.ts` baru, `POST /assessments/{id}/exam-sessions` (reuse `assessment_service.createAttempt` yang sudah ada, bukan duplikasi) + `GET /exam-sessions/{id}`. Korelasi attempt↔session tanpa FK baru, cuma `(assessment_id, user_id, status='in_progress')` — aman karena `createAttempt` sendiri sudah menolak in-progress kedua. `duration_minutes` dibaca dari `assessments.config` jsonb yang sudah ada, tidak butuh migrasi. Submit lewat deadline tetap diterima+digrading normal (auto-submit-on-timeout), cuma `exam_sessions.status` jadi `timed_out` — ditambahkan murni di level HANDLER (`assessment_handler.postSubmit`), nol perubahan ke `assessment_service.ts` (pola sama alasan P3-004 dulu compose di handler). 7 test baru, route-coverage 74 route/0 gap. **P7-002 done** (`titian-backend-bun`, 2026-09-02): `questions.skill_category` baru (nullable, `CHECK IN (vocabulary/grammar/reading/listening/writing/speaking/pronunciation)`, migrasi additive pola persis `cefr_tag` P2-014) — `POST /question-banks/{id}/questions` terima field opsional ini, `GET /questions/{id}` + `GET /question-banks/{id}/questions` mengembalikannya. Validasi taruh langsung di `question_service.createQuestion` (konstanta lokal, bukan `question_schema.ts` yang khusus validasi bentuk-jawaban per tipe soal). 1 caller lain ketauan lewat `tsc` (bukan grep manual): `ai_content_service.ts`'s AI question generation — diteruskan `skillCategory: null` eksplisit (di luar scope AI pipeline). 4 test baru. **P7-003 done** (`titian-backend-bun`, 2026-09-02, ADR-0011): inti Phase 7. Service baru `level_assessment_scoring_service.ts`, didispatch dari `assessment_handler.postSubmit` (HANDLER-level, bukan cabang di dalam `assessment_service.submitAttempt` — `assessment_service.ts` nol baris berubah, jaminan regresi terkuat buat `unit_test`/`mock_exam`). Soal Knowledge (`skill_category` vocabulary/grammar/reading/listening) dihitung sama persis seperti sebelumnya; soal Communication (writing/speaking/pronunciation) memicu evaluasi AI PER-SOAL (bukan per-attempt) — `evaluations` dapat kolom baru `question_id` (nullable, NULL buat evaluasi lesson-attempt P3-004/P6-002 yang tidak berubah). `attempts.score = Knowledge*0.4 + Communication*0.6`, fallback ke 100% bucket yang ada kalau salah satu kosong. Soal tanpa `skill_category` masuk `unscored_question_ids`, dikeluarkan dari kedua bucket. **Refactor signifikan**: `ai_writing_evaluation_service.ts`/`ai_speaking_evaluation_service.ts` masing-masing dapat fungsi baru diekspor (`runWritingEvaluation`/`runSpeakingEvaluation`) — AI-call+parse+persist core diekstrak dari `submitWritingAttempt`/`submitSpeakingAttempt` (yang sekarang jadi wrapper tipis), supaya bisa dipanggil berkali-kali (1x per soal Communication) tanpa duplikasi logic. **1 bug regresi nyata ketemu+diperbaiki sebelum commit** — ketauan dari test LAMA (`ai-speaking-evaluation.test.ts`), bukan test baru: refactor pertama bikin transcript hilang kalau evaluasi gagal SETELAH transcribe sukses (harusnya tetap dikembalikan) — diperbaiki (`transcript`/`evaluation` jadi 2 field independen, bukan 1 union), 12/12 test lama lulus lagi. Kebijakan kegagalan Level Assessment sengaja beda dari jalur lesson: 1 asset audio rusak di antara banyak soal Communication cuma dikeluarkan dari skor, TIDAK menolak seluruh submission. 5 test baru. **P7-004 done** (`titian-backend-bun`, 2026-09-02): route-coverage audit 74 route — 0 gap. Checkpoint end-to-end baru (`tests/phase7-checkpoint.test.ts`, 2 test) — blueprint persis diminta ticket (2 grammar + 1 reading + 1 listening Knowledge, 1 writing + 1 speaking Communication), lewat `POST /assessments/{id}/exam-sessions` → `POST /attempts/{id}/submit` asli, skor dicocokkan ULANG dari row `evaluations` yang benar-benar tersimpan (bukan ditebak). **Diverifikasi lewat provider AI ASLI** (checkpoint poin 3 eksplisit minta ini, bukan cuma `FakeAIProvider`) lewat skrip sekali-pakai (scratchpad, tidak di-commit): skor komposit **70.8** = `100*0.4 + 51.3*0.6` cocok PERSIS formula ADR-0011, 3 feedback grammar dari AI semuanya benar. **1 temuan menarik dicatat, bukan bug**: percobaan pertama dapat respons AI kosong — evaluasi gagal graceful sesuai desain (attempt tetap submitted, fallback ke skor Knowledge-only), bukti nyata jalur kegagalan bekerja dengan provider asli juga. Data demo dihapus total, dikonfirmasi lewat query ulang (0 baris tersisa). Total sekarang **278/278 test lulus**. Semua 4 poin checkpoint keluar Phase 7 diverifikasi ulang (bukan diasumsikan dari ticket sebelumnya) — detail lengkap di `docs/tickets/phase-7.md`'s "Catatan implementasi" P7-004.

**🟢 Phase 8 — Gamification (roadmap-Fase-6, §6.1-6.8 saja): SELESAI, semua 6 ticket done, semua 5 checkpoint keluar tercentang.** Setelah Phase 7 ditutup, user diminta lanjut ke roadmap artifact TITIAN — artifact-nya sendiri sudah terhapus (dikonfirmasi via `Artifact` read: "not found"), tapi user pilih eksplisit lewat `AskUserQuestion`: **"lanjut ke kandidat berikutnya di daftar itu"**, yaitu Gamification & Economy. Tidak ada gerbang ADR eksplisit dari sumber (beda dari Phase 7's bobot skor) — langsung riset + tulis ticket file, pola sama Phase 4. `docs/tickets/phase-8.md` ditulis (6 ticket: P8-001 XP ledger+hook, P8-002 streak+freeze, P8-003 achievement 3 kategori, P8-004 personal mission harian, P8-005 leaderboard weekly+personal+league, P8-006 test suite+checkpoint) — scope sengaja dibatasi §6.1-6.8 (mekanik engagement inti), **§6.9-6.16 (subscription tier, Diamond framing UI, iklan, wallet tutor marketplace, cron expiry) eksplisit di luar scope** — masing-masing alasannya sudah dirinci di ticket file bagian "Keputusan scope" (butuh payment/SDK iklan/entitas tutor yang belum ada, bukan keputusan teknis sesi ini). **1 prinsip keras ditegakkan eksplisit**: XP/streak/achievement/league tidak pernah masuk formula `mastery.compute`/`frss.apply` (§6.1's "3 metrik tidak boleh dicampur") — ditambahkan sebagai guard baru di bagian "JANGAN lakukan ini tanpa ADR baru" di atas. **1 temuan nyata dari riset**: jalur lesson-attempt writing/speaking (P3-004/P6-002) TIDAK PERNAH menulis `learning_events` sama sekali (beda dari checkAnswer/submitAttempt/level_assessment) — XP-awarding P8-001 tidak bisa cukup hook ke `learning_events`, harus hook eksplisit ke SEMUA 5 titik submit.

**P8-001 done** (`titian-backend-bun`, 2026-09-02): `xp_events` (ledger, `reference` UNIQUE nullable) + `user_xp` (cache) baru, mirror persis `credits`/`transactions` ADR-0005. `xp_repository.award` (bukan `xp_service`) pegang transaksi DB-nya — `tsc` menolak meneruskan `tx` (`PgTransaction`) ke fungsi lain yang menerima `Db` biasa, jadi ikutin pola `economy_repository.charge` (transaksi utuh 1 fungsi repository). Idempotency murni dari `ON CONFLICT DO NOTHING` di `reference` UNIQUE — NULL tidak pernah collide (semantik SQL standar), jadi `/check` (praktik berulang legitimate) sengaja tidak dikasih `reference`, sementara 4 jalur submit lain (assessment/level_assessment/writing/speaking, semua dijaga status "sekali submit" oleh `loadSubmittableAttempt`) pakai `reference = "attempt:{attemptId}"`. XP untuk writing/speaking diberikan segera setelah submission sukses, TIDAK digantung keberhasilan evaluasi AI (dibuktikan test: fake AI response gagal parse, XP tetap masuk) — prinsip "submission selalu diterima" yang sudah ada di P3-004/P6-002. `question_service.checkAnswer` dapat field baru `skillCategory` di return-nya (bukan query kedua). `GET /me/xp` baru. 8 test baru, route-coverage 75 route/0 gap. **P8-002 done** (`titian-backend-bun`, 2026-09-02): `user_streaks` baru (`last_active_date` sebagai `text` "YYYY-MM-DD", bukan tipe `date` native — cuma butuh perbandingan string lurus, `today` selalu parameter eksplisit ke `recordActivity` bukan `new Date()` internal, jadi test bisa simulasikan hari manapun tanpa manipulasi DB). Non-punitive sesuai §6.5: gap 1 hari dengan freeze tersedia → streak TETAP (bukan naik/reset), freeze berkurang; tanpa freeze → reset ke 1 (bukan 0). **1 bug nyata dicegah sebelum sempat jadi bug**: bonus freeze 7-hari awalnya cuma cek `newCurrentStreak % 7 === 0` — kalau dipakai pas freeze-consumption (streak TETAP di kelipatan 7), itu salah trigger bonus kedua; diperbaiki dengan syarat tambahan `streakAdvanced` (bonus cuma lewat jalur hari-berurutan). Hook di titik yang sama persis dengan XP (P8-001) — 2 panggilan berurutan per handler, bukan digabung 1 fungsi (streak sudah idempotent per-hari sendiri, tidak butuh `reference` kayak XP). `GET /me/streak` baru. 9 test baru, route-coverage 76 route/0 gap. **P8-003 done** (`titian-backend-bun`, 2026-09-02): `achievements` (katalog, 10 baris: 3 Learning + 4 Skill + 3 Improvement) + `user_achievements` + `user_concept_mastery_baselines` baru. `xp_events` dapat kolom baru `skill_category` (revisit kecil ke P8-001 — `user_xp` cuma nyimpen total gabungan, Skill Achievement butuh SUM per kategori). Improvement Achievement butuh "baseline" karena `masteries` tetap current-value-only (batasan lama sejak riset Phase 4) — `user_concept_mastery_baselines` nyimpen skor PERTAMA yang pernah diobservasi per (user, concept), direkam sekali, delta dihitung dari situ, bukan jendela bergerak. Improvement diimplementasi sebagai 3 TINGKAT dari 1 mekanisme (+10/+20/+40), bukan 3 kriteria beda seperti 3 nama contoh §6.7 (yang sebenarnya butuh ranking relatif antar-user atau nyambung ke streak-freeze — didokumentasikan eksplisit sebagai simplifikasi sadar). `checkAndAward` terima `questionIds` (bukan `conceptIds` langsung) — `assessment_handler` sudah punya `answers.keys()` gratis, `achievement_service` sendiri resolve concept id-nya. `GET /me/achievements` TIDAK balikin progress-ke-belum-didapat (AC sendiri menandai opsional, butuh query tambahan per achievement per request). 11 test baru, route-coverage 77 route/0 gap. **P8-004 done** (`titian-backend-bun`, 2026-09-02): `user_daily_missions` baru (`mission_date` text "YYYY-MM-DD", pola sama `user_streaks` P8-002). 4 skill dilacak — `vocabulary`/`grammar`/`listening`/`speaking` — PERSIS sesuai contoh §6.8 sendiri, sengaja BUKAN 7 `skill_category` penuh P7-002 (`reading`/`writing`/`pronunciation` tidak dilacak) — dites eksplisit cabang lesson writing tidak menambah progress meski `recordProgress` tetap dipanggil di situ (konsisten "panggil di semua 5 titik, biarkan service no-op"). Semua 4 target tercapai → +80 XP otomatis sekali (`reference` UNIQUE `xp_events` P8-001 yang jaga idempotency, bukan transaksi DB baru — didokumentasikan eksplisit kenapa cukup). `GET /me/daily-mission` baru. 7 test baru, route-coverage 78 route/0 gap. **P8-005 done** (`titian-backend-bun`, 2026-09-02): **nol migrasi baru** — leaderboard & league murni agregasi on-read atas `xp_events`/`user_streaks`/`masteries` yang sudah ada, konsisten AC-nya sendiri. `GET /leaderboard/weekly` — `xp_events` 7 hari terakhir per user, `me` (standing si pemanggil) dihitung dari HASIL AGREGASI PENUH yang sama (bukan query kedua), tetap benar meski si pemanggil di luar `limit` yang diminta (dites eksplisit). Percentile = `(totalUsers-rank)/(totalUsers-1)*100` — penyebut TIDAK termasuk diri sendiri. `GET /me/league` — tier dari formula gabungan (streak capped 30 hari + rata-rata mastery confident-only + JUMLAH/COUNT `xp_events` 7 hari, sengaja COUNT bukan SUM biar tidak jadi proxy XP lewat pintu belakang), masing-masing di-rescale 0-100 lalu dirata-rata. Dites eksplisit: 1 event XP raksasa (streak=0, mastery=0) TIDAK bisa dorong tier ke Diamond/Master — bukti formula tahan "dibeli" pakai XP doang, sesuai §6.4's syarat eksplisit. 9 test baru, route-coverage 80 route/0 gap. **P8-006 done** (`titian-backend-bun`, 2026-09-02): route-coverage audit 80 route — 0 gap. Checkpoint end-to-end baru (`tests/phase8-checkpoint.test.ts`, 1 test, 14 assertion) — 1 user jawab 4 soal lintas skill (vocabulary/grammar/listening/speaking) via `/check` + 1 lesson writing via `/attempts/{id}/submit`, semua lewat request flow asli: `user_xp.total = 60` (cocok tabel XP), `user_streaks.current_streak = 1`, achievement `first_lesson` ter-award (dicek via `GET /me/achievements` asli), `user_daily_missions` progress `{vocabulary:1,grammar:1,listening:1,speaking:1}` dengan `reward_claimed: false` (target vocabulary 5 sengaja belum tercapai, membuktikan mission TIDAK prematur complete). Hari ke-2 (`user_streaks.last_active_date` dimundurkan 1 hari langsung di DB, pola sama P5-001/P7-001) → `current_streak = 2`, muncul di `GET /leaderboard/weekly`. **Audit isolasi mastery/FRSS via grep langsung** (bukan janji desain): `masteryService.recomputeForConcept`/`frssService.recordReview` HANYA dipanggil dari `assessment_service.ts`/`question_service.ts`/`level_assessment_scoring_service.ts` — nol dari 5 service gamification manapun; `achievement_service`/`league_service` baca `masteries` murni READ, nol panggilan `mastery.compute`/nol tulis balik. Total sekarang **323/323 test lulus**. Semua 5 poin checkpoint keluar Phase 8 diverifikasi ulang (bukan diasumsikan dari ticket sebelumnya) — detail lengkap di `docs/tickets/phase-8.md`'s "Catatan implementasi" P8-006.

**Catatan link artifact roadmap (2026-09-02, dikoreksi):** link asli (`.../artifact/48ee7531-...`) TERNYATA MASIH HIDUP — 2 percobaan `Artifact` read sebelumnya balikin "not found" (kemungkinan transient/caching platform, bukan terhapus permanen seperti yang dicatat sebelumnya di sini). Link kedua yang sempat dipublish (`.../artifact/a3b4e3ac-...`) sekarang JUSTRU yang mati. **Link yang benar dan aktif dipakai: `48ee7531`** (link ASLI/pertama) — sudah diupdate 2026-09-02 mencakup Phase 7/8/9 + rebrand "Titian Asa". Kalau user minta update lagi, pakai `48ee7531`, cek dulu dengan `Artifact` read sebelum asumsi mati — jangan langsung publish baru kalau read pertama gagal, retry dulu.

**Phase 9 — Organization / LMS / Marketplace (roadmap-Fase-8): ticket breakdown ditulis (2026-09-02).** User update roadmap artifact lalu eksplisit pilih Organization/LMS/Marketplace, scope disebut sendiri: "RBAC penuh, class management (cohort, enrollment, attendance, sertifikat), marketplace tutor split revenue 30/70, pembayaran QRIS." **Fase pertama di seluruh proyek yang menyentuh uang sungguhan** — 1 keputusan eksplisit ditanyakan ke user dulu (pola sama Phase 7): tidak ada kredensial payment gateway di `.env`, bagaimana scope-nya? **User pilih: bangun abstraksi+alur lengkap, provider QRIS di-stub dulu** (pola persis `AIProvider`/`FakeAIProvider`, tidak charge uang sungguhan sampai ada kredensial nyata). `docs/tickets/phase-9.md` ditulis (7 ticket: P9-001 tutor+RBAC surface, P9-002 learning products private/group, P9-003 cohort+enrollment, P9-004 attendance manual, P9-005 certificates, P9-006 payment stub+wallet 30/70+cancellation, P9-007 test suite+checkpoint) — scope dipersempit dari §8.1-8.15 penuh (assignment/gradebook/reputation/student-diagnosis/marketplace-8-kategori-penuh/payment-asli semua di luar scope, alasan masing-masing di ticket file "Keputusan scope"). **2 temuan nyata dari riset**: (1) `transactions.type` sudah punya `payout_earned`/`payout_withdrawn` sejak ADR-0001 tapi nol kode pernah menulisnya — pola sama `exam_sessions`/`concept_prerequisites` sebelum diaktifkan; (2) unit `transactions.amount` ambigu antar-type (credit buat ekonomi AI, tapi Rupiah mentah buat payout tutor) — didokumentasikan eksplisit sebagai keputusan ticket ini, bukan silent reinterpretation. Tidak ada ADR baru yang dibutuhkan — ADR-0005 sudah eksplisit mengunci desain wallet tutor reuse `transactions`, ADR-0006 sudah punya role `teacher`/`tutor`/`org_owner` lengkap.

**P9-001 done** (`titian-backend-bun`, 2026-09-02): `tutor_profiles` baru (`user_id` PK+FK, `organization_id` FK, `bio`, `specializations` jsonb). `permissions.ts` dapat 1 case baru: `tutor_profile:create` → `platform_admin`/`org_owner`/`academic_director` (role list sama `organization_members:view`). `PATCH /tutors/me` sengaja TIDAK lewat role matrix — ownership check murni (`ctx.userId` = profile owner), 404 kalau caller belum jadi tutor, bukan 403. `tutor_repository.assign` gabung insert role `tutor` (ADR-0006, ada sejak Phase 0, baru dipakai di sini) + insert profile dalam 1 `db.transaction()`, dua-duanya `onConflictDoNothing`, fallback SELECT kalau profile insert no-op — assign yang sama 2x terbukti idempotent (tes eksplisit). **1 bug PATCH-semantics ketemu & diperbaiki pas nulis kode** (bukan pas testing): draft pertama default field yang di-omit ke `""`/`[]` sebelum masuk service, yang diam-diam bakal ngosongin `specializations` yang sudah ada kalau caller cuma kirim `{bio: "x"}`. Fix: service terima `string | undefined` dan fallback ke row existing yang sudah di-fetch, bukan default kosong — ada test regresi eksplisit. Insight desain: tutor marketplace-wide vs sekolah-spesifik tidak butuh konsep baru — tutor di-assign di org bertipe `platform` = marketplace-wide, di org bertipe `school` = khusus sekolah itu, model org yang sama. `POST/GET /organizations/{id}/tutors` + `PATCH /tutors/me` baru, `agent/docs/api-contract.md` diupdate. 8 test baru (331/331 total, dari 323), `bunx tsc --noEmit` bersih, 3 route baru dikonfirmasi kepakai lewat audit manual (tidak ada script coverage terpisah di repo ini). Detail lengkap di `docs/tickets/phase-9.md`'s "Catatan implementasi" P9-001.

**Ditemukan setelah P9-001 (2026-09-02)**, saat user tanya beda `teacher`/`tutor`/`org_owner`/`academic_director` dan role default student: **alur registrasi asli (`auth_service.ts`) tidak pernah insert row `user_organization_roles`** — user asli manapun (bukan test-seed) tidak pernah bisa dapat role apa pun sejak Phase 0, jadi `AuthContext.organizationId`/`role` permanen `null` dan endpoint apa pun yang butuh role tidak bisa diakses. Gap lintas-fase, bukan cuma marketplace. **User pilih (via `AskUserQuestion`): auto-join 1 org platform singleton sebagai `student` saat registrasi** (bukan wajib invite eksplisit dulu) — ditambal sebagai **ticket baru P9-002** (disisipkan, ticket lama P9-002-007 di-renumber jadi P9-003-008, lihat `docs/tickets/phase-9.md`). **`teacher` vs `tutor` TIDAK digabung/dipisah** — matrix ADR-0006 sudah menganggap keduanya setara persis sejak awal (1 kolom gabungan), `tutor_profiles` dipakai bersama, dibedakan cuma di UI lewat `organizations.type` (`school`→"Guru", `platform`/`tutor_org`→"Tutor") — didokumentasikan sebagai addendum ADR-0006 (2026-09-02), bukan ADR baru (tidak ada desain yang berubah, cuma diklarifikasi).

**P9-002 done** (`titian-backend-bun`, 2026-09-02): `organization_repository.ts` dapat `findOrCreatePlatformOrg` (org singleton slug tetap `alr-platform`, idempotent — pola sama `tutor_repository.assign`: insert+`onConflictDoNothing`+fallback SELECT) + `assignDefaultStudentRole`. `auth_service.googleCallback` dicek di SETIAP login (bukan cuma cabang insert-user-baru): `findAllRoles` kosong → assign default — diperluas dari AC tertulis semula ("cuma user baru") karena itu strict superset yang otomatis self-heal user yang kena gagal-parsial di percobaan sebelumnya (tidak bisa dibungkus 1 `db.transaction()` bareng insert user karena kendala tipe `Db`/`PgTransaction` yang sama seperti P8-001), tanpa butuh retry/error-handling terpisah. 5 test baru di `tests/auth.test.ts` (336/336 total, dari 331), `bunx tsc --noEmit` bersih. Tidak ada endpoint baru, jadi tidak ada audit route-coverage yang perlu dijalankan ulang. Detail lengkap: `docs/tickets/phase-9.md`'s "Catatan implementasi" P9-002.

**P9-003 done** (`titian-backend-bun`, 2026-09-02): `learning_products` baru (`tutor_id` FK `tutor_profiles`, `type` CHECK `private`/`group`, `price_idr` bigint — Rupiah mentah bukan credit, `capacity` nullable, `status` `draft`/`published`/`archived`). Validasi `type`/harga/`capacity` di 2 lapis: service (422 rapi) + CHECK constraint DB gabungan `(type='private' and capacity is null) or (type='group' and capacity>0)` sebagai defense-in-depth, dites eksplisit lewat raw insert yang sengaja melanggar constraint. `learning_product:create` role matrix: `platform_admin`/`tutor`, plain `requirePermission` (bukan org-scoped). `tutor_id` selalu dari `ctx.userId`, tidak pernah dari body. Draft produk milik orang lain → 404 (bukan 403) di `GET /products/{id}`, pola "jangan bocorkan keberadaan" yang direncanakan dipakai lagi di P9-006 (certificate verify). `POST /tutors/me/products` + `GET /tutors/{id}/products` + `GET /products/{id}` baru, `agent/docs/api-contract.md` dapat section baru "## Marketplace" (bakal nampung P9-004 s/d P9-007 juga). 14 test baru (350/350 total, dari 336), `bunx tsc --noEmit` bersih, audit route-coverage 86 route/0 gap. Detail lengkap: `docs/tickets/phase-9.md`'s "Catatan implementasi" P9-003.

**P9-004 done** (`titian-backend-bun`, 2026-09-02): `cohorts` (`product_id` FK) + `enrollments` (`cohort_id`/`student_id` FK, UNIQUE pair, `status` `pending`/`active`/`completed`/`cancelled`) baru. Auth `POST /products/{id}/cohorts` TERNYATA hybrid (tutor pemilik ATAU org admin dari org tutor itu) — butuh row-context yang tidak muat di `permissions.ts`'s matrix role-only, jadi diimplementasi sebagai `cohort_service.canManageCohorts` (boolean, dipakai ulang di `GET /cohorts/{id}/students` juga), bukan lewat matrix — pola sama `PATCH /tutors/me`. `POST /cohorts/{id}/enrollments` self-enroll only (`student_id` dari `ctx.userId`), idempotent kalau siswa yang sama enroll ulang (dicek SEBELUM gerbang kapasitas), tapi siswa BEDA yang enroll ke cohort yang sudah penuh → 422. **Keterbatasan diketahui dicatat eksplisit**: gerbang kapasitas (COUNT lalu INSERT) belum row-locked, race antar 2 siswa berbeda yang enroll bersamaan ke kursi terakhir belum ditangani — konsisten batasan MVP capacity-check lain di proyek ini (daily mission dst), bukan regresi baru. `GET /cohorts/{id}/students` py 3 cabang visibilitas dari 1 endpoint: tutor/org-admin lihat roster penuh, siswa terdaftar lihat baris sendiri saja, siapa pun tidak terkait → 403. 14 test baru (364/364 total, dari 350), `bunx tsc --noEmit` bersih, audit route-coverage 89 route/0 gap. Detail lengkap: `docs/tickets/phase-9.md`'s "Catatan implementasi" P9-004.

**P9-005 done** (`titian-backend-bun`, 2026-09-02): `attendance_records` baru (`cohort_id`/`student_id` FK, `session_date` TEXT "YYYY-MM-DD" — bukan tipe `date` native, disengaja beda dari AC tertulis biar konsisten `user_streaks`/`user_daily_missions` P8-002/P8-004, `status` present/absent/late/excused, `method` CHECK 1-nilai `manual` — kolom disiapkan dari awal biar QR/Geolocation nanti additive, UNIQUE `(cohort_id, student_id, session_date)`). Upsert pakai `onConflictDoUpdate` pola persis `mastery_repository.upsert`. **Auth "tutor pemilik cohort" DIPERLUAS ke `cohort_service.canManageCohorts` P9-004** (tutor ATAU org admin ATAU platform_admin), bukan "cuma tutor" literal — disengaja biar konsisten `POST /products/{id}/cohorts`/`GET /cohorts/{id}/students`, dipakai ulang bukan logic baru. `GET /cohorts/{id}/attendance` 3 cabang visibilitas sama persis `GET /cohorts/{id}/students`. Setiap `student_id` yang ditandai WAJIB sudah enrolled di cohort itu (422 kalau tidak). 10 test baru (374/374 total, dari 364), `bunx tsc --noEmit` bersih, audit route-coverage 91 route/0 gap. Detail lengkap: `docs/tickets/phase-9.md`'s "Catatan implementasi" P9-005.

**P9-006 done** (`titian-backend-bun`, 2026-09-02): `certificates` baru (`enrollment_id` FK unique, `certificate_code` unique random, `completion_percent`, `estimated_cefr` nullable, `skill_summary` jsonb — snapshot, bukan live-query). **3 penyimpangan sadar dari prosa AC, dicatat eksplisit**: (1) attendance TIDAK jadi gerbang tambahan penerbitan — AC bullet-nya sendiri cuma minta `enrollment.status='completed'`, "Depends on" yang menyebut attendance itu cuma prosa longgar; (2) `skill_summary` snapshot mastery PLATFORM-WIDE dikelompokkan per `questions.skill_category` (fungsi baru `mastery_repository.findAverageScoreBySkillCategory`, lewat `question_concepts`), BUKAN per concept — `learning_products`/`cohorts` ternyata tidak punya FK ke `concepts`/`curricula` sama sekali, jadi "concept yang ter-link ke produk itu" tidak ada jalur schema-nya; (3) `completion_percent` selalu `100` (tidak ada tabel progress parsial). **Re-terbit sertifikat = 409 (`AppError.conflict`, factory baru), BUKAN idempotent** — beda sengaja dari pola "assign berulang = no-op" P9-001/P9-002/P9-004, karena `certificate_code`+`issued_at` unik per penerbitan bikin replay ambigu. Auth reuse `canManageCohorts` (pola sama P9-004/P9-005). `certificate_code` = 9 byte random base64url, pola sama `token_service.generateRefreshToken`. 8 test baru (382/382 total, dari 374), `bunx tsc --noEmit` bersih, audit route-coverage 93 route/0 gap. Detail lengkap: `docs/tickets/phase-9.md`'s "Catatan implementasi" P9-006.

**P9-007 done** (`titian-backend-bun`, 2026-09-02): `payment_provider.ts` baru (`PaymentProvider` interface + `StubQrisProvider`, pola persis `AIProvider`/`FakeAIProvider` ADR-0004 — TIDAK PERNAH hit network asli, dikonfirmasi grep eksplisit). `orders` baru (`enrollment_id` FK unique, `amount_idr` bigint Rupiah mentah, `status` pending/paid/failed/refunded). **`AppState` dapat field wajib baru `paymentProvider`** — nyentuh SEMUA 40 file test yang manggil `buildApp()` (ditambah `paymentProvider: testPaymentProvider()`, helper baru) + `src/index.ts` produksi (`StubQrisProvider` dipakai di produksi juga, bukan cuma test, karena memang belum ada provider asli — bukan lupa). `POST /payments/{payment_id}/webhook` **PUBLIK, tanpa auth sama sekali** — provider asli nanti otentikasi lewat signature-nya sendiri (belum ada buat diverifikasi), bukan bearer token user. Webhook **idempotent by design** (order bukan `pending` lagi → no-op) — mencegah bug finansial nyata (tutor dibayar 2x kalau webhook redeliver, bukan cuma soal kebersihan data kayak pola idempotent lain di fase ini. Split 30/70: tutor share `Math.round()`, fee platform = sisa (bukan dibulatkan independen) — selalu pas total. Kebijakan cancel §8.14: `cohorts.startsAt` kosong → default 100% refund (bukan 0%, tidak ada dasar menahan uang siswa kalau tutor belum commit jadwal); `orders.status` cuma jadi `refunded` kalau ADA uang benar-benar dikembalikan (tingkat <6 jam/0% tetap `paid`). `wallet_repository.ts` dokumentasikan ambiguitas unit `transactions.amount` (Rupiah mentah utk payout, credit utk ekonomi AI) langsung di kode, bukan cuma di doc. 11 test baru (393/393 total, dari 382), `bunx tsc --noEmit` bersih, audit route-coverage 97 route/0 gap. Detail lengkap: `docs/tickets/phase-9.md`'s "Catatan implementasi" P9-007.

**P9-008 done — Phase 9 SELESAI PENUH** (`titian-backend-bun`, 2026-09-02): route-coverage audit 98 route/0 gap. **1 gap nyata ditemukan pas nulis checkpoint scenario 1** (pola sama P4-005/P8-006's closing session menemukan lubang nyata) — checkpoint poin 1 sendiri mensyaratkan "lewat request asli, bukan potongan-potongan terpisah", tapi P9-006's `enrollment.status = 'completed'` precondition ternyata TIDAK PERNAH bisa dicapai lewat kode manapun (tidak ada scheduling infra buat auto-complete, tidak ada endpoint manual). Ditambal SEBAGAI BAGIAN closing ticket ini (bukan ticket terpisah): **`POST /cohorts/{id}/enrollments/{enrollment_id}/complete`** baru (`enrollment_service.completeEnrollment`, auth reuse `canManageCohorts`, idempotent, manual — pola sama attendance manual P9-005: tidak ada otomasi, manusia yang menandai). Checkpoint 2 skenario (`tests/phase9-checkpoint.test.ts`): (1) alur penuh tutor→produk→cohort→enrollment→payment→attendance→complete→certificate→verify lewat 7 request HTTP asli berurutan, wallet tutor terbukti PERSIS 70% dari harga (dicek row `transactions` DAN `GET /tutors/me/wallet`); (2) siswa ke-2 cancel <6 jam → no refund, wallet tutor TIDAK berkurang dan payout siswa pertama tidak tersentuh. **Semua 5 poin "Checkpoint keluar Phase 9" diverifikasi ulang** (bukan diasumsikan): alur end-to-end nyata, wallet 70% eksplisit, 3 tingkat cancellation (2 dari P9-007 dikonfirmasi ulang hijau + <6 jam didemonstrasikan ulang di checkpoint), `StubQrisProvider` 0 hasil grep `fetch/http/XMLHttpRequest/axios`, sertifikat terhubung mastery asli + verify publik tanpa `Authorization` header sama sekali. 399/399 test (dari 393 + 4 test `complete` + 2 test checkpoint). Detail lengkap: `docs/tickets/phase-9.md`'s "Catatan implementasi" P9-008 dan "Checkpoint keluar Phase 9".

**User eksplisit menentukan urutan 4 roadmap-Fase berikutnya (2026-09-02, di luar sesi tanya-jawab)**: roadmap-Fase 5.3 (IELTS/TOEFL/PTE) → roadmap-Fase 6.9-16 (Gamification model bisnis) → roadmap-Fase 8 sisa (Marketplace lanjutan) → roadmap-Fase 9 (Proctoring). Ini bukan hasil `AskUserQuestion` — user langsung menyebutkan urutan penuh, jadi TIDAK perlu tanya prioritas lagi di antara ke-4 ini; lanjut otomatis begitu 1 selesai, kecuali user bilang lain.

**Phase 10 done — roadmap-Fase 5.3 (IELTS/TOEFL/PTE), yang PERTAMA dari 4** (`titian-backend-bun`, 2026-09-02): sumbernya sendiri (§5.3) menunda SELURUH scope ini tanpa kasih urutan MVP-first (beda dari tiap §-lain) — jadi ticket-phase ini yang pertama kali motong scope sendiri, hasilnya: **IELTS SAJA** (bukan TOEFL/PTE — dicatat kandidat lanjutan terpisah), dan dari IELTS sendiri cuma 3 tipe soal + bukti reuse (bukan 14 tipe Reading/Writing/Speaking/Foundation penuh). **P10-001**: 3 tipe soal baru (`true_false_not_given`, `matching_headings`, `short_answer`) di `question_schema.ts`+`grading.ts` — bukti nyata `question_type` genuinely extensible (cuma 3 dari 14 tipe IELTS Reading eksplisit yang sebelumnya terdaftar: `mcq`/`fill_blank`/`matching`, bukan satu pun match literal). `short_answer` implementasi "Listening word-limit engine" §5.3 — `grading.isCorrect` dapat parameter ke-4 opsional (`data`) karena `max_words` hidup di kolom `data` soal, bukan `correct_answer`. Dites lewat KEDUA jalur (`/check` + submit formal) dari awal — pelajaran eksplisit dari gap `matching`/P3-001 yang cuma ke-cover 1 jalur dulu. **P10-002**: seed IELTS sebagai subject TERPISAH dari "English" (§2.12) + concept tree + practice test — **ZERO kode baru**, murni data, membuktikan P4-001's `mastery-breakdown` dan `assessments.config` reuse penuh. **Dead capability lain ketemu**: `assessments_type_check` sudah punya `'ielts'`/`'toefl'`/`'pte'` sejak ADR-0001, nol kode pernah pakai sampai sekarang. Seed persisten (`scripts/seed-ielts-reading.ts`, idempotent) beda dari fixture test (test butuh 3 soal/skill bukan 2, biar `confidence` tembus `masteryConfidenceThreshold` 0.6 — ketemu pas nulis test, didokumentasikan sebagai 2 artefak beda tujuan, bukan inkonsistensi). **P10-003**: checkpoint = `tests/ielts-reading.test.ts` yang SUDAH ditulis P10-002 (tidak diduplikasi jadi file baru) — real Exam Session Runtime (P7-001) → submit formal → `GET /concepts/.../mastery-breakdown` (P4-001, 0 baris disentuh) balikin `weak:true` utk skill yang sengaja salah (termasuk via word-limit engine) dan `weak:false` utk yang benar. Route-coverage **98/98, TIDAK bertambah sama sekali** dari P10-001 s/d P10-003 — bukti langsung "reuse, bukan endpoint baru". Total 413/413 test (dari 399), `bunx tsc --noEmit` bersih. **Dideferred eksplisit** (kandidat lanjutan terpisah, bukan hilang): TOEFL (iBT+ITP)/PTE, 11 dari 14 tipe soal IELTS Reading sisanya, IELTS Writing Task 1/2 generator, IELTS Speaking Part 1/2/3, Pre-Exam Diagnostic/Foundation modules (kerja authoring konten, bukan engineering), FE `QuestionRenderer` buat 3 tipe baru. Detail lengkap: `docs/tickets/phase-10.md`.

**Phase 11 done — roadmap-Fase 6.9-16 (Gamification: model bisnis), yang KEDUA dari 4** (`titian-backend-bun`+`titian-web`, 2026-09-02): §6.9/§6.14 (kerangka konseptual)/§6.15 (butuh analytics infra)/§6.12 (disupersede ADR-0005) tidak dikerjakan sebagai fitur — cuma §6.10/§6.11/§6.13/§6.16 yang genuinely actionable. **P11-001**: label "Kredit" → "Diamond" di `titian-web/src/components/dashboard/stat-cards.tsx` (1 lokasi, dikonfirmasi grep — nama internal `credits`/`Credits` TIDAK diubah, ADR-0005 sudah mengunci pemisahan itu); screenshot dev-server dilewati (perubahan 1 kata, 0 risiko layout, dicatat eksplisit sebagai yang tidak diverifikasi visual). **P11-002**: `subscriptions` table (Plus 100/Pro 300 credit/bulan, angka contoh ADR-0005) + `POST /subscriptions/subscribe` + `GET /subscriptions/me`. **`GET /me/credits` baru ditemukan HARUS dibangun**: charge sudah dipakai sejak ADR-0005/Phase 1, tapi nol endpoint pernah expose cara BACA saldo sendiri — dashboard FE selama ini menampilkan angka placeholder, bukan data asli. **P11-003**: **dead capability lain ketemu** — `transactions.expiresAt` sudah ada di schema sejak ADR-0005 (dirancang buat expiry allowance), nol kode pernah membaca/menulisnya sampai sekarang; ADR-0005 sendiri eksplisit minta "cron job" tapi tidak ada infra scheduling di backend ini — diganti **lazy sweep on-read** (pola sama FRSS/streak), `transactions.type` dapat nilai baru `expire` (audit trail, bukan UPDATE/DELETE baris lama). Idempotent utk 2 panggilan berurutan, TAPI belum di-hardening buat race 2 sweep bersamaan (keterbatasan MVP diketahui, sama kelas P9-004's capacity race, didokumentasikan bukan disembunyikan). **P11-004**: `AdProvider`/`StubAdProvider` (pola persis `PaymentProvider`/`StubQrisProvider` P9-007) — nonton iklan (simulasi, 0 network call asli) buka 1 aksi AI berbayar (grammar evaluation) tanpa charge diamond. **1 penyimpangan sadar dari precedent P9-007**: `AdProvider` TIDAK dijadikan field wajib `AppState` (yang berarti ~57 file test harus diedit ulang) karena `StubAdProvider` tidak pernah butuh skenario test yang beda perilaku (selalu "berhasil", tidak seperti `AIProvider`/`PaymentProvider`) — jadi cukup parameter opsional dengan default, bukan DI penuh. **P11-005**: checkpoint (`tests/phase11-checkpoint.test.ts`) sengaja lewat endpoint SUBSCRIBE ASLI (bukan cuma insert transaction manual seperti test unit) — subscribe → balance 100 → simulasi waktu lewat → balance 0 otomatis. Route-coverage 102/102 (98+4 baru: `GET /me/credits`, `POST /subscriptions/subscribe`, `GET /subscriptions/me`, `POST /ads/watch`). Total 429/429 test (dari 413). **Dideferred eksplisit**: payment gateway subscription asli, ad SDK asli (AdMob dst), funnel bisnis tracking (§6.15, butuh analytics infra terpisah), §6.6 Kids Gamification (sudah di luar §6.9-16 yang diminta). Detail lengkap: `docs/tickets/phase-11.md`.

**Phase 12 done — roadmap-Fase 8 sisa (Marketplace lanjutan), yang KETIGA dari 4** (`titian-backend-bun`, 2026-09-02): riset ulang §8.1/8.2/8.6/8.7/8.9-11 sebelum motong scope (`docs/tickets/phase-12.md`'s "Keputusan scope"). **1 gap nyata ketemu dari riset, bukan exclusion sadar**: §8.14 (Cancellation Policy) TERNYATA cuma setengah dibangun Phase 9 — `POST /enrollments/{id}/cancel` (P9-007) cuma jalur SISWA, jalur tutor-cancel tidak pernah dibangun DAN tidak dicatat sebagai deferred di `phase-9.md`. **§8.10 (Student Diagnosis) SENGAJA TIDAK DIKERJAKAN, TIDAK DITANYAKAN JUGA** — sumbernya sendiri eksplisit minta keputusan privasi/consent dulu sebelum dibangun; ini keputusan kebijakan, bukan arsitektur, jadi didefer eksplisit dengan alasan dicatat tebal di sini supaya tidak menghentikan urutan 4 item, TAPI TIDAK BOLEH dibangun tanpa keputusan eksplisit user kapan pun nanti. §8.1 (marketplace 8 kategori)/§8.2 (tutor dashboard FE)/§8.11 (Learning Package redeem-sesi) semua didefer penuh (infra tidak ada / kerja FE murni / mekanisme genuinely rumit). **P12-001**: tutor-cancel — SELALU 100% refund (beda dari cancel siswa yang tingkatan waktu), `enrollments.cancelled_by` kolom baru (`'student'`/`'tutor'`) buat P12-004's cancellation-rate akurat. **P12-002**: Assignment — submission + tutor grading manual; AI evaluation DIDEFER eksplisit (`ai_writing_evaluation_service.runWritingEvaluation` terikat erat `attempts.id`, tidak alami di-reuse tanpa memaksakan skema — submission tanpa skor sama sekali lebih buruk dari manual grading yang solid). **P12-003**: Gradebook — agregasi attendance+assignment murni, sengaja TIDAK menyentuh mastery engine (sumbernya sendiri eksplisit: 2 metrik beda tujuan). **P12-004**: Tutor Reputation — rating (BARU, dari `tutor_reviews`, 1x per enrollment `completed`, re-review = 409 bukan update) + statistik yang sudah computable dari data existing (`student_count`/`lesson_count`/`completion_rate`/`cancellation_rate`, semua on-read agregasi, nol tabel cache baru selain `tutor_reviews` sendiri); "response time" DIDEFER (butuh sistem messaging yang tidak ada). **P12-005**: checkpoint (`tests/phase12-checkpoint.test.ts`) — 1 cohort 2 siswa, A diselesaikan penuh (attendance+assignment+completed+review), B di-tutor-cancel; gradebook benar buat A (B hilang total dari roster, bukan baris 0), reputation A tercermin BERDAMPINGAN dengan `cancellation_rate>0` dari B tanpa saling campur. Route-coverage 110/110 (98+12 baru dari P10/P11/P12 gabungan). Total **447/447 test** (dari 429). Detail lengkap: `docs/tickets/phase-12.md`.

**Phase 13 done — roadmap-Fase 9 (Proctoring), yang KEEMPAT/TERAKHIR dari 4** (`titian-backend-bun`, 2026-09-02): mengaktifkan `proctoring_policies`/`proctoring_sessions`/`proctoring_events`, dead sejak ADR-0001 (pola dead-capability yang sama seperti `exam_sessions` sebelum P7-001). **Consent-first ditegakkan literal**: `POST /exam-sessions/{id}/proctoring-session` menolak `consent !== true` dengan 422 SEBELUM baris apa pun di-insert — tidak ada state "sesi dibuat tapi belum consent" yang bisa eksis. **Risk Engine murni fungsi** (`computeRiskScore`, bobot `low=1`/`medium=3`/`high=7`, di-cap 100) — dites eksplisit BAHWA skor tinggi (5 event `high` = 35) TIDAK PERNAH mengubah `review_status` dengan sendirinya, cuma `POST /proctoring-sessions/{id}/review` (manusia, role `platform_admin`/`org_owner`/`academic_director`) yang bisa. **Privacy-by-design nyata, bukan slogan**: `GET /proctoring-sessions/{id}` balikin UNION TYPE beda struktur untuk siswa pemilik (status/consent/review saja) vs staff (paket penuh + `events`+`risk_score`) — siswa tidak pernah bisa lihat detail event mentah, supaya tidak bisa "belajar" persis apa yang memicu deteksi. **Retention lazy-sweep** (pola sama P11-003, tidak ada scheduler): `GET` memicu purge evidence yang lewat `policy.retention_days`, `evidence_id` di-null-kan + `purged_at` dicatat — baris `assets` itu sendiri TIDAK PERNAH dihapus (siklus hidup asset domain Drive terpisah, cuma referensi proctoring-nya sendiri yang diputus). **Re-review DIPERBOLEHKAN** (beda sengaja dari `tutor_reviews`'s 409-on-repeat P12-004) — keputusan moderasi internal butuh bisa dikoreksi. **Dideferred eksplisit, dicatat di `phase-13.md` dan sini**: capture kamera/mic/screen sungguhan (FE+computer vision, jauh di luar scope), encryption-at-rest (keputusan infra/provider, butuh kredensial produksi — kelas sama payment gateway asli P9-007), audit sistem-lebar (cuma jejak 1 tindakan sensitif — keputusan review — yang dibangun, bukan audit generik semua endpoint), `teacher/tutor` "kelas sendiri" review scope (ADR-0006 matrix punya baris ini tapi butuh linkage exam_session↔cohort yang tidak ada — MVP cuma implementasi `platform_admin`/`org_owner`/`academic_director`). Route-coverage 116/116 (110+6 baru). Total **461/461 test** (dari 447). Detail lengkap: `docs/tickets/phase-13.md`.

**Urutan 4 roadmap-Fase yang diminta user (2026-09-02) SEKARANG SELESAI SEMUA**: roadmap-Fase 5.3 (Phase 10) → roadmap-Fase 6.9-16 (Phase 11) → roadmap-Fase 8 sisa (Phase 12) → roadmap-Fase 9 (Phase 13). Tidak ada fase berikutnya yang menunggu — instruksi berikutnya murni tergantung user, bukan urutan yang sudah ditentukan.

---

## Audit gap penuh + instruksi "kerjakan semuanya kecuali Phase 7 & 10+" (2026-09-03)

Diminta user untuk cek ulang SEMUA dokumen `agent/` (termasuk `agent/lms full.md`, 16758 baris, belum pernah dibaca penuh sebelumnya — 3 dokumen ringkas lain sudah mengekstrak sebagian besar tapi TIDAK SEMUA keputusan konkret darinya) terhadap apa yang sudah dibangun. Temuan lengkap disampaikan ke user sebagai laporan chat (tidak disalin ulang di sini, poin-poinnya jadi dasar ticket-phase di bawah). Ringkasan temuan kunci yang BARU (tidak ada di ticket-phase manapun sebelumnya):
- **Gap terbesar: backend Phase 6/8/9/11/12/13 nyaris NOL frontend** — `titian-web` cuma punya landing/login/beranda/belajar/latihan/profil/progres/studio. Marketplace, LMS/class-management, tutor dashboard, wallet, sertifikat, subscription/Diamond, ads, leaderboard/league/achievement/daily-mission, proctoring — SEMUA backend-only, dikonfirmasi `grep` `api-client.ts`: nol `marketplaceApi`/`walletApi`/`subscriptionApi`/`proctoringApi`/`gamificationApi`.
- **Collaborative Canvas / real-time collaboration** (`lms full.md` §14-17) — engine terpisah (live tutor↔student writing view, Learning/Assessment/Exam mode, document versioning) — TIDAK PERNAH masuk ticket-phase manapun, tidak ada di 3 dokumen ringkas.
- **AI Tutor Runtime** (§18) seperti dirancang asli (loop Teach→Ask→Listen→Evaluate→Adapt→Recommend→Practice, generate mini-exercise dari kesalahan aktual real-time) jauh lebih dalam dari P6-003 yang cuma 1 prompt statis + koreksi setelah submit.
- **AI Content Generation pipeline** (Blueprint→Generate→Validate→QA Agent→Human Review→Publish) + **Content Factory** (§48) — TIDAK PERNAH dibangun. Semua konten di dev DB hasil seed manual/Content Studio manual, bukan AI-generated.
- **Konten kurikulum asli Pre-Basic→C2 sendiri BELUM PERNAH digenerate** — ini isi utama `lms full.md` (ribuan baris modul/unit), gap terbesar kedua setelah FE.
- **Module Completion Rule** (§37 — accuracy≥80% AND aktivitas wajib AND minimum mastery, "skip pakai Diamond") — belum ada gating logic di lesson manapun.
- **Live Exam Monitor + Student Detail dashboard** + **continuous evidence recording mode** (proctoring §11-13) — P13 cuma bangun paket review setelah-kejadian, bukan live monitoring/dashboard.
- **OCR-to-Question pipeline** (§2.6/§39) — belum dibangun.

**User membalas eksplisit: "kerjakan semuanya kecuali phase 7 dan phase 10+, 2 phase itu ditunda dulu."** Ini instruksi pre-otorisasi luas (pola sama urutan 4-item sebelumnya) — TIDAK perlu tanya prioritas per item, tapi urutan eksekusi tetap keputusan saya sendiri (didokumentasikan di sini supaya bisa dilanjut lintas sesi kalau context reset).

**Urutan eksekusi yang dipilih** (alasan: FE dulu karena itu gap ber-ROI tertinggi—backend sudah ada, tinggal dipakai; baru mekanisme engine kecil yang cepat; baru infra baru yang besar/belum ada sama sekali):
1. **Phase 14 — Gamification & Economy Frontend** (leaderboard/league/achievement/daily-mission/credits/subscribe/ads) — paling kecil, paling cepat, murni consume API yang sudah ada.
2. **Phase 15 — Marketplace Frontend** (browse tutor/produk, booking, checkout, enrollment milik sendiri).
3. **Phase 16 — LMS/Class Management Frontend** (tutor: cohort/attendance/assignment/gradebook; siswa: kelas saya/tugas saya/nilai saya).
4. **Phase 17 — Tutor Dashboard + Reputation/Reviews Frontend** (earnings/wallet, ulasan, reputasi).
5. **Phase 18 — Organization Admin Frontend** (member management, RBAC UI).
6. **Phase 19 — Certificates Frontend** (lihat + halaman verifikasi publik).
7. **Phase 20 — Proctoring Frontend** (layar consent, pelaporan event browser, live monitor tutor/admin, student detail).
8. **Phase 21 — Module Completion Rule + §3.9 rescue-mode/tutor-bridge** (ticket engine kecil, quick win — §3.9 blocker-nya (tutor marketplace) sekarang sudah selesai).
9. **Phase 22 — Messaging system** (fondasi buat response-time reputation P12-004, LMS messaging, nanti dipakai Collaborative Canvas juga).
10. **Phase 23 — Marketplace kategori penuh** (Live Class/Self-paced/Bootcamp/Hybrid/Kids Course) + **Learning Package** fleksibel.
11. **Phase 24 — AI Content Generation Pipeline + Content Factory**.
12. **Phase 25 — Generate konten kurikulum asli**, mulai 1 modul Pre-Basic penuh dulu (persis instruksi roadmap sendiri) baru scale.
13. **Phase 26 — Collaborative Canvas** (real-time collaboration, infra paling baru/belum ada sama sekali di backend ini).
14. **Phase 27 — OCR-to-Question pipeline**.

**Item yang TETAP di luar jangkauan meski "kerjakan semuanya"** (bukan ditunda karena prioritas, tapi genuinely tidak bisa dikerjakan sekarang):
- **Student Diagnosis (§8.10)** — masih butuh keputusan privasi/consent eksplisit dari user SEBELUM dikerjakan (komitmen yang sudah dicatat sejak `phase-12.md`) — akan ditanyakan LANGSUNG ke user begitu sesi sampai ke titik itu, bukan diasumsikan.
- **Payment gateway asli, ad SDK asli** — butuh kredensial produksi yang tidak ada di `.env`, tetap stub (`StubQrisProvider`/`StubAdProvider`) sampai user menyediakan.
- **Encryption-at-rest** — keputusan infra/provider hosting, bukan kode aplikasi.
- **Phase 7 (Mobile) dan Phase 10+ (Scale ke domain lain)** — eksplisit ditunda user sendiri, TIDAK dikerjakan sesi ini.

Lihat `docs/tickets/phase-14.md` dst untuk breakdown detail tiap fase begitu ditulis — pola sama semua ticket-phase sebelumnya (MVP-first, Keputusan scope, exit checkpoint).

**Phase 14 done — Gamification & Economy Frontend, item KE-1 dari 14** (`titian-web`, 2026-09-03): `docs/tickets/phase-14.md`, 7 ticket. **Temuan nyata sebelum coding**: `user-store.ts`'s `streakDays`/`xp`/`creditBalance` — ditampilkan di `TopBar` DAN `StatCards` di SETIAP halaman — ternyata angka HARDCODE (`4`/`1280`/`35`) sejak awal proyek, comment kode sendiri sudah bilang "mocked until gamification tickets exist"; endpoint asli (`GET /me/xp`/`/me/streak`/`/me/credits`) sudah ada sejak Phase 8/11 tapi tidak pernah disambungkan. **P14-001**: field itu DIHAPUS dari `user-store.ts` (bukan cuma tidak dipakai — ini data server, bukan client state), diganti hook react-query baru (`use-gamification.ts`/`use-economy.ts`). **P14-002/004/005**: Pencapaian/Peringkat/Liga jadi TAB baru di `/progres` (bukan nav item baru — bottom nav sudah penuh 5 slot). **P14-003**: Daily Mission card baru di `/beranda`. **P14-006**: halaman baru `/profil/diamond` (saldo+subscribe Plus/Pro+watch-ad simulasi), dijangkau dari baris baru di `SettingsList`. **P14-007**: diverifikasi PENUH lewat `Bun.WebView` (`backend: "chrome"`) terhadap backend asli yang jalan — skrip seed sekali-pakai (2 user demo, xp/streak/credits/achievement/daily-mission asli, refresh token diinsert langsung ke `refresh_tokens`, dihapus lagi + data demo dibersihkan dari dev DB setelah verifikasi selesai). **1 race condition nyata ditemukan saat verifikasi, murni bug test-harness bukan aplikasi**: inject `localStorage` dari luar via `evaluate()` bisa "distomp" balik ke `null` oleh `SessionBootstrap`'s `clear()` yang masih in-flight dari page-load sebelumnya — dikonfirmasi via instrumentasi `console.log` sementara (di-revert, `git diff` bersih), fix ada di SKRIP VERIFIKASI (tunggu ~1.5 detik sebelum inject), bukan di kode aplikasi. Subscribe Plus→saldo 42→142, upgrade ke Pro→442, watch-ad→saldo TETAP 442 (jujur, tidak menjanjikan diamond nambah) — semua dibuktikan lewat klik tombol asli+screenshot. Tidak ada perubahan backend sama sekali fase ini — 461/461 backend test tetap hijau, `bunx tsc --noEmit`/`bun run lint`/`bun run build` bersih di `titian-web`. Detail lengkap: `docs/tickets/phase-14.md`.

**Phase 15 done — Marketplace Frontend, item KE-2 dari 14** (`titian-backend-bun` + `titian-web`, 2026-09-03): `docs/tickets/phase-15.md`, 5 ticket. **3 gap backend nyata ditemukan SEBELUM sempat menulis 1 baris FE** (dikonfirmasi grep `app.ts`) — marketplace backend (P9-003/004/007) dibangun ticket-demi-ticket terhadap AC sempit, TIDAK PERNAH terhadap "bagaimana siswa MENEMUKAN produk untuk dibooking": tidak ada `GET /products` (browse platform-wide), tidak ada `GET /products/{id}/cohorts` (lihat batch yang dibuka), tidak ada `GET /me/enrollments` (lihat kelas sendiri). **P15-001** menutup ketiganya (backend, additive, tanpa migrasi baru) SEBELUM FE ditulis — pola sama P9-008's `/complete` gap. `GET /me/enrollments` pakai LEFT JOIN `orders` (enrollment bisa ada sebelum checkout — 2 langkah terpisah P9-004 vs P9-007, `order_status: null` valid). **P15-002/003/004**: `/marketplace` (tab Jelajahi + Kelas Saya, bottom nav sudah penuh jadi tidak nambah nav item — entry point cuma 1 card baru di Beranda) dan `/marketplace/{id}` (detail produk → pilih cohort → enroll → checkout → **"Simulasikan pembayaran berhasil (dev)"** — dilabeli eksplisit sebagai simulasi karena belum ada gateway QRIS asli, pola sama P14's "Tonton iklan (ini simulasi)"). **1 deviasi sadar dari AC awal**: cohort penuh SEHARUSNYA disable tombol enroll duluan, tapi tidak ada cara aman menampilkan "X/Y kursi terisi" ke calon siswa yang belum terdaftar (`GET /cohorts/{id}/students` butuh jadi manager/sudah terdaftar) — jadi ditangani via catch 422 `cohort_full` + pesan ramah saat klik, bukan pre-disable; hasil akhir sama (tidak pernah lihat error API mentah). **P15-005**: diverifikasi PENUH lewat `Bun.WebView` — browse→pilih produk privat→pilih batch→Daftar→Lanjut ke pembayaran→simulasi bayar→status `active`→lihat di "Kelas Saya"→Batalkan, SEMUA lewat klik tombol asli di browser asli (skrip seed 2 user demo, dihapus + data dibersihkan dari dev DB setelahnya). Route-coverage 119/119 (116+3 baru). Total **465/465 backend test** (dari 461, tidak berubah dari fase FE murni P15-002..005). Detail lengkap: `docs/tickets/phase-15.md`.

**Phase 16 done — LMS/Class Management Frontend, item KE-3 dari 14** (`titian-backend-bun` + `titian-web`, 2026-09-03): `docs/tickets/phase-16.md`, 4 ticket. **3 gap backend nyata ditemukan** (pola sama Phase 15): (1) roster/gradebook cuma balikin `student_id` UUID mentah — TIDAK PERNAH JOIN `users` — ditutup `enrollment_repository.listByCohortWithStudent`, `student_name`/`student_email` ditambah aditif ke kedua response; (2) `GET /assignments/{id}/submissions` TIDAK PERNAH ADA — `listByAssignment` dead code sejak P12-002, tutor punya `POST /submissions/{id}/grade` tapi nol cara TAHU `submission_id` apa yang mau dinilai; (3) **ketauan SAAT testing FE**: endpoint baru itu awalnya manager-only, tapi siswa juga butuh cara baca status submission-nya sendiri yang BERTAHAN lintas reload (versi pertama FE cuma nyimpen di state React lokal, hilang begitu di-refresh) — diperbaiki dengan memperlebar endpoint YANG SAMA (manager lihat semua, siswa lihat baris sendiri), pola persis `GET /cohorts/{id}/students`/`attendance`. **Keputusan navigasi**: 1 route `/marketplace/kelas/{cohortId}` melayani tutor DAN siswa, dibedakan dari `GET /cohorts/{id}/gradebook`'s 403 (satu-satunya endpoint cohort-scoped yang MENOLAK, bukan bercabang) — dipakai sebagai sinyal role, bukan duplikasi `canManageCohorts` di FE. Product detail (`/marketplace/{id}`) menukar alur booking jadi tombol "Kelola" kalau `product.tutor_id === me.id`. **Diverifikasi end-to-end lewat `Bun.WebView`**: tutor tandai hadir → buat tugas → siswa submit → tutor nilai → siswa lihat nilai SETELAH RELOAD ASLI (bukan cuma state React) → gradebook 94 = rata2 100%+88 benar. **1 bug test-script ditemukan+diperbaiki saat verifikasi (bukan bug app)**: tombol grade "Nilai" di baris submission dan tab trigger "Nilai" di atasnya punya teks PERSIS SAMA — klik-by-text pertama grab elemen yang salah (tab trigger, bukan tombol grade) — diperbaiki dengan scoping pencarian ke baris submission. Backend perlu di-restart manual sekali saat verifikasi (`bun run src/index.ts` tanpa `--watch` tidak hot-reload). Route-coverage 120/120. Total **469/469 backend test** (dari 467). Detail lengkap: `docs/tickets/phase-16.md`.

**Phase 17 done — Tutor Dashboard + Reputation/Reviews Frontend, item KE-4 dari 14** (`titian-backend-bun` + `titian-web`, 2026-09-03): `docs/tickets/phase-17.md`, 4 ticket. **1 gap backend KRITIS ditemukan SEBELUM sempat menulis UI "Buat Produk"** (ketiga kalinya berturut-turut backend marketplace ternyata dibangun terhadap AC sempit, bukan alur pemakaian nyata): `learning_products.create` SELALU insert `status='draft'` dan TIDAK ADA function/endpoint MANAPUN yang pernah mengubahnya ke `published` — tanpa ditutup duluan, "Buat Produk" akan jadi jalan buntu mutlak (produk tidak akan PERNAH muncul di `GET /products` P15-001). **P17-001** menutup ini SEBELUM FE ditulis: `POST /products/{id}/publish` baru, ownership-only, idempotent. **Keputusan "Tandai Selesai" sebagai bagian ticket, bukan skip**: alur review (P12-004) butuh `enrollment.status='completed'`, tapi TIDAK ADA UI manapun yang bisa mencapai status itu (`POST .../complete` P9-008 cuma pernah dipanggil test/API langsung) — kalau tidak ditambahkan, P17-003 (review) tidak bisa diverifikasi lewat UI sama sekali, jadi ditambahkan ke `AttendancePanel` (Phase 16) sebagai prasyarat. **`/mengajar`** ("My Teaching") — entry point baru di dropdown TopBar (muncul cuma `role==='tutor'`) — menggabungkan SEMUA yang Phase 15/16 sengaja deferred: wallet, buat+terbitkan produk, buat batch (endpoint sudah ada sejak P9-004, TIDAK PERNAH punya UI sampai sekarang), link "Kelola" ke Phase 16's halaman kelas (reuse penuh, nol kode baru di situ). **Diverifikasi PENUH lewat `Bun.WebView`**: buat produk → terbitkan → BENAR MUNCUL di `GET /products` asli (bukti nyata gap P17-001 tertutup, bukan diasumsikan) → buat batch → siswa beri ulasan 5 bintang → `GET /tutors/{id}/reputation` asli balikin `average_rating: 5, review_count: 1` → badge reputasi muncul di halaman produk → saldo wallet **Rp84.000 dari Rp120.000, PERSIS 70%** P9-007. Route-coverage 121/121 (120+1 baru). Total **472/472 backend test** (dari 469). Detail lengkap: `docs/tickets/phase-17.md`.

**Phase 18 done — Organization Admin Frontend, item KE-5 dari 14** (`titian-backend-bun` + `titian-web`, 2026-09-03): `docs/tickets/phase-18.md`, 3 ticket. **1 bug backend nyata ditemukan** (bukan gap fitur — response shape salah sejak awal): `GET /organizations/{id}/members` balikin `userId` (camelCase, langsung dari `MemberRow` repository TANPA mapping) padahal `api-contract.md` SUDAH sejak awal dokumentasikan `user_id` (snake_case, konsisten SEMUA endpoint lain) — tidak pernah ketauan karena nol test yang assert nama field di situ, Phase 18 FE ini consumer PERTAMA yang benar-benar pakai response-nya. Ditutup di `organization_handler.ts` + dikunci lewat assertion field-shape eksplisit. **Riset scope**: cuma ADA 3 endpoint organization-related di SELURUH backend, dan TIDAK ADA endpoint generik "ubah role jadi X" untuk role manapun selain `tutor` — jadi `/organisasi` SECARA JUJUR cuma menawarkan 1 aksi (Jadikan Tutor), bukan UI promosi role umum yang berpura-pura lengkap. **1 bug React nyata ditemukan pas verifikasi**: 1 user bisa punya LEBIH dari 1 role di org yang sama (`user_organization_roles`'s unique key `(user,org,role)` bukan `(user,org)`) — daftar anggota awalnya pakai `key={userId}` yang collide begitu seseorang dipromosikan (2 baris, key sama, React warning nyata) — diperbaiki jadi `key={user_id}-{role}`. Entry point baru di dropdown TopBar, muncul cuma `org_owner`/`academic_director`/`platform_admin`. **Diverifikasi PENUH lewat `Bun.WebView`**: promosi 1 siswa jadi tutor, baris "Tutor" baru muncul berdampingan dengan baris "Pelajar" lama TANPA reload manual. Route-coverage tetap 121/121 (perbaikan shape, bukan endpoint baru). Total **472/472 backend test** (tidak berubah dari Phase 17, murni fix+FE). Detail lengkap: `docs/tickets/phase-18.md`.

**Phase 19 done — Certificates Frontend, item KE-6 dari 14** (`titian-backend-bun` + `titian-web`, 2026-09-03): `docs/tickets/phase-19.md`, 4 ticket. **1 gap + 1 bug backend nyata ditemukan**: (1) sertifikat TIDAK BISA DIBACA ULANG setelah diterbitkan — `findByEnrollmentId` (P9-006) cuma dipakai internal buat cek konflik re-issue, tidak pernah jadi endpoint baca — ditutup `GET /enrollments/{id}/certificate` baru (`{issued:false}` bukan `null`, pola sama `GET /subscriptions/me`); (2) `GET /organizations/{id}/tutors` JUGA balikin `userId` camelCase — bug KELAS SAMA P18-001's member-list bug, ditemukan proaktif (cek endpoint terkait sebelum FE ditulis, bukan reaktif) — ditutup + `api-contract.md` diperbaiki (dokumennya sendiri ikut salah sejak awal). **Keputusan arsitektur penting**: halaman verifikasi (`/verifikasi/{code}`) DITARUH DI LUAR `(app)` route group — seluruh app dibungkus `RequireAuth`, kalau verifikasi ditaruh di dalam, orang TANPA akun (siapa pun yang scan QR code sertifikat) akan dilempar ke login duluan, padahal backend-nya sengaja publik sejak P9-006. "Terbitkan Sertifikat" ditambahkan ke `AttendancePanel` Phase 16/17 (bukan halaman baru) — konteksnya (per-siswa completed) sudah pas di situ. **Diverifikasi PENUH lewat `Bun.WebView`**: tutor terbitkan → siswa lihat sertifikat SETELAH RELOAD dengan estimasi CEFR nyata dari data mastery (bukan hardcode) → halaman verifikasi publik dicek dari sesi browser YANG BENAR-BENAR TIDAK PUNYA localStorage/auth sama sekali (kode valid DAN kode palsu, keduanya benar). Route-coverage 122/122 (121+1 baru). Total **476/476 backend test** (dari 472). Detail lengkap: `docs/tickets/phase-19.md`.

**Phase 20 done — Proctoring Frontend, item KE-7 dari 14** (`titian-backend-bun` + `titian-web`, 2026-09-03): `docs/tickets/phase-20.md`, 4 ticket. **1 gap kecil ditutup (P20-001) + 1 gap arsitektur besar didefer secara eksplisit**: reviewer tidak punya cara MENEMUKAN sesi proctoring mana yang perlu ditinjau (semua endpoint sebelumnya per-ID) — ditutup `GET /proctoring-sessions` baru. Yang jauh lebih besar: Exam Runtime (P7-001) TIDAK PUNYA FRONTEND SAMA SEKALI (nol referensi `exam-session` di `titian-web`, dikonfirmasi grep) — tanpa itu tidak ada alur nyata untuk memasangi proctoring, jadi P20-003 TERPAKSA membangun halaman ambil-ujian minimal (`/latihan/ujian/{id}`, reuse penuh `questionRenderer`+`assessmentsApi.submit`) sebagai prasyarat, bukan proyek Phase 7 penuh. **Keputusan arsitektur**: consent CUMA BISA DITAWARKAN, tidak bisa dipaksakan — tidak ada kolom di skema manapun yang mengikat `proctoring_policy_id` wajib untuk `assessment` tertentu, jadi `ConsentGate` selalu sediakan "Lanjut Tanpa Pengawasan". **Dieksplisit DIDEFER (didokumentasikan, bukan disembunyikan)**: discovery ujian dari `/latihan` — `assessments` table BERDIRI SENDIRI, nol relasi ke cohort/product di skema manapun, jadi discovery yang benar butuh tabel relasi baru = ticket-phase sendiri, bukan tambahan kecil di sini; `/latihan/ujian/{id}` untuk sekarang cuma bisa dicapai lewat URL langsung. **Diverifikasi PENUH lewat `Bun.WebView`**: siswa mulai ujian → consent gate → setuju → event `tab_hidden` SUNGGUHAN lewat `document.hidden`/`visibilitychange` asli (bukan tombol simulasi) → jawab & kirim → skor 100 → staff buka `/pengawasan` → sesi muncul dengan `risk_score: 4` → buka detail → riwayat 2 event dengan timestamp asli → klik "Bersih" → status berubah. **1 bug operasional ditemukan+diperbaiki selama verifikasi (bukan bug app)**: backend `.env`'s `FRONTEND_ORIGIN=http://localhost:3001` tidak cocok dev server yang sempat jalan di port 3000 default — CORS diam-diam menolak `/auth/refresh`, sesi selalu ke-`clear()` walau token valid — diperbaiki jalankan `next dev` di port 3001. Route-coverage 123/123 (122+1 baru). Total **478/478 backend test** (dari 476). Detail lengkap: `docs/tickets/phase-20.md`.

**Phase 21 done — Module Completion Rule + Rescue Mode/Tutor Bridge, item KE-8 dari 14** (`titian-backend-bun` + `titian-web`, 2026-09-03): `docs/tickets/phase-21.md`, 4 ticket. **ADR-0012 ditulis lebih dulu** (`ALR_Phase_Detail_Breakdown.md` line 821 menandai eksplisit ini butuh ADR baru sebelum implementasi) — 2 keputusan inti: (1) "3 kali gagal" rescue mode diinterpretasi ulang ke granularitas per-jawaban (3 `learning_events` terakhir untuk 1 concept, berurutan, semua salah) karena TIDAK ADA tabel history mastery/attempt-per-concept di manapun (cuma current-value, keterbatasan yang sudah dicatat sejak Phase 4) — bukan reinterpretasi konten, murni pemetaan istilah dokumen ke data yang benar-benar ada; (2) remediasi 8-langkah dari dokumen (visual→analogi→...→retest) TIDAK dibangun (itu Phase 24/25), yang dikirim cuma pesan sederhana + link ke lesson YANG SUDAH TERBIT via `lesson_concepts`, plus Tutor Bridge sebagai link UMUM ke `/marketplace` (BUKAN pencarian tutor per-concept — `tutor_profiles.specializations` itu `jsonb` bebas, tidak terhubung ke `concepts` manapun). **Module Completion Rule (§37 `lms full.md`)**: "module" dipetakan ke `lessons` (tidak ada tabel modules), 3 syarat AND (akurasi≥80%/semua embedded question dijawab/mastery tiap concept terhubung ≥ `weaknessScoreThreshold` YANG SUDAH ADA sejak P4-001, direuse bukan diduplikasi) dihitung on-the-fly (pola sama `getMasteryBreakdown`), "skip pakai Diamond" REUSE LANGSUNG `economy_repository.charge`/`AppError.insufficientCredit` yang sudah dipakai `ai_gateway_service` sejak P11 — bukan mekanisme charge baru. 1 tabel additive (`lesson_completion_overrides`) buat simpan hasil skip. `POST /questions/{id}/check` dapat field baru `rescue_triggered`/`rescue_suggested_lesson_ids` (edge-triggered — cuma true di jawaban yang BARU SAJA melewati ambang batas, tidak menyala ulang di jawaban salah ke-4/5/dst, diverifikasi lewat test snapshot-sebelum-vs-sesudah). **Diverifikasi PENUH lewat `Bun.WebView`**: 3 jawaban salah berturut-turut pada concept yang sama → banner rescue muncul dengan 2 link lesson nyata → card completion (independen dari state React, terbukti bertahan lintas reload penuh) tunjukkan 3 syarat terpisah → klik "Lewati pakai 15 Diamond" → saldo TopBar beneran turun 50→35 → badge status berubah jadi "Selesai". Route-coverage 126/126 (123+3 baru). Total **493/493 backend test** (dari 478). Detail lengkap: `docs/tickets/phase-21.md`, `docs/adr/0012-rescue-mode-tutor-bridge-thresholds.md`.

**Phase 22 done — Messaging System, item KE-9 dari 14** (`titian-backend-bun` + `titian-web`, 2026-09-03): `docs/tickets/phase-22.md`, 4 ticket. **Tidak ada spesifikasi konkret di manapun** — `lms full.md` cuma menyebut "messaging" 2 kali sebagai 1 bullet daftar fitur LMS, jadi seluruh desain (bukan penerjemahan spek yang hilang) dikerjakan dari nol memakai pola yang sudah ada: **1 conversation per (cohort, student)**, direct reuse `enrollments`/`cohorts`/`canManageCohorts` (bukan model DM bebas baru) — `conversations.tutor_id` diambil dari `learning_products.tutor_id` saat dibuat, tidak pernah reassign. **Privasi pesan SENGAJA lebih sempit** dari `canManageCohorts`'s cakupan biasa (attendance/gradebook/assignment) — org_owner/academic_director TIDAK otomatis bisa baca isi percakapan tutor↔siswa, cuma 2 partisipan (`student_id`/`tutor_id`) yang bisa kirim/baca; `canManageCohorts` cuma dipakai staff untuk MEMBUKA conversation atas nama siswa, bukan membaca isinya — dicatat eksplisit sebagai batasan sengaja. **Response-time P12-004 yang eksplisit didefer sejak `phase-12.md` DITUTUP di sini juga** (bukan sekadar infra nganggur menunggu fase lain) — `GET /tutors/{id}/reputation` dapat `avg_response_minutes`/`response_under_1h_rate`, dihitung dari jeda pesan siswa pertama yang belum dijawab sampai balasan tutor berikutnya, di semua conversation tutor itu; `null` (bukan `0`) kalau belum ada history sama sekali. **1 gap ditemukan+ditutup sebelum FE ditulis** (pola sama fase-fase sebelumnya): respons conversation awalnya cuma balikin `student_id`/`tutor_id` mentah — nama peserta ditambahkan (`student_name`/`tutor_name`, JOIN `users` 2x pakai `alias()`) supaya inbox tidak menampilkan UUID mentah. **Dieksplisit DIDEFER**: real-time delivery (WebSocket/SSE — polling React Query dipakai, 15 detik untuk inbox/4 detik untuk thread), read receipts granular, attachment/gambar. **Diverifikasi PENUH lewat `Bun.WebView`**: siswa buka percakapan dari halaman kelas → kirim pesan → inbox tutor tampilkan nama siswa asli + badge unread benar → tutor balas dari thread → `GET /tutors/{id}/reputation` BENAR balikin `avg_response_minutes: 3, response_under_1h_rate: 1` dari data pesan nyata (bukan `null` lagi). Route-coverage 131/131 (126+5 baru). Total **504/504 backend test** (dari 493). Detail lengkap: `docs/tickets/phase-22.md`.

**Phase 23 done — Marketplace Delivery Modes + Learning Package, item KE-10 dari 14** (`titian-backend-bun` + `titian-web`, 2026-09-03): `docs/tickets/phase-23.md`, 4 ticket. **Kesalahpahaman diperbaiki dulu sebelum membangun**: `learning_products.type` (private/group) BUKAN dimensi yang sama dengan "8 kategori marketplace" roadmap — `type` itu kapasitas, kategori roadmap itu MODE PENGIRIMAN, dimensi ORTOGONAL — kolom baru `delivery_mode` (additive, default `live_class` supaya SEMUA produk lama tetap sama persis perilakunya). **Temuan kunci: "Live Class" SUDAH ADA** — cohort+jadwal+attendance (P9-004/005) itu sendirinya sudah "kelas langsung". Yang genuinely hilang cuma `cohorts.meeting_url` (link eksternal Zoom/Meet yang tutor tempel sendiri) — BUKAN "integrasi video-conference" (WebRTC tertanam, butuh infra besar tidak ada). Bootcamp/Hybrid murni LABEL di atas mekanisme yang sudah ada (sumbernya sendiri tidak kasih perilaku beda apa pun). **Kids Course TETAP didefer** — bukan infra blocker seperti 3 lainnya, tapi keputusan posisi produk yang sudah ditunda BERULANG sejak Phase 3/6/8 dengan alasan sama persis ("tunda kalau target awal bukan Kids"), belum ada konfirmasi user. **Learning Package (§8.11) dibangun tapi dipersempit**: redeem sesi lewat endpoint attendance YANG SUDAH ADA (`enrollments.sessions_remaining` diisi dari `session_count` produk, dikurangi 1 SETIAP TRANSISI GENUINE ke 'present' — bukan setiap panggilan, re-mark tanggal sama tidak dobel-kurang, batal-present balikin sesi, tidak pernah negatif) — BUKAN booking-slot-fleksibel penuh dari deskripsi asli §8.11 (didefer eksplisit, butuh keputusan desain sendiri). **1 bug nyata ditemukan+diperbaiki saat verifikasi**: `useMarkAttendanceMutation` invalidate query attendance+gradebook tapi LUPA `cohort-students` — roster tutor sendiri nunjukkin `sessions_remaining` BASI (angka sebelum ditandai hadir) sampai reload manual, padahal backend sudah benar mengurangi. Ditutup dengan invalidate query yang benar, diverifikasi ulang LIVE (3→2→2, tanpa reload, tanpa dobel-kurang). **1 insiden operasional selama verifikasi**: environment/mesin sempat restart di tengah sesi (Postgres container + kedua dev server mati, 17 background task orphan) — kerja KODE (sudah dikomit) selamat penuh, tapi 1 efek-samping attendance yang belum sempat dikomit ke DB (`sessions_remaining` sempat balik ke 3 dari 2) hilang bareng restart — bukan bug aplikasi, cuma state DB dev yang belum di-commit-kan lifecycle-nya; diverifikasi ulang dari awal setelah restart dan hasilnya konsisten. **Diverifikasi PENUH lewat `Bun.WebView`**: tutor buat produk Paket 3x → terbitkan → buat batch → siswa enroll+bayar (`sessions_remaining: 3` nyata dari API) → tutor tandai hadir → **live** turun ke 2 tanpa reload → tandai hadir lagi tanggal sama → tetap 2 (tidak dobel kurang). Route-coverage tetap 131/131 (tidak ada route baru, endpoint yang ada diperluas). Total **515/515 backend test** (dari 504). Detail lengkap: `docs/tickets/phase-23.md`.

**Phase 24 done — AI Content Generation Pipeline + Content Factory, item KE-11 dari 14** (`titian-backend-bun` + `titian-web`, 2026-09-03): `docs/tickets/phase-24.md`, 4 ticket. **Temuan utama SEBELUM menulis kode apa pun**: "Blueprint→Generate→Validate→QA Agent→Human Review→Publish" (roadmap-Fase-2 poin 6, alasan fase ini ada) SUDAH LENGKAP sejak P2-013+P2-014+P2-005 — backend live, dipakai nyata sejak P2-016. **UI trigger-nya JUGA sudah ada** (`add-lesson-dialog.tsx`/`add-question-dialog.tsx`'s tab "Generate dengan AI") — laporan riset pertama (agent Explore) SALAH bilang Studio "pure manual-authoring CRUD" karena scope grep salah (cek `src/app/(app)/studio/` yang tidak ada, padahal Studio di `src/app/studio/` + komponennya di `src/components/studio/`) — ketauan cuma karena verifikasi manual baca file langsung sebelum percaya laporan riset, nyaris membangun ulang sesuatu yang sudah jadi. **Scope jadi jauh lebih kecil**: cuma 3 gap nyata — (1) provenance `generated_by` (ai/human) di `lessons`/`questions`, sebelumnya nol jejak di baris konten sendiri (cuma ada di tabel audit `ai_tasks` terpisah); (2) OCR-to-Question (P2-015) TIDAK PUNYA FRONTEND SAMA SEKALI (dikonfirmasi grep, nol hasil) — ditutup: tab ke-3 "OCR dari gambar" di `AddQuestionDialog`, upload langsung dari dialog (reuse `driveApi.uploadAsset`); (3) OCR belum pernah diverifikasi sukses lewat model vision ASLI sejak dicatat gagal di `phase-2.md` (`HTTP 404` guardrail privasi akun OpenRouter). **Poin (3) BERHASIL saat dicoba ulang** — gambar sintetis MCQ ("I ___ a student. a) am b) is c) are") diekstrak benar oleh model vision nyata (`ocr_raw_text` persis sama, `type=mcq` confident, `data.options` benar, cuma flag `ocr_verification` wajib-review seperti didesain, bukan `ocr_uncertain_type`) — penyebab guardrail sebelumnya hilang tidak diketahui pasti, tapi hasilnya dikonfirmasi nyata. **Item OCR di "Item lepas" (poin 3 lama) DIHAPUS**, bukan didiamkan. Total **515/515 backend test** (tidak berubah dari 515, cuma assertion tambahan di test yang sudah ada). Detail lengkap: `docs/tickets/phase-24.md`.

**Phase 25 done — Generate Konten Kurikulum Asli, item KE-12 dari 14** (`titian-backend-bun` + `titian-web`, 2026-09-03; **satu-satunya fase dari 14 yang user diminta konfirmasi eksplisit dulu lewat pertanyaan**, karena isinya generate konten AI sungguhan berbiaya nyata, bukan sekadar verifikasi infra — user pilih "Proceed with Phase 25 now"): `docs/tickets/phase-25.md`, 4 ticket. **Ditemukan SEBELUM sempat generate konten baru apa pun — 2 bug KRITIS yang membuat SELURUH `/belajar`+`/beranda` tidak berfungsi sejak cutover ADR-0009**: `ACTIVE_CURRICULUM_ID`/`DEFAULT_SUBJECT_ID` (`titian-web/src/lib/curriculum.ts`) menunjuk UUID yang CUMA PERNAH ADA di `titian-backend` (Rust) yang sudah diretire — TIDAK PERNAH ada di backend Bun ini sama sekali. Akibatnya: Module 1 "Alphabet" yang sudah `published` PENUH sejak P2-016 (2026-08-31) **TIDAK PERNAH BISA DILIHAT SATU SISWA PUN** lewat UI nyata sejak cutover — bukan gap kecil, ini `/belajar` (halaman utama belajar) rusak total selama ini, cuma baru ketauan sekarang karena baru sekarang ada yang benar-benar cek "bisa dilihat siswa atau tidak" sebelum menambah konten baru. Ditutup: `useCurriculumTree()` resolve curriculum aktif dinamis lewat `GET /curricula` (bukan hardcode), `DEFAULT_SUBJECT_ID` diperbaiki ke subject yang benar ada. **1 bug backend nyata ditemukan SAAT generate konten**: `stripCodeFence` crash `TypeError` kalau provider AI balikin `content: null` (bukan cuma `undefined` yang sudah dicek) — kejadian nyata, bukan hipotetis, saat generate lesson "writing" Module 2. Ditutup di `ai_provider.ts`, jalur graceful yang SUDAH didesain (`AIProviderError` → `ai_tasks` gagal tercatat → 422) sekarang benar-benar dipakai untuk kasus ini juga. **Modul 2 Pre-Basic ("Numbers") dibangun penuh** — 6 lesson (3 AI-generated + 3 manual, pola sama P2-016 setelah 3x percobaan AI gagal validasi berturut-turut untuk 1 lesson), 5 soal mcq (semua AI-generated), 1 assessment nyata — semua lewat submit-review→publish ASLI (author terpisah reviewer), skor attempt = 100 dikonfirmasi lewat API asli. **1 gap ditemukan+didokumentasikan TAPI SENGAJA TIDAK ditutup**: `POST /assessments/{id}/attempts`'s 409 `attempt_already_in_progress` sudah balikin `attempt_id` untuk resume (niat aslinya tertulis di komentar `error.ts` sendiri), tapi tidak ada `GET /attempts/{id}` dan FE (`AssessmentAttempt`, P3-003) tidak pernah menangkap id itu — siswa yang attempt-nya "nyangkut" macet total di UI. Ini gap Phase 3, bukan Phase 25 — didokumentasikan eksplisit, bukan ditutup diam-diam maupun didiamkan tanpa catatan. Generate seluruh 15 modul Pre-Basic + A1-C2 **dieksplisit DIDEFER** (operasi konten berkelanjutan, bukan 1 ticket-phase, sesuai instruksi roadmap sendiri "jangan generate sekaligus"). Total **515/515 backend test** (tidak berubah, murni bugfix + konten). Detail lengkap: `docs/tickets/phase-25.md`.

**Phase 26 done — Collaborative Canvas, item KE-13 dari 14** (`titian-backend-bun` + `titian-web`, 2026-09-03; **fase KEDUA dari 14 yang user diminta konfirmasi eksplisit dulu**, karena ini infra realtime genuinely nol precedent di backend ini — nol WebSocket, nol pub/sub, nol document-versioning sebelumnya — user pilih "Proceed with Phase 26 now"): `docs/tickets/phase-26.md`, 4 ticket. **Sumber**: `lms full.md` §14-17, dicontohkan lewat writing exam (siswa mengetik esai, tutor lihat live+comment). **Temuan struktural sebelum desain**: alur writing yang sudah ada (`WritingAttempt.tsx`, P3-004) itu 1-shot (attempt baru tercipta PAS submit) — canvas session tidak diikat ke `attempts` (belum ada), diikat ke `lessons` bertipe `writing` langsung, submit canvas reuse `createLessonAttempt`+`submitWritingAttempt` yang sudah ada persis di akhir sesi. **Arsitektur**: WebSocket native Elysia (`.ws()`) + Bun native `ws.subscribe(topic)`/`ws.publish(topic, data)` (1 topic = 1 session id, bukan `Map<sessionId,Set<ws>>` manual) — batasan single-process eksplisit didokumentasikan (tidak scale horizontal tanpa Redis pub/sub). Auth WS lewat query param `?token=` (browser tidak bisa set header custom di WS handshake), direkonstruksi jadi `Headers` sintetis lalu reuse `resolveAuthContext` yang sudah ada. **"Jangan simpan tiap keystroke"**: `document_updated`/`cursor_moved`/`selection_changed` TIDAK PERNAH jadi row `canvas_events` — broadcast langsung + `canvas_sessions.content` 1 baris mutable, `canvas_snapshots` periodik (throttle in-memory 10 detik) untuk riwayat revisi; cuma `comment_created`/`comment_resolved`/`mode_changed` jadi event. **Mode enforcement server-side nyata**: `canvas_service.applyDocumentUpdate` menolak edit tutor di luar mode `learning` (`AppError.forbidden()`), bukan cuma disembunyikan di UI — textarea `readOnly` FE murni cerminan, bukan gerbang asli. **Gap discovery ditemukan+ditutup SEBELUM FE ditulis** (pola berulang di sesi ini): awalnya tidak ada cara tutor menemukan sesi aktif tanpa tahu id-nya duluan — ditambah `GET /conversations/{id}/canvas-sessions` + banner "Sesi live sedang berlangsung" di thread pesan (polling 10 detik). **1 bug FE nyata ditemukan+diperbaiki SAAT verifikasi browser**: `sendDocumentUpdate` tidak meng-update state `content` lokal — karena Bun `ws.publish()` sengaja skip pengirim sendiri, ketikan pengetik sendiri bisa revert ke nilai tersinkron terakhir kalau ada re-render tak terkait (comment peer datang di tengah ketikan); diperbaiki dengan `setContent` optimistic sebelum `ws.send`. **1 insiden operasional (bukan bug produk)**: `500 internal_error` sempat reproducible di endpoint discovery karena 4 proses `Bun.WebView` zombie dari 2 putaran verifikasi sebelumnya (tidak pernah ditutup, terus reconnect) menumpuk beban di background — hilang total setelah proses zombie di-kill + backend restart bersih; endpoint yang sama lolos 2 test otomatis dari awal, jadi bukan bug `canvas_repository`/`canvas_handler`. **Diverifikasi lewat 3 putaran `Bun.WebView` paralel (siswa+tutor) + 1 raw-WebSocket smoke test lebih awal** — rincian jujur soal apa yang terbukti live lewat screenshot vs. lewat state akhir database ada di `docs/tickets/phase-26.md`'s P26-004 (discovery+join dan comment broadcast terbukti live lintas-browser; document-sync dan mode-badge ke peer non-aktor terbukti lewat mekanisme identik + state akhir DB yang benar, bukan screenshot langsung — harness 2-proses OS terpisah tanpa sinyal sinkronisasi, timing-sensitive). Data seed + skrip sekali-pakai sudah dibersihkan/dihapus. Total tetap **526/526 backend test** (dari 515 — 11 test baru murni Phase 26, mode enforcement + no-per-keystroke-row + discovery secara eksplisit dites). Detail lengkap: `docs/tickets/phase-26.md`.

**Phase 27 ditutup administratif — item KE-14 dari 14, TANPA kerja tambahan.** OCR-to-Question pipeline (roadmap-Fase-2 poin 6's bagian OCR) sudah genuinely selesai lewat Phase 2 (`P2-015`, endpoint backend) + Phase 24 (`P24-002`, frontend tab "OCR dari gambar" di `AddQuestionDialog`, diverifikasi lewat model vision asli) — nol gap tersisa untuk dikerjakan Phase 27 secara terpisah. Lihat `docs/tickets/phase-24.md` dan `docs/tickets/phase-2.md`'s P2-015 untuk detail asli.

**14 dari 14 item instruksi "kerjakan semuanya kecuali Phase 7 dan Phase 10+" (2026-09-03) SELESAI.** Phase 7 dan Phase 10+ tetap ditunda sesuai instruksi awal user, belum ada permintaan lanjut. Lihat "Item lepas" di bawah untuk pekerjaan yang genuinely tersisa (semuanya di luar cakupan instruksi ini — keputusan user murni atau ticket-phase terpisah kalau mau ditutup).

**Next action: tidak ada yang di-otorisasi berjalan otomatis lagi** — instruksi "kerjakan semuanya" (item 1-14) sudah tuntas. Menunggu arahan user berikutnya: lanjut ke Phase 7/10+ yang ditunda, kerjakan salah satu "Item lepas" di bawah, atau prioritas baru di luar daftar itu.

**Default platform organization di-rename 2026-09-04**: `organization_repository.ts`'s `PLATFORM_ORG_SLUG` (singleton org yang otomatis diikuti setiap signup baru sebagai `student`, P9-002) diganti dari `"alr-platform"`/`"ALR Platform"` (peninggalan pra-rebrand) jadi `"titian-asa"`/`"Titian Asa"` — row yang sudah ada di dev DB di-rename di tempat (bukan bikin baru) supaya user yang sudah terdaftar tetap terhubung ke org yang sama.

**Phase 28 (ticket-numbering) — Attendance Verification, di luar 14 item "kerjakan semuanya"** (`titian-backend-bun` + `titian-web`, 2026-09-04): fitur BARU yang diminta eksplisit user setelah diskusi kelayakan integrasi Google Meet/Zoom API untuk mencegah tutor mengklaim mengajar dan siswa mengklaim hadir secara curang. `docs/tickets/phase-28.md`, 4 ticket, semua done, 4/4 checkpoint tercentang. **2 keputusan diminta eksplisit ke user dulu** (pola sama Phase 6/9): (1) bangun dulu sebagai stub (kredensial Google Workspace belum ada) — user pilih ini; (2) Google Meet sebagai provider target utama (bukan Zoom) — dipilih user. **Gap struktural ditemukan sebelum desain**: tidak ada konsep "1 sesi kelas dengan jadwal konkret" di skema manapun (`cohorts.schedule` cuma jsonb passthrough, tidak pernah di-parse) — tabel baru `class_sessions` ditambah untuk itu, plus `session_participant_records` sebagai tabel evidence (join/leave/duration mentah, identity matching MURNI lewat `users.google_id` yang sudah ada sejak awal, TIDAK PERNAH fuzzy-name-matching). **Reuse, bukan sistem paralel**: hasil verifikasi menulis ke `attendance_records` yang SUDAH ADA (method='online', slot yang sengaja disiapkan sejak P9-004) — bukan tabel attendance kedua. `MeetingProvider`/`StubMeetingProvider` (persis pola `PaymentProvider`/`StubQrisProvider` P9-007) — `GoogleMeetProvider` SENGAJA belum ditulis (nol kredensial, nol cara mengetesnya). **2 bug nyata ditemukan+diperbaiki**: (1) draf pertama roster-filter pakai `enrollments.status==='active'`, tapi alur manual attendance yang sudah ada TIDAK PERNAH membuat pembedaan itu — ditemukan dari 3 test gagal, diperbaiki jadi `status !== 'cancelled'`; (2) Select peserta simulasi di FE menampilkan raw UUID bukan nama (keterbatasan Radix `SelectValue` yang baru tahu label setelah dropdown pernah dibuka) — ditemukan dari screenshot browser asli, diperbaiki dengan render label eksplisit. **Diverifikasi PENUH**: 13 test otomatis baru + smoke test curl langsung ke dev DB (3 skenario present/late/partial di 1 sesi yang sama, idempotency `sync-attendance` dikonfirmasi, privasi student-cuma-lihat-punya-sendiri dikonfirmasi) + browser `Bun.WebView` asli end-to-end (buat sesi → simulasi tutor+siswa → sync → laporan menunjukkan status berbeda persis sesuai durasi, screenshot disimpan sebagai bukti). Total **539 test** (dari 526 — 13 baru Phase 28; 2 kegagalan di `leaderboard-league.test.ts` dikonfirmasi PRE-EXISTING dan TIDAK TERKAIT lewat `git stash` — data XP akun asli user sendiri bocor ke agregat test platform-wide). **Bonus temuan operasional saat verifikasi** (di luar scope Phase 28, ditemukan user sendiri saat coba `/mengajar`): `platform_admin` tanpa `tutor_profiles` dapat 500 mentah (FK violation) saat buat produk — `learning_product_service.createProduct` diperbaiki dengan lazy-provisioning `tutor_profiles`, pola sama `assignDefaultStudentRole`. **Update 2026-09-05 (P28-005) — GoogleMeetProvider ASLI, bukan lagi rencana**: eligibility test nyata dilakukan bersama user (OAuth Client ID Desktop-app terpisah di Google Cloud Console, scope `meetings.space.readonly`+`meetings.space.created`) membuktikan **akun Google personal `@gmail.com` dengan Google One 2TB (BUKAN Workspace) BISA akses penuh** `conferenceRecords`/`participants` (dites dengan meeting nyata 3 peserta — data join/leave/identitas lengkap, `signedinUser.user` formatnya SAMA dengan `users.google_id` yang sudah ada) DAN `autoRecordingGeneration` lewat API (recording nyala otomatis tanpa klik manual). Klaim "harus Workspace" dari sumber sekunder yang dibaca sebelumnya **terbukti salah/ketinggalan zaman** untuk kasus ini. `attendanceReportGenerationType` (laporan kehadiran bawaan Google, Developer Preview) dicoba tapi TIDAK bisa diakses lewat REST langsung — didefer, tidak menghalangi karena data mentah `participants` sudah cukup. **`google_meet_provider.ts` (`GoogleMeetProvider`) ditulis dan live** — `index.ts` pakai provider ini kalau kredensial (`GOOGLE_MEET_CLIENT_ID`/`_SECRET`/`_REFRESH_TOKEN`, organizer tunggal = akun personal user sendiri) ada di `.env`, fallback stub kalau tidak. **1 bug nyata ditemukan dari smoke test pertama**: `class_session_service.createSession` hardcode `meeting_provider='stub'` ke DB apa pun provider yang benar-benar dipakai — celah yang berarti sesi ASLI tetap bisa menerima `simulate-participant` (harusnya ditolak) — diperbaiki dengan `MeetingProvider.name` ditulis apa adanya, bukan literal. Diverifikasi lewat smoke test kode produksi asli (skrip sekali-pakai, dihapus) terhadap cohort nyata milik user sendiri — `join_url` link Google Meet asli berhasil dibuat lewat `class_session_service.createSession` yang sama persis dipakai app. **Model arsitektur 1 akun organizer terpusat** (bukan per-tutor connect akun sendiri) terpakai persis seperti dirancang. Detail lengkap: `docs/tickets/phase-28.md`'s P28-005.

**P28-006 (2026-09-05) — Lihat rekaman dari dalam app**: `GET /class-sessions/:id/recording` baru + tombol "Lihat Rekaman" di panel Sesi Live (poll 15 detik selama `in_progress`), resolve `driveDestination.exportUri` dari Meet API. **P28-007 (2026-09-05) — organizer dipindah ke akun terpisah, bot auto-join DICOBA lalu DIBATALKAN untuk MVP**: kredensial Meet dipindah dari `ndsanja@gmail.com` ke `pedasnyabumbu@gmail.com` (akun dedicated, memisahkan risiko otomasi dari akun pribadi founder) — nol perubahan kode, cuma ganti `.env`. **2 riset empiris penting**: (1) pembatasan peserta (`accessType=RESTRICTED`, `members.create`) TERBUKTI genuinely Workspace-only (403/404 langsung dari akun asli, bukan cuma gap dokumentasi) — diterima sebagai keterbatasan, attendance verification sendiri sudah cukup jadi penyaring "siapa yang diakui hadir"; (2) hak rekam TERBUKTI terikat ke akun yang MEMBUAT space via API, bukan sekadar siapa yang hadir (`pedasnyabumbu` join sendirian ke meeting `ndsanja` → nol recording meski peserta valid). **Bot auto-join dicoba SEKALI sebagai proof-of-concept** (Playwright headful, sesi login persisten `pedasnyabumbu`, buat meeting lewat API `pedasnyabumbu` sendiri lalu bot coba join otomatis) — **Google MENOLAK join itu** ("You can't join this video call... unless invited or admitted by host", padahal akun itu host asli) — dibandingkan tes join MANUAL sebelumnya yang berhasil dengan sesi login SAMA, satu-satunya beda adalah klik dilakukan skrip bukan manusia. **Kesimpulan: Google mendeteksi pola join terprogram dan memblokirnya aktif** — bukan risiko teoretis, sudah terjadi nyata di percobaan pertama. Percobaan dihentikan saat itu juga (tidak diulang, menghindari akun makin ter-flag). **User memutuskan eksplisit: bot auto-join DIBATALKAN untuk MVP** (bukan ditunda) — recording tetap manual (admin join sendiri kalau mau merekam), attendance verification TIDAK terpengaruh (selalu jalan begitu ada peserta join, terlepas recording nyala atau tidak). 3 alternatif dicatat untuk pertimbangan nanti TANPA ada yang dieksekusi: per-tutor OAuth linking (tutor pakai akun sendiri, butuh verifikasi app Google berminggu-minggu untuk scale), Zoom (`auto_recording`+`join_before_host`, server-side jadi tidak berisiko deteksi anti-bot, belum dites), dan Drive-sharing bridge (kasih izin akses file rekaman otomatis ke peserta kelas, independen dari keputusan bot, belum dibangun). Detail lengkap: `docs/tickets/phase-28.md`'s P28-006/P28-007.

Item lepas (tidak memblokir ataupun diblokir oleh fase manapun, kerjakan kapan pun ada slot/prioritas berubah):

4. **User: whitelist `http://localhost:3001` di Google Cloud Console** (lihat "Blocker aktif" — port berubah dari 3000 ke 3001 karena port 3000 sering dipakai proyek lain di mesin ini). Verifikasi FE sesi-sesi sebelumnya (baik layar learner maupun Content Studio) **bypass** popup Google OAuth (refresh token asli di-mint langsung ke DB, bukan lewat login browser sungguhan) justru karena blocker ini — begitu di-whitelist, sebaiknya divalidasi ulang sekali lewat klik tombol Google beneran di browser.
5. **User: keputusan soal `api-contract.md` "Base URL: /api/v1"** (lihat "Deviasi") — pilih (a) hapus baris itu dari dokumen, atau (b) rencanakan reverse proxy production yang menyuntikkan prefix itu (kode tidak perlu berubah). Bukan blocker teknis.
6. `titian-mobile` (Expo, repo git sendiri) — mulai kapan pun user siap, ditunda bukan dibatalkan.
7. P0-010 (CI) — CI config per repo (`titian-web`, `titian-backend-bun`, nanti `titian-mobile`). `titian-backend-bun` sekarang 224 test, layak masuk CI kapan saja.
8. **Catatan operasional:** `titian-web` sekarang jalan via `bun run dev`/`bun run build`/`bun run start` (migrasi pnpm→Bun, lihat "Ticket selesai"), bukan lagi `npm run dev`. Dev server manapun (`titian-web`, `titian-backend-bun`) yang dijalankan manual di background selama sesi kemungkinan sudah tidak jalan lagi begitu sesi/mesin ini berakhir (bukan proses yang di-daemonize) — restart manual kalau mau demo browser lagi (`docker start titian-bun-postgres` juga, kalau containernya ikut berhenti).
9. **Gap resume-attempt (P3-003, ditemukan Phase 25)**: `POST /assessments/{id}/attempts`'s 409 `attempt_already_in_progress` sudah balikin `attempt_id` (niatnya tertulis di komentar `error.ts` sendiri: "so the client can resume it instead of retrying blindly"), tapi tidak ada `GET /attempts/{id}` untuk ambil ulang soal-soalnya, dan `AssessmentAttempt` (FE) tidak pernah menangkap `attempt_id` dari error itu — siswa yang attempt-nya "nyangkut" (reload di tengah kuis, koneksi putus) macet total, cuma lihat teks error mentah. Perbaikan butuh 1 endpoint backend baru + alur resume FE — bukan quick-fix, ticket-phase sendiri kalau mau ditutup.
