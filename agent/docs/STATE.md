# ALR — Current State
Last updated: 2026-08-23 by p2-session-1-2

## Fase aktif
**Phase 1 — Backend Core resmi tertutup** (semua 13 ticket P1-001 s/d P1-013 done, semua 5 checkpoint keluar Phase 1 dicentang — lihat `docs/tickets/phase-1.md`, diverifikasi ulang 2026-08-23: 79 test lulus, working tree bersih di `titian-backend`/`titian-web`/`alr`). Phase 0 belum tertutup formal (P0-010 CI dan mobile scaffold masih terbuka) — ini disengaja atas instruksi eksplisit user, lihat "Deviasi".

**Phase 2 — Content Engine & Curriculum Pipeline: sesi 1 + sesi 2 selesai** (`docs/tickets/phase-2.md`, 17 ticket total). Atas instruksi eksplisit user ("buat sekalian yang terbaik arsitektur... yang bisa menopang fase 2 berikutnya"), sesi 1 (2 ADR) dan sesi 2 (3 ticket infra) dikerjakan sekaligus di sesi yang sama alih-alih berhenti untuk approval user di antara keduanya seperti rencana semula — user secara eksplisit mengizinkan lanjut. **P2-001 s/d P2-005 done** (5 dari 17 ticket). P2-006 dst (ALM parser, authoring API, AI pipeline, OCR) **belum dimulai** — lihat "Next action".

## PENTING — baca sebelum mulai kerja
1. **Ada proyek lain di `/home/john/Dev/sanja-workspace/lms/`** (sejajar dengan `alr/`, di luar folder ini) berisi `alr-backend`/`alr-web`/`alr-mobile` versi lain yang jauh lebih maju (Phase 0-2 sudah banyak selesai di sana), dengan dokumentasi sendiri (`ALR_TECHNICAL_BLUEPRINT.md`, bukan `agent/docs/`). **User sudah eksplisit konfirmasi (2026-08-23): `lms/` tidak dipakai lagi, abaikan.** Proyek yang aktif adalah `alr/` ini. Jangan tanya ulang soal ini kecuali user membawanya lagi.
2. **Struktur bukan monorepo** — lihat bagian di bawah.

## Struktur repo (PENTING — beda dari `ALR_Build_Roadmap.md`)
`ALR_Build_Roadmap.md` menyebut monorepo sebagai bagian stack "terkunci". Itu sudah **tidak berlaku** —
atas instruksi eksplisit user (2026-08-23), proyek pindah ke **satu repo git independen per aplikasi**
supaya gampang dikerjakan tim terpisah:
- `alr/titian-web/` — Next.js 16, repo git sendiri. **Sudah di-scaffold, sudah di-commit.**
- `alr/titian-backend/` — Rust/Axum, repo git sendiri. **Sudah di-scaffold, migration jalan, sudah di-commit.**
- `alr/titian-mobile/` — Expo/React Native, repo git sendiri. **Belum dibuat — ditunda atas instruksi user ("mobile nanti aja").**
- `alr/agent/` — dokumentasi bersama (roadmap, ADR, ticket, file ini) — satu-satunya isi repo `alr/` itu sendiri.

Konsekuensi: tidak ada lagi `packages/shared` package. Kontrak tipe (zod schema dll) di-duplikasi
manual per-repo saat dibutuhkan — lihat `titian-web/src/lib/schemas.ts`. Setiap repo build & deploy
independen; yang menyatukan mereka cuma kontrak yang didokumentasikan di `docs/api-contract.md` dan
`docs/domain-model.md`, bukan tooling. Detail lengkap di catatan P0-009 (`docs/tickets/phase-0.md`).

## Ticket sedang dikerjakan
Tidak ada yang in-progress. P2-001 s/d P2-005 done. Next up: P2-006 (ALM Parser) — sesi 3 di `docs/tickets/phase-2.md`, lihat "Next action".

## Ticket selesai
- P0-001 s/d P0-006: ADR-0001 s/d ADR-0006 [done]
- P0-007: Migration SQL — 10 grup migration (`titian-backend/migrations/`, grup ke-10 ditambah di sesi P1-001), jalan + revert teruji, 34 tabel terverifikasi [done]
- P0-008: API contract cross-check — tidak ada mismatch field antara `api-contract.md` dan schema [done]
- P0-009: `titian-web` + `titian-backend` selesai di-scaffold, jalan, di-commit. `titian-mobile` masih belum (ditunda) — lihat status P0-009 di `docs/tickets/phase-0.md` untuk detail, ticket ini tetap tercatat in-progress di sana.
- P0-011: file protokol & STATE.md [done]
- **P1-001: Auth Google OAuth + JWT** — `POST /auth/google/callback` dan `POST /auth/refresh` jalan penuh di `titian-backend`, 11 integration test lulus. Detail lengkap di `docs/tickets/phase-1.md`.
- **`titian-web` disambungkan ke auth asli** — tombol login mock diganti Google Identity Services beneran (`src/components/auth/google-sign-in-button.tsx`), session di-persist via `auth-store.ts` (cuma refresh token yang disimpan, access token di-re-derive tiap boot lewat `/auth/refresh` — lihat `session-bootstrap.tsx`), route `(app)/*` di-gate lewat `require-auth.tsx` (redirect ke `/login` kalau belum login). CORS ditambah di backend (`FRONTEND_ORIGIN`) supaya browser boleh manggil API cross-origin.
- **P1-002: User & Profile + middleware AuthContext** — `GET /users/me` dan `GET /organizations/{id}/members` jalan penuh, 7 integration test lulus (total 18 test di `titian-backend`). `AuthContext` (`middleware/auth_context.rs`) sekarang jadi extractor axum yang dipakai handler manapun yang butuh auth — pola ini yang bakal dipakai P1-003 dst. Detail lengkap di `docs/tickets/phase-1.md`.
- **P1-003+P1-004: Content API + Question Bank API** — `GET /curricula/{id}/tree`, `GET /lessons/{id}`, `POST`/`GET /question-banks/{id}/questions` jalan penuh, 11 integration test lulus (total 29 test) + smoke test manual end-to-end lewat curl pakai data seed asli (`titian-backend/seed.sql`, aman dijalankan ulang). `service/permissions.rs` dipecah jadi `require_permission` (role-only, buat resource platform-wide kayak `curricula`/`question_banks` yang di `domain-model.md` memang tidak punya kolom `organization_id`) vs `require_permission_in_org` (buat resource yang scoped ke 1 org kayak org members). Detail lengkap di `docs/tickets/phase-1.md`.
- **P1-005+P1-006: Assessment API + Attempt Submit/Scoring** — `GET /assessments/{id}`, `POST /assessments/{id}/attempts` (tidak pernah bocorkan `correct_answer`, tolak attempt in-progress duplikat), `POST /attempts/{id}/submit` (auto-grade mcq/fill_blank via `service/grading.rs`, tolak submit ganda/kurang jawaban) jalan penuh, 10 integration test + 4 unit test grading lulus (total 43 test) + smoke test manual end-to-end lewat curl (skor 100 buat semua benar, 422 `missing` yang isinya benar, 409 submit ganda — semua cocok kontrak). Detail lengkap di `docs/tickets/phase-1.md`.
- **P1-007: Learning Event Writer** — setiap `POST /attempts/{id}/submit` sekarang menulis 1 `learning_event` (`event_type=question_answered`) per soal dijawab, payload `{correct: bool|null, difficulty, question_id, concept_ids}` (`repository/learning_event_repository.rs`, `question_repository::find_concept_ids_for_questions`). `learning_events_created` di response sekarang count asli (bukan hardcode `0` lagi). 2 integration test baru + smoke test manual end-to-end lewat curl. Detail lengkap di `docs/tickets/phase-1.md`.
- **P1-008+P1-009: Mastery Calculator v1 + FRSS Scheduler v1** — `GET /mastery/{concept_id}` dan `GET /review-queue?limit=` jalan penuh. Formula ADR-0002 (`service/mastery.rs::compute`) dan ADR-0003 (`service/frss.rs::apply`) diimplementasi sebagai pure function, masing-masing dengan unit test yang mereproduksi angka contoh manual di ADR **persis** (`mastery_score = 72`, tabel 5 siklus SM-2). `assessment_service::submit_attempt` sekarang memicu recompute mastery + update FRSS untuk tiap concept yang tersentuh submit — **checkpoint keluar Phase 1 poin 4 (submit → learning_events → masteries → frss_schedule) sekarang jalan end-to-end**, diverifikasi manual lewat curl (termasuk retroactive concept-linking dan review-queue gap/due filtering). **Ada 1 deviasi ADR yang sengaja & di-flag** — lihat "Deviasi" di bawah soal trigger FRSS. Detail lengkap (termasuk kenapa `min_gap_hours` tidak butuh kolom baru) di `docs/tickets/phase-1.md`.
- **P1-010: Asset Upload (Cloudflare R2)** — `POST /assets/upload` jalan penuh lewat trait `service/storage.rs::AssetStorage` (real `R2Storage` di prod, `InMemoryStorage` di test — pola sama seperti `GoogleTokenVerifier`). Smoke test manual **ke R2 asli** (bukan cuma test lokal): upload lewat curl → signed URL asli → di-fetch lagi → 200 dengan isi file yang benar. Bucket `lms` (punya bersama proyek `lms/` lama) di-namespace pakai prefix `titian/assets/`. Detail di `docs/tickets/phase-1.md`.
- **P1-011: AI Gateway v1 (grammar_evaluation)** — `POST /ai/evaluate` jalan penuh sesuai alur ADR-0004 (cost estimate → cek saldo → provider → validasi output → `ai_tasks` → charge credit ADR-0005), lewat trait `service/ai_provider.rs::AIProvider`. **Provider asli = OpenRouter (bukan DeepSeek API langsung)** — satu-satunya kredensial AI yang ada — routing ke model `deepseek/deepseek-chat`, `ai_tasks.provider` tetap tercatat `"deepseek"`. Smoke test manual **ke OpenRouter/DeepSeek asli** berhasil penuh (skor grammar check benar, credit ke-charge, semua row DB benar). **2 keputusan penting di-flag** — lihat "Deviasi" di bawah (substitusi provider, dan markdown-fence stripping karena model tidak selalu patuh instruksi "no fences"). Detail di `docs/tickets/phase-1.md`.
- **P1-012: Health Check & Logging Standar** — `x-request-id` middleware terpasang di `main.rs` (`tower_http::request_id`, auto-generate + propagate + masuk ke setiap `tracing` span), jadi semua log 1 request kebawa id yang sama otomatis. Error format diaudit manual — semua kode error di semua handler cocok kontrak. **Redis sengaja tidak diimplementasikan** (tetap jujur lapor `"not_configured"`) — tidak ada satupun fitur Phase 1 yang butuh Redis, nambah infra itu buat lolos 1 baris health check adalah scope creep. **1 temuan besar di-flag** — lihat "Deviasi" di bawah soal `api-contract.md`'s "Base URL: /api/v1" yang tidak pernah diimplementasikan. Detail di `docs/tickets/phase-1.md`.
- **P1-013: Integration Test Suite Penuh** — cross-check konfirmasi semua 15 route punya ≥1 integration test; **1 gap nyata ditemukan & ditutup**: `POST /auth/google/callback` belum pernah dites lewat HTTP route asli (cuma fungsi level-bawah) — ditambah `GoogleTokenVerifier::with_seeded_jwk` (`service/google_oauth.rs`) supaya jalur `verify()` produksi asli bisa dites tanpa network call ke JWKS Google. `tests/phase1_checkpoint_test.rs::phase1_exit_checkpoint_end_to_end` — 1 fungsi test, 1 user, 1 router, mengalir lurus lewat kelima checkpoint keluar Phase 1 tanpa reset state di antaranya. **Kelima checkbox "Checkpoint keluar Phase 1" sekarang dicentang** (sebelumnya sengaja kosong sampai ada bukti otomatis, bukan cuma verifikasi manual). Detail di `docs/tickets/phase-1.md`.

**Phase 1 — Backend Core resmi tertutup** (P1-001 s/d P1-013 semua done).

- **P2-001: ADR-0007 Concept Hierarchy** — `concepts.parent_concept_id` (migrasi `0011`), `concept_repository::find_ancestors`/`find_descendants` (`WITH RECURSIVE`), `concept_service::set_parent` (cycle prevention di application layer — Postgres tidak bisa enforce ini di self-referencing FK). 6 test di `tests/concept_hierarchy_test.rs`. Detail di `docs/tickets/phase-2.md`.
- **P2-002: ADR-0008 Content Versioning & Attempt Snapshot** — `attempts.question_snapshot` (migrasi `0012`, captured di submit time, terverifikasi tidak berubah walau `questions` diedit belakangan), `questions.superseded_by`/`lessons.superseded_by` (schema siap, belum ada consumer — itu scope tiket berikutnya). 1 test baru di `tests/assessment_test.rs`. Detail di `docs/tickets/phase-2.md`.
- **P2-003: Content Block SDK v1** — `service/block_schema.rs` (registry 8 tipe block: text/heading/example/audio/video/image/flashcard/question_embed), `content_block_service::validate_and_replace_blocks` (transaksional, cek referensi `question_embed` batched). 4 integration test + unit test per tipe. Detail di `docs/tickets/phase-2.md`.
- **P2-004: Question Type Registry v1** — `service/question_schema.rs` di-refactor dari `match` jadi trait `QuestionTypeValidator` + registry (pola sama `AssetStorage`/`AIProvider`), `matching` ditambah sebagai bukti extensibility. Tidak ada regression di test P1-004. Detail di `docs/tickets/phase-2.md`.
- **P2-005: Content & Question Publish Flow** — `service/publish_flow.rs` (1 state machine dipakai `questions` DAN `lessons`), 6 endpoint baru (`/questions|lessons/{id}/submit-review|publish|reject`), permission baru di ADR-0006 pattern (`curriculum_developer` submit-review, **bukan** publish — cuma reviewer+). Menutup "P1-004b" yang dulu ditunda. 9 integration test + 3 unit test + **smoke test manual end-to-end ke server asli** (submit→publish→publish-ulang-ditolak). Detail di `docs/tickets/phase-2.md`.

**Total test `titian-backend` sekarang: 114** (`cargo test`, butuh `DATABASE_URL` ke Postgres yang bisa CREATEDB).

## Blocker aktif
**Google Sign-In belum bisa dites end-to-end sampai origin di-whitelist.** Waktu dicoba di `http://localhost:3000`, Google nolak dengan `[GSI_LOGGER]: The given origin is not allowed for the given client ID` (403). Ini bukan bug kode — client ID di `Credential.md`/`google-auth.md` belum punya `http://localhost:3000` (dan domain produksi nanti) di daftar "Authorized JavaScript origins"-nya. **Cuma user yang bisa fix ini** (butuh akses Google Cloud Console project punya client ID `1029049482781-...`), bukan sesuatu yang bisa diperbaiki dari kode. Langkah: Google Cloud Console → APIs & Services → Credentials → pilih OAuth client itu → tambahkan `http://localhost:3000` ke "Authorized JavaScript origins" → save (biasanya langsung aktif, kadang perlu beberapa menit). Sisa alur (redirect kalau belum login, error handling, sesi persist, dsb) sudah diverifikasi jalan lewat Playwright — cuma langkah terakhir (klik tombol Google beneran) yang belum bisa dites dari sini.

## Lingkungan dev lokal (state mesin ini, bukan "keputusan" — dicatat biar next action tidak install ulang)
- Rust toolchain (rustup, stable) ter-install di `~/.cargo` — **tidak ada di PATH shell non-interaktif**, selalu `source "$HOME/.cargo/env"` dulu sebelum `cargo`/`sqlx` di sesi baru.
- `sqlx-cli` ter-install (`cargo install sqlx-cli --no-default-features --features rustls,postgres`).
- Postgres lokal jalan via Docker: `docker run -d --name titian-postgres -e POSTGRES_USER=titian -e POSTGRES_PASSWORD=titian_dev_password -e POSTGRES_DB=titian -p 5432:5432 postgres:16-alpine`. Kalau container ini mati/hilang, `titian-backend/.env`/`.env.example` sudah berisi `DATABASE_URL` yang cocok — tinggal jalankan ulang container dengan command yang sama.
- Git identity di-set **lokal per-repo saja** (bukan global) di `titian-web` dan `titian-backend`: `Sanja <ndsanja@gmail.com>`. `alr/` (root docs repo) belum di-set — set kalau mau commit di situ lagi dan belum ke-inherit.
- Dev server `titian-web` (`next dev --webpack`, port 3000) dan `titian-backend` (`cargo run`/binary langsung, port 8080) sempat dijalankan manual di background lintas sesi (termasuk sesi P1-001 ini) untuk verifikasi — kemungkinan sudah tidak jalan lagi begitu sesi berakhir (bukan proses yang di-daemonize, bukan systemd/supervisor).
- `titian-backend` sekarang struktur `lib.rs` + `main.rs` (dipecah di sesi P1-001 supaya `tests/*.rs` bisa import modul internal & build router asli). Kalau nambah module baru, daftarkan di `lib.rs`, bukan `main.rs`.

## Deviasi dari rencana
- `domain-model.md` (SQL migration siap pakai) dan `api-contract.md` ditulis lebih awal dari urutan ticket asli (harusnya P0-007/P0-008) karena isinya derivatif langsung dari ADR-0001 — bukan keputusan baru, jadi tidak perlu ADR terpisah.
- P0-009 dikerjakan sebelum P0-007/P0-008 pada sesi sebelumnya (bukan pelanggaran urutan — P0-009 "Depends on: -", user eksplisit minta mulai dari UI). P0-007/P0-008 lalu dikerjakan di sesi ini, setelah backend discaffold — urutan akhirnya: P0-009 (web) → P0-009 (backend scaffold kosong) → P0-007 → P0-008, bukan urutan linear dokumen asli, tapi semua acceptance criteria tetap terpenuhi.
- **Monorepo → repo terpisah per app** (lihat bagian "Struktur repo" di atas). Ini membatalkan satu poin yang disebut "locked" di `ALR_Build_Roadmap.md`. Keputusan datang langsung dari user.
- Next.js 16 default ke Turbopack, tapi PWA plugin (`@ducanh2912/next-pwa`) hanya support webpack → `titian-web` dev/build script dipaksa `--webpack`.
- shadcn/ui di `titian-web` di-init dengan library **Base UI** (bukan Radix, preset "Nova"/Lucide+Geist) — beda API polymorphism (`render` prop, bukan `asChild`).
- `titian-backend` pakai `docker run` manual untuk Postgres lokal, bukan `docker-compose` — tidak ada docker-compose.yml di repo ini. Kalau P0-010 (CI) atau kerja tim butuh compose file, itu belum dibuat.
- Belum ada ADR baru untuk keputusan stack/struktur repo di atas — dianggap keputusan tooling/proses, bukan keputusan yang mengunci data model, AI gateway, atau permission matrix (cakupan ADR 0001–0006).
- **Tabel `refresh_tokens` ditambahkan** (migrasi ke-10, `titian-backend/migrations/0010_auth_sessions.*`) untuk P1-001 — tidak ada di dump ERD asli ADR-0001. Ini additive (tabel baru, bukan ubah tabel lama), jadi menurut aturan "JANGAN lakukan ini tanpa ADR baru" di bawah tidak melanggar (yang dilarang cuma ubah `learning_events`/`masteries`/`frss_schedule`, dan mengunci struktur tanpa ADR — nambah tabel baru untuk kebutuhan ticket yang sudah disetujui itu normal). Sudah disinkronkan ke `docs/domain-model.md`.
- Google id_token diverifikasi dengan fetch JWKS asli Google (`https://www.googleapis.com/oauth2/v3/certs`) di runtime, di-cache in-memory (`Arc<RwLock<Vec<Jwk>>>`, refresh sekali kalau `kid` tidak ketemu). Belum ada TTL/refresh berkala terjadwal — kalau Google rotate key lebih sering dari yang diasumsikan ini masih aman (refresh-on-miss), tapi kalau butuh proactive refresh itu future work, bukan blocker sekarang.
- **Gotcha zustand-persist (kejadian di sesi ini, catat biar tidak keulang):** jangan panggil `useXStore.setState(...)` di dalam `onRehydrateStorage` milik store yang sama — itu bisa jalan **sinkron selama `create()` masih berjalan**, sebelum `const useXStore = ...` selesai di-assign, jadi referensinya belum ada (gagal diam-diam, hydration flag jadi macet, komponen yang nunggu hydration nge-hang selamanya tanpa error). Pola yang benar: taruh registrasi listener-nya **setelah** `const useXStore = create(...)` selesai, pakai `useXStore.persist.onFinishHydration(fn)` + cek `useXStore.persist.hasHydrated()` buat kasus udah kejadian duluan. Lihat `titian-web/src/store/auth-store.ts` buat contoh polanya.
- **Trigger FRSS: ADR-0003 vs checkpoint Phase 1 (flagged, P1-009 session) — resolusi dipilih, dicatat di sini biar next session tidak kaget:** ADR-0003 bilang update FRSS "dipicu dari learning_event hasil review, bukan attempt biasa" (implikasinya butuh alur "review" terpisah dari submit assessment biasa). Tapi checkpoint keluar Phase 1 poin 4 eksplisit minta submit attempt memicu update `masteries` **dan** `frss_schedule` sebagai 1 jalur kritis, dan Phase 1 belum punya ticket/endpoint apapun untuk alur "review" terpisah. Resolusi: untuk MVP, `submit_attempt` biasa **juga** memicu FRSS update (dianggap "review" sampai ada alur terpisah). Kalau nanti alur practice vs review perlu dipisah beneran (kemungkinan pas recommendation engine dibangun), itu butuh ADR baru yang men-supersede bagian trigger di ADR-0003 — jangan diam-diam diubah lagi tanpa dokumentasi. Detail lengkap di catatan implementasi P1-009 (`docs/tickets/phase-1.md`).
- **AI provider asli untuk P1-011: OpenRouter, bukan DeepSeek API langsung (flagged, P1-011 session):** ADR-0004's routing table nyebut "deepseek" sebagai provider default, tapi kredensial AI yang tersedia di `Credential.md` cuma OpenRouter API key, bukan DeepSeek API key langsung. OpenRouter itu router OpenAI-compatible yang bisa target model DeepSeek lewat model id (`deepseek/deepseek-chat`) — jadi `service/ai_provider.rs::DeepSeekProvider` secara transport HTTP manggil OpenRouter, tapi kolom `ai_tasks.provider` tetap `"deepseek"` sesuai routing table. Kalau nanti dapat DeepSeek API key langsung, tinggal ganti base URL di `DeepSeekProvider`, tidak perlu ubah struktur data/kontrak. Kalau butuh provider lain (OpenAI dst) yang beda API key, itu perlu keputusan baru soal kredensial, bukan sekadar kode.
- **Model AI kadang bungkus JSON dalam markdown fence walau diminta jangan (P1-011 session):** smoke test manual pertama gagal (`ai_output_validation_failed`) padahal isinya JSON yang benar & lengkap — modelnya bungkus dengan ` ```json ... ``` ` walau system prompt eksplisit bilang "no markdown fences". Fix: `service/ai_gateway_service.rs::strip_code_fence` buang fence sebelum parse, JSON di dalamnya tetap divalidasi penuh sesudahnya (bukan bypass validasi ADR-0004, cuma toleran ke noise formatting yang umum dari LLM). Kalau nanti ada task AI lain yang butuh output non-JSON, jangan asal reuse fungsi ini tanpa mikir ulang.
- **`api-contract.md` bilang "Base URL: /api/v1" tapi tidak pernah diimplementasikan (ditemukan & di-flag saat audit P1-012, BELUM diperbaiki — butuh keputusan user):** semua route dari P1-001 s/d P1-011 nempel langsung di root (`/health`, `/auth/google/callback`, dst — lihat `titian-backend/src/routes/mod.rs`), bukan di bawah prefix `/api/v1` seperti yang tertulis di baris pertama `api-contract.md`. `titian-web` sudah manggil endpoint-endpoint ini tanpa prefix (jadi FE dan BE konsisten satu sama lain, cuma dokumennya yang menyimpang). **Belum diperbaiki** karena mengubah salah satu sisi (nambah prefix di router ATAU hapus baris itu dari dokumen) adalah keputusan yang lebih baik dikonfirmasi user dulu — kalau nanti mau nambah reverse proxy/gateway di depan production yang menyuntikkan prefix `/api/v1`, kode saat ini sudah kompatibel (tidak perlu ubah apa-apa); kalau tidak, opsi termudah cuma hapus baris "Base URL: /api/v1" dari `api-contract.md` biar dokumen cocok realita.
- **Sesi 1 dan sesi 2 Phase 2 (5 ticket, 2 ADR) dikerjakan sekaligus dalam 1 sesi, bukan berhenti di antaranya untuk approval user seperti rencana awal di `docs/tickets/phase-2.md`:** rencana strategi eksekusi semula eksplisit bilang "sesi 1 wajib berhenti dan menunggu user review/approve ADR-0007/0008 sebelum sesi 2 dimulai". User secara eksplisit meminta ("buat sekalian yang terbaik arsitektur dan lainnya yang bisa menopang fase 2 berikutnya, karena fase 2 ini memang fundamental") untuk langsung membangun arsitektur fondasi sekaligus — ini dianggap otorisasi eksplisit yang menggantikan gerbang "berhenti-dan-tanya" default, bukan pelanggaran diam-diam terhadap rencana sendiri. ADR-0007/0008 tetap ditulis lengkap dengan Context/Decision/Alternatives/Consequences (bisa direview user kapan saja, status `Accepted` bisa diturunkan lagi kalau user tidak setuju) — cuma tidak ada jeda menunggu konfirmasi eksplisit sebelum lanjut ke implementasi.
- **P1-004b (publish flow, dulu ditunda di Phase 1) resmi ditutup lewat P2-005** — bukan ticket terpisah "P1-004b" seperti nama sementara sebelumnya, digabung 1 ticket generik untuk `questions` DAN `lessons` (state machine identik). Lihat `docs/tickets/phase-1.md` P1-004 dan `docs/tickets/phase-2.md` P2-005 untuk detail lengkap.

## JANGAN lakukan ini tanpa ADR baru
- Jangan ubah struktur tabel di `domain-model.md` (khususnya `learning_events`, `masteries`, `frss_schedule` — dipakai lintas banyak fase) tanpa ADR baru yang menyatakan supersedes.
- Jangan panggil provider AI (DeepSeek/dst) langsung dari business logic — selalu lewat AI Gateway abstraction (ADR-0004).
- Jangan tambah role baru di luar daftar ADR-0006 tanpa update permission matrix.
- Jangan buat allowance subscription sebagai kolom terpisah dari `credits.balance` — semua saldo lewat agregasi `transactions` (ADR-0005).
- Jangan ubah semantik `concepts.parent_concept_id` (containment hierarchy) supaya jadi multi-parent/DAG, atau campur artinya dengan `concept_prerequisites` (yang tetap prerequisite graph, beda konsep) — tanpa ADR baru yang men-supersede ADR-0007.
- Jangan buat endpoint yang mengizinkan edit langsung ke row `questions`/`lessons` berstatus `published` (mutate in-place) — ADR-0008 mengunci "row published tidak pernah dimutasi, edit = row baru + `superseded_by`". Kalau butuh alur edit-setelah-publish yang lebih mulus dari yang ada sekarang (archive manual + draft baru), itu perlu direncanakan sesuai ADR-0008, bukan jalan pintas di kode.

## Next action
**Jalur utama sekarang: Phase 2, lanjut ke sesi 3.** Sesi 1 (2 ADR) dan sesi 2 (Block SDK/question registry/publish flow) sudah selesai — lihat tabel "Strategi eksekusi" di `docs/tickets/phase-2.md` untuk urutan sesi lengkap.

1. **Sesi 3 Phase 2: P2-006 (ALM Parser) + P2-007 (Paste Normalizer) + P2-008 (Lesson Authoring API) + P2-009 (Curriculum/Level/Unit Authoring API)** — jalur authoring manual, dibangun sebelum AI generation pipeline (sesi 5) supaya "jalur manual terbukti jalan dulu" (prinsip roadmap). P2-006 depends on P2-003 (done, Block SDK siap dipakai parser). Baca `ALR_Phase_Detail_Breakdown.md` 2.1/2.3 untuk spesifikasi ALM persis (format directive `:::type ... :::`) sebelum mulai — jangan improvisasi format dari ingatan.
2. Setelah sesi 3, lanjut sesi 4 s/d 8 sesuai tabel di `docs/tickets/phase-2.md` (presigned upload+constitution+Indonesian blocks → AI generation pipeline+QA agent → OCR → validasi Module 1 penuh → test suite).
3. `content_blocks`/`questions`/`lessons` yang sudah ada infra-nya (P2-001 s/d P2-005) belum punya data nyata dibuat lewat pipeline Phase 2 — belum ada demo end-to-end "admin tulis lesson via ALM → published → muncul di `/lessons/{id}`" sampai P2-006+P2-008 selesai. Jangan anggap Phase 2 "kelihatan jalan" hanya dari infrastruktur, checkpoint sebenarnya baru di P2-016/017.

Item lepas (independen dari Phase 2, tidak memblokir ataupun diblokir olehnya — kerjakan kapan pun ada slot/prioritas berubah):

4. **User: whitelist `http://localhost:3000` di Google Cloud Console** (lihat "Blocker aktif") — masih outstanding. Tidak menghalangi kerja backend, tapi menghalangi demo login asli dari browser `titian-web`.
5. **User: keputusan soal `api-contract.md` "Base URL: /api/v1"** (lihat "Deviasi") — pilih (a) hapus baris itu dari dokumen, atau (b) rencanakan reverse proxy production yang menyuntikkan prefix itu (kode tidak perlu berubah). Bukan blocker teknis.
6. `titian-mobile` (Expo, repo git sendiri) — mulai kapan pun user siap, ditunda bukan dibatalkan.
7. P0-010 (CI) — CI config per repo (`titian-web`, `titian-backend`, nanti `titian-mobile`). `titian-backend` sekarang 114 test, layak masuk CI kapan saja.
8. `titian-web`: beberapa layar masih UI shell dari sesi awal (belum disambungkan ke endpoint Phase 1/2 — curriculum tree, assessment, mastery/review-queue, publish flow). Makin relevan begitu Phase 2 mulai menghasilkan konten nyata untuk ditampilkan. **Catatan operasional:** dev server `titian-web` sempat berhenti (interupsi sesi) — perlu `pnpm dev`/binary restart manual kalau mau demo browser lagi, ini bukan regresi kode.
